# fix_apply_r3b — 라운드3 재검증 결함 9건 수정 적용 결과 (26.08.16)

- 대상 : `IFX4GRC60ibOVCWNrd6VQt` 페이지 `3783:890` › 섹션 `3783:906` "정산 수식" **내부에만 쓰기**
- 입력 : `fix_r3_round2.json`(9건) · `qa_r3_final.md`(재검증) · `fix_apply_r3.md`(직전 이력) · `dsl_delta.md`(원고 정본)
- 처리 순서 준수 : ① 판독 위험(#1 · #6) → ② 겹침·관통(#3 · #4 · #5) → ③ 마커 겹침(#7) → ④ 문구 정합(#2) → ⑤ 저위험(#8 · #9)
- 실행 방식 : `use_figma` 원자 호출 7회(쓰기 6 · 읽기 1), per-job try/catch, 폰트 Noto Sans KR — 폰트 로드 실패 0
- 결과 : **적용 9 / 보류 0 / 실패 0**

---

## ① 판독 위험 (2건)

| # | 노드 | 처리 | 실제로 한 일 | 근거 · 특이사항 |
|---|---|---|---|---|
| 1 | 4133:330 | 적용 | §7 둘째 불릿(지급 캘린더 줄)의 리스트 마커를 `#1F2937` 검정으로 전환. 문구 · 좌표 · 줄 수 무변경(x39 y493 491×105 그대로) | **마커 색의 실제 지배자를 실측으로 규명** — Figma 리스트 마커는 해당 문단의 첫 글자가 아니라 **직전 문단을 끝내는 개행 문자(index 59)의 스타일**을 따른다. 본문 문자(60~116)는 이미 검정이었는데 마커만 빨강이던 원인이 이것. `setRangeFills(59,60,#1F2937)` 1회로 해소. 5배 확대 렌더 재판독 결과 **마커 검정 1점(2번 줄) · 빨강 6점(나머지 6줄)** — 확정 줄만 검정으로 분리됐고 나머지 미확정 불릿 마커는 `#FF3B30` 그대로. 문자범위 fill 최종 = `0-60:255,59,48` / `60-117:31,41,55` / `117-349:255,59,48` |
| 6 | 3944:68 · 3944:86 | 적용 | 두 주석 모두 **빨강 구간 앞 공백을 개행으로 치환**해 별도 불릿 줄로 분리. `3944:68` → 3줄(`…배치당 1회 : 이체 수수료` / `차액 수수료 : 실 정산에서만 발생` / **빨강** `요율 값은 화면 미노출 — 서술 금지`), `3944:86` → 2줄(`상태 : … · 기록용` / **빨강** `부채상계 발동 조건 미정`) | 지시서 1안(줄바꿈) 채택 — ` — ` 구분자는 불채택. 3944:68은 이미 본문에 `—`가 있어 구분자를 넣으면 대시가 2개가 되고, 줄 분리가 높이 제약에 걸리지 않았기 때문. **높이 영향 실측** : 3944:68 = 76 → **76 불변**(기존 랩 위치와 분리 지점이 일치), 3944:86 = 38 → 57(+19, 하단 434→453). 3944:86 하단 대역은 빈 구간이고 대각선 3945:2는 같은 y에서 x1446~1498이라 비접촉 — 신규 겹침 0 확인. **빨강 fill 범위 정확** : 3944:68 `0-61:0,0,0` / `61-81:255,59,48`, 3944:86 `0-37:0,0,0` / `37-50:255,59,48` — 새 줄 전체를 정확히 덮고 검정 구간 번짐 0 |

> **특이사항(잔여 관찰)** — 분리된 빨강 줄의 **리스트 마커는 검정으로 남는다.** #1에서 규명한 규칙(마커 = 직전 개행 문자 스타일)에 따라 개행을 빨강으로 만들려 `insertCharacters(…,'AFTER')` · `setRangeFills(개행,빨강)` 두 방식을 시도했으나, Figma가 개행 문자의 fill을 앞 런(검정)으로 정규화해 되돌린다(반환 세그먼트가 `0-61:0,0,0`으로 병합). 6배 확대 재판독으로 3회 확인. **수정 전에도 두 주석에는 빨강 마커가 존재하지 않았으므로 회귀는 아니며**, #1과 달리 위험 방향이 반대다 — #1은 *검정 확정 문장*이 빨강 마커를 달아 미확정으로 오독되는 건이었고, 이 건은 *빨강 문장*이 검정 마커를 다는 것이라 문장 자체가 빨강이므로 미확정 식별은 유지된다. 규격서에 "마커 색은 문단 직전 개행 문자가 지배하며, 검정→빨강 방향 전환은 API로 불가"를 기록해 둘 것을 권고.

---

## ② 겹침 · 관통 (3건) — 간선 논리 연결 전건 보존

| # | 노드 | 처리 | 실제로 한 일 | 근거 · 특이사항 |
|---|---|---|---|---|
| 3 | 4154:2 ↔ 3944:37 | 적용 | 브래킷 라벨 `4143:24`와 캡션 `4154:2`를 **x880 → x720으로 함께 좌측 이동**(y 634 · 660 불변). 캡션 우단 991 ↔ 화살표 3944:37의 해당 y대 최소 x 1004.9 → **약 14px 이격** | **지시서의 "x≥1030 우측 이동"은 불채택 — 기하 불성립.** 지시서는 우측 여유를 주석 3944:36(x1290) 기준 260px로 봤으나, 그 사이를 **대각 화살표 3944:38이 지난다**(캡션 y대 660~677에서 x1220~1254). x1030 배치 시 캡션 우단 1301이 3944:38·3944:36 양쪽과 충돌한다. 좌측은 3944:24(x320~620)까지 100px 비어 있어 x720이 유일한 안전 대역. 캡션 우단 991이 브래킷 4143:23(x880~1460) 시작부와 111px 겹쳐 브래킷과의 시각적 결속도 유지. **화살표 3944:37은 손대지 않음**(시작 (1020,610) · 끝 (1000,700) 원형 보존) |
| 4 | 3944:89 ↔ 3944:68 | 적용 | 점선 화살표 3944:89를 **좌측 레인 우회 5구간 경로로 재조성** — (570,700) → (570,680) → (390,680) → (390,378) → (540,378) → (540,364). `setVectorNetworkAsync`로 6정점 5세그먼트 재작성, 노드 원점 (540,364)→(390,364) | **시작 · 끝 접속점 완전 보존** : 시작 (570,700)=정산 시뮬레이션(3944:87) 상단 경계, 끝 (540,364)=수수료 원장 생성(3944:65) 하단 경계 + 화살촉(`ARROW_EQUILATERAL`) 유지. dash `[6,6]` · stroke `rgb(69,69,71)` 2px 전부 보존. **지시서 대안(3944:68을 y480 아래로 이동)은 불채택** — 점선이 y364~700 전 구간을 지나므로 주석을 세로로 옮겨도 관통이 해소되지 않는다(수평 회피만 유효). 좌측 레인 x390은 y378~680 전 구간이 빈 대역임을 실측 확인. 수평 구간 y378은 3944:65 하단(364)과 3944:68 상단(392) 사이 정중앙 |
| 5 | 3945:2 ↔ 3944:81 | 적용 | 주석 3944:81을 **x1030 → x960으로 좌측 이동**(폭 330 · y550 · h38 불변). 대각선 3945:2의 해당 y대 x = 1357(y550)~1322(y588), 주석 우단 1290 → **최소 32px 이격** | **지시서 1안(폭 330→290)은 불채택 — 리플로우 위험.** 폭을 줄이면 3줄로 늘어 하단 y607까지 내려가는데, 대각선은 아래로 갈수록 좌측으로 이동해 y607에서 x1304.6이 되므로 폭 290(우단 1320)은 **오히려 다시 관통**한다. 좌측 이동은 줄 수 · 높이가 불변이라 리플로우 위험이 0. x960 좌측 인접 노드는 4143:29(x600~920)로 40px 이격. **화살표 3945:2는 손대지 않음**(시작 (1520,372)=이해관계자 배분·이체 하단, 끝 (1220,700)=기간 합산 상단 원형 보존) |

### 겹침 감사 재실행 (벡터 포함, Flow 2장 전수)

지시서 요구대로 **벡터를 포함한 겹침 감사**를 `3944:2`(텍스트 46) · `3944:57`(텍스트 23) 전체에 재실행했다. 각 벡터의 `vectorNetwork`를 절대좌표 폴리라인으로 펴서 모든 세그먼트 × 모든 텍스트 사각형에 대해 선분–사각형 교차 판정.

| 프레임 | 벡터↔텍스트 관통 | 텍스트↔텍스트 겹침 |
|---|---|---|
| 3944:2 | **1건** (범위 밖 선존, 아래 참조) | **0** |
| 3944:57 | **1건** (범위 밖 선존, 아래 참조) | **0** |

대상 3건(#3 4154:2↔3944:37 · #4 3944:89↔3944:68 · #5 3945:2↔3944:81)은 **전건 검출 소멸**.

> **범위 밖 신규 검출 2건 — 손대지 않음(지정 노드 외 수정 금지).** 이번 라운드가 만든 결함이 아니고 `fix_r3_round2.json` 9건에도 없는 선존분이다. 다음 라운드 입력으로 넘긴다.
> 1. **3944:48 ↔ 3944:24** (Flow 3944:2) — 세로 화살표(x500, y604→700, 선정산 결과 확인 → 검증 차이=0)가 주석 `선정산 지급액 = 대상액 − 수수료 + …`(x320~620 · y632~689)를 관통.
> 2. **3944:78 ↔ 3944:77** (Flow 3944:57) — 대각 화살표((900,375)→(1030,495))가 간선 라벨 `아니오 : 시스템 이용료 · 이체 수수료`(x890~1133 · y430~449)의 앞부분(x960~980 구간)을 관통.
>
> 두 건 모두 qa_r3_final ④가 지적한 "겹침 감사에 벡터를 포함하라"는 규칙 결손의 잔여분이며, 이번 감사에서 처음 드러났다.

---

## ③ 마커 겹침 (1건)

| # | 노드 | 처리 | 실제로 한 일 | 근거 · 특이사항 |
|---|---|---|---|---|
| 7 | 3940:12 ↔ 3940:13 | 적용 | 하단 마커 `3940:13`의 **상단만 y8953 → y8958로 잘라냄**(h 16.8 → 11.8, **하단 8969.8 고정**). 상단 마커 `3940:12`는 8868 · h90 원형 유지. 겹침 150×5 → **0**(경계 y8958에서 접점만, 면적 겹침 없음) | **지시서 두 안을 모두 시도한 뒤 실측으로 제3안 채택.** ① 먼저 지시서대로 `3940:12`를 h90→84로 줄였으나, 4배 확대 캡처 재판독에서 **상단 마커 하단선이 "김성호떡볶이 본점 / 2026-07-18 매출 · 42건 3,002,250원" 금액 줄을 관통** — "가리키는 UI 영역을 벗어나면 안 된다"는 제약 위반이라 **즉시 원복**. ② 실측 결과 행 구분선은 y≈8958이고, 원래 `3940:12`의 하단(8958)이 구분선 정위치이며 **`3940:13`의 상단(8953)이 이전 행을 5px 침범**하고 있던 것이 원인. 잘라낸 5px는 애초에 자기 대상(미정산 날짜 머리글 행)이 아니었으므로 손실 없음. 재렌더 확인 : 상단 마커는 `정산 목록 12건`~`3,002,250원` 전체를 온전히 감싸고, 하단 마커는 `2026-07-18 매출 (미정산) 6개 가맹점 · 94건 · 5,339,390원` 한 행을 정확히 감쌈 |

- 레이어명 `hl-정산상세-미정산묶음`은 **개명하지 않음**(지시서의 조건부 항목). 이 마커가 가리키는 대상은 화면상 **날짜 머리글 행**(`2026-07-18 매출 (미정산)`)이고, #8에서 확정한 "정산 배치" 개념과는 층위가 다르다. 원고에도 이 마커를 지칭하는 별도 지시가 없어 현행 유지하고 판정 근거만 남긴다.

---

## ④ 문구 정합 (1건)

| # | 노드 | 처리 | 실제로 한 일 | 근거 · 특이사항 |
|---|---|---|---|---|
| 2 | 3943:33 | 적용 | 2번 불릿 `차액 반영분에 대해 붙는 수수료` → **`차액 반영분에 붙는 수수료`** (조사 ` 대해` 3자 삭제). 노드 361×96 · 4줄 불변 | **지시서 (a)안 채택**(정본 방향 확정, 원고 완화 아님). A4 `4128:207`을 `get_design_context`가 아닌 실측으로 재확인 — 실제 문구는 `차액 반영분에 붙는 수수료 (구성 항목)`. 조사는 지시서가 "반드시 '에'로 맞춘다"고 못박은 대로 정본과 일치시켰다. `(구성 항목)`은 지시서가 명시한 중복 예외를 적용해 생략 — **같은 박스 1번 불릿이 `차액이 아니라 수수료 구성 항목의 하나`**로 이미 같은 말을 하고 있어 괄호를 넣으면 4줄 중 2줄이 '구성 항목'을 반복한다 |

> **원고 잔여 1줄** — `dsl_delta.md:834`는 여전히 `차액 반영분에 붙는 수수료 (구성 항목)` ('A4 §3① 표와 자구 일치')로 적혀 있어, 적용문과 괄호 1개만큼 다르다. 지시서가 허용한 생략이지만 정본에 그 예외가 기록돼 있지 않으면 다음 재조판에서 괄호가 되살아난다. **원고 834행에 "슬라이드는 1번 불릿과의 중복 회피로 `(구성 항목)` 생략, 조사는 '에'로 일치"를 부기할 것**을 권고한다(원고 소관이라 이번엔 손대지 않음).

---

## ⑤ 저위험 (2건)

| # | 노드 | 처리 | 실제로 한 일 | 근거 · 특이사항 |
|---|---|---|---|---|
| 8 | 3940:16 | 적용 | `< 5. 정산일 없는 묶음 = "○일 매출(미정산)" 표기 …` → **`< 5. 정산일 없는 정산 배치 = "○일 매출(미정산)" 표기 …`** | **원고 정본으로 판정 — 정산 배치가 맞다.** 꺾쇠가 스스로 인용한 `(5절 case5)`를 따라가면 `dsl_delta.md:377` [정산 상세] §5 케이스표에 `case5) **미정산 배치** \| 정산일 없음 \| 매출일로 묶고 "(미정산)" 표기` — 원고가 이 대상을 명시적으로 **배치**라 부른다. 날짜 묶음 계열이 아님은 **바로 위 형제 꺾쇠 3940:15**가 방증한다 : `< 6. 좌측 정산 목록 — 정산일 기준 **날짜 묶음** …`로 별개 개념을 이미 날짜 묶음이라 쓰고 있어, 두 꺾쇠가 같은 화면에서 서로 다른 층위를 가리킨다. 치환 대장의 `정산 묶음 → 정산 배치`가 적용될 자리로 확정. 높이 34 불변, 후속 3940:17과 10px 간격 보존 |
| 9 | 3944:41 · 3944:12 | 적용 | 도형 레이어명만 표시 텍스트에 동기화 — `이미지급 (기록만)` → **`바로이체 기록형`**, `선정산 배치` → **`선정산 실행`** | 텍스트 · 좌표 · 색 무변경(3944:41 = 1220,700 240×64 / 3944:12 = 660,300 200×64 그대로). 렌더 대조 결과 **무변화 확인**. 정본 §4 공통규율의 "도형 레이어 이름까지 동기화" 요구 충족 |

---

## 좌표 불변 검증 (수정 전후 동일)

| 항목 | 값 | 판정 |
|---|---|---|
| RD-0 읽는 순서 (4138:66) | y **240** | 불변 |
| HDR-A 개념·용어 (4138:2) | y **580** | 불변 |
| HDR-B 실행·결과 (4138:15) | y **2860** | 불변 |
| HDR-C 차액 계열 (4138:30) | y **6260** | 불변 |
| HDR-D 배분·원장 (4138:42) | y **8460** | 불변 |
| HDR-E 이체·대사 (4138:57) | y **13200** | 불변 |
| 섹션 3783:906 | **4500 × 14560** | 불변 |
| 섹션 콘텐츠 하단 | **14428** (< 14560) | 불변 |
| Flow 3944:2 / 3944:57 | 각 1920×1280 @ (44,3940) / (44,11780) | 불변 |
| 가이드 텍스트 3783:907 | 무수정 | 불변 |

- 섹션 직속 자식 하이라이트 마커 전수 겹침 감사 → **잔존 1건뿐**이며 `3940:2 ↔ 3940:3`(요약 8카드 안의 지급액 카드)로 **의도적 중첩**. `3940:12 ↔ 3940:13`은 소멸.
- 신규 노드 0 · 삭제 노드 0. 이번 라운드는 기존 노드의 속성 · 좌표 · 문자만 수정했다.
- C1(수수료율) · C2(지급 캘린더) · C4(예상 지급 차액) **어느 값도 채택하지 않음**. #1은 이미 검정으로 조판된 확정 문장의 **마커 색만** 맞춘 것이고, 미확정 빨강 줄(계약 '매일' · 자료 설계 '익익영업일' 정합)은 문구 · 색 그대로다.
- 순한글 규율 유지 — 이번에 투입한 문자열에 영문 토큰 0.

## 다음 라운드로 넘기는 항목

1. **선존 관통 2건** — `3944:48 ↔ 3944:24`, `3944:78 ↔ 3944:77` (본문 ② 참조). 둘 다 간선 우회 또는 주석 이동으로 처리 가능하나 지정 노드 밖이라 미착수.
2. **원고 1줄** — `dsl_delta.md:834`에 `(구성 항목)` 생략 예외 부기.
3. **규격서 1줄** — 리스트 마커 색은 문단 직전 개행 문자가 지배하며, 검정→빨강 방향 전환은 Plugin API로 불가(정규화됨). 인라인 색 전환 규칙에 명시.
