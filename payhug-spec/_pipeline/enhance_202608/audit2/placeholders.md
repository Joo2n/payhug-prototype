# 심화 분석 placeholder 대체 콘텐츠 (시트 5장 · 17+2 텍스트 노드)

구조 공통: 심화 섹션 = 헤더("정책 · 계산 로직 · 데이터 출처") → **인트로 문단**(`test`) → 그룹 1개(카테고리 태그 + **그룹 제목** `h`) → 행 1개(**행 제목** `a`/`k` + **행 본문** `b`/`v` + 상태 pill).
상태 pill 노드(1611:177, 1613:194, 1612:158, 1674:155, 1702:458)는 이미 «확정»으로 채워져 있고, 아래 제안 행은 전부 코드 검증 가능한 내용이라 **pill 변경 불필요(확정 유지)**.
코드 경로 약칭: `fresh-admin/` = `/private/tmp/claude-501/-Users-semi-cursor-payhug/d845932c-7f84-4039-9996-117da0987331/scratchpad/fresh-admin/`

---

## 1. 1611:2 — AD_MERCHANT_DT_RESIGN (계약 재서명 요청 모달) · 그룹 태그 «정책»

| 노드ID | 슬롯 | 현재 | 대체 텍스트 (그대로 삽입) | pill | 코드 근거 |
|---|---|---|---|---|---|
| 1611:157 | 인트로 본문 | test | 계약 내용이 바뀌었는데 서명이 옛것일 때(대표 사례: 승인 후 소개 파트너 교체 — "기존 계약서는 자동으로 갱신되지 않습니다" 안내가 뜸) 어드민이 가맹점에 다시 서명을 요구하는 통로. 핵심은 계약 버전 관리 — 재서명이 끝나도 기존 계약은 지워지지 않고 '대체됨' 이력으로 보존되며, 서명 현황 조회도 대체된 구버전을 빼고 최신 유효 계약 기준으로 돈다. 재서명 대기 중에도 정산은 중단되지 않고, 요청은 [요청 취소]로 되돌릴 수 있다. 사유는 필수이며 내부 메모가 아니라 가맹점에게 그대로 노출된다. 재서명을 계속 안 하면 어떻게 되는지(기한·독촉·제재)는 코드에 없음 — 확인필요. | — | 파트너 교체 안내 `app/merchants/[id]/page.tsx:840-844` · SUPERSEDED 보존·서명현황 제외 `:214-216, 1431-1433, 1483-1499` · 요청 취소 `:595-605` · 사유 필수 `:572, 1851` · 안내문(정산 비중단) `:1841-1845` |
| 1611:165 | 그룹 제목 (태그 «정책» 옆) | h | 요청 가능 대상과 계약 버전 처리 | — | 아래 행 근거와 동일 |
| 1611:170 | 행 제목 | a | 요청 조건과 요청 후 흐름 | — | `app/merchants/[id]/page.tsx:1437-1439` |
| 1611:174 | 행 본문 | b | 관리자 권한 + 서명완료/유효 상태의 서비스·선정산 이용계약에만 [재서명 요청] 버튼이 붙음 — 수기 업로드된 레거시 계약은 대상 아님. 요청하면 계약 카드가 '재서명 요청됨 · 가맹점 서명 대기'로 바뀌고 입력한 사유가 카드에 노출되며, 가맹점이 새로 서명하면 기존 계약은 'N차 · 대체됨' 이력으로 내려가고 현재 계약에 '재서명 N회'가 누적 표기된다 | 확정 (1611:177 유지) | 조건 `canRequestResign = isAdmin && (SIGNED‖ACTIVE) && SERVICE/PRE_SETTLEMENT_SERVICE` `:1438-1439` · 요청됨 표기·사유 노출 `:1463-1472` · N차·대체됨 / 재서명 N회 `:1433, 1447, 1491-1499` |

---

## 2. 1613:2 — AD_MERCHANT_DT_TRANSFER (수동 이체 모달) · 그룹 태그 «정책», 그룹 제목 «진입 조건»(기존 유지)

| 노드ID | 슬롯 | 현재 | 대체 텍스트 (그대로 삽입) | pill | 코드 근거 |
|---|---|---|---|---|---|
| 1613:174 | 인트로 본문 | test | 정산 파이프라인 밖에서 어드민이 임의 금액을 가맹점에 보내는 예외 지급 수단. 심화 포인트 셋. (1) 입금 대상이 락계좌가 아니라 가맹점 자유입출금 계좌 — 회수(락계좌 스윕)와 반대 방향의 '지급'이며, 이 이체가 6대 개념 중 무엇으로(미지급금 지급·환급 등) 원장에 잡히는지는 서버 소관 미확정. (2) 이중 송금 방지는 멱등키뿐(같은 이체 의도의 재시도엔 같은 키 재사용, 금액·적요를 고치면 새 의도로 보고 키 재발급) — 별도 승인자 확인 단계는 없음. (3) 적요 라벨은 '최대 20바이트'인데 실제 입력 제한은 20글자라 한글 적요는 20바이트를 넘을 수 있음 — 은행 전문에서 잘리는지 서버 검증 확인필요. 이체 사유를 남기는 내부 기록 입력란도 없다(적요는 입금통장 표시용뿐). | — | 수취계좌=자유입출금 `components/ManualTransferModal.tsx:8-15, 166` · 멱등키 `:41-42, 80, 92-94, 215` · 적요 20글자 slice vs '20바이트' 라벨 `:210, 215` · 사유 입력란 없음(금액·적요·멱등키만 전송) `:100-104` |
| 1613:187 | 행 제목 | a | 관리자 + 승인완료 + 정산활성 | — | `app/merchants/[id]/page.tsx:740` |
| 1613:191 | 행 본문 | b | 세 조건이 모두 참일 때만 상세 상단 액션바에 [수동 이체] 버튼 노출 — 파트너 계정은 액션바를 봐도 이 버튼은 없음(관리자 전용, 모달 자체도 관리자일 때만 뜸). 모달은 열 때마다 수취 계좌를 서버에서 새로 조회하고, 조회 실패 시 금액 입력 단계 없이 [닫기]만 가능 — 계좌 미확인 상태의 이체 원천 차단. 닫으면 금액·적요·확인 단계·멱등키가 모두 초기화된다 | 확정 (1613:194 유지) | 버튼 조건 `isAdmin && isApproved && isSettlementActive` `app/merchants/[id]/page.tsx:740-746` · 모달 관리자 한정 `:2073-2082` · 열 때마다 재조회·실패 시 닫기만 `components/ManualTransferModal.tsx:44-47, 58-74, 300-309` · 닫을 때 초기화 `:48-55` |

---

## 3. 1612:2 — AD_MERCHANT_DT_WEEKDAY (요일별 상세 테이블 펼침) · 그룹 태그 «데이터»

| 노드ID | 슬롯 | 현재 | 대체 텍스트 (그대로 삽입) | pill | 코드 근거 |
|---|---|---|---|---|---|
| 1612:138 | 인트로 본문 | test | 펼침 표는 서버가 내려준 요일×출처(카드 / 배달 합산 / 배민·요기요·쿠팡이츠) 평균값을 가공 없이 그대로 보여주는 원자료 뷰. 반면 카드 상단 '카드/배달/전체 일평균' 요약은 이 요일값 7개를 화면에서 단순 평균한 2차 가공값 — 요일별 표본 일수가 달라도 가중하지 않아 상세 표와 미세하게 어긋날 수 있다. 수수료율도 두 경로 공존: 표의 요일별 수수료율은 서버 계산값 그대로(산정 근거는 C1 미확정), 요약 카드 수수료율은 화면에서 평균수수료÷평균매출로 재산출. 상단 '예상 매출 정산' 경고 배너도 서버가 준 요일 평균으로 예상 정산 금액을 표시하는데, 이 표와 같은 집계를 쓰는지는 서버 소관 — 확인필요(C4 연관). | — | 표=서버값 그대로 `components/MerchantAvgSalesSummary.tsx:514-576` · 요약=화면 단순평균(가중 없음) `:74-94, 149-155` · 요약 수수료율 재산출 `:92, 303` · 배너는 별도 조회값(ForecastMerchant) `app/merchants/[id]/page.tsx:626-660` |
| 1612:146 | 그룹 제목 (태그 «데이터» 옆) | h | 요일 평균의 출처와 갱신 경로 | — | 아래 행 근거와 동일 |
| 1612:151 | 행 제목 | a | 서버 집계값 + 수동 재계산 | — | `components/MerchantAvgSalesSummary.tsx:105-136` |
| 1612:155 | 행 본문 | b | 요일별 평균 매출·수수료·건수는 최근 N개월(기본 3, 3~6개월 선택) 조건으로 서버에서 받은 값을 그대로 표시 — 이 표를 화면에서 다시 계산하지 않는다. [재계산]을 누르면 서버가 재집계해 '평균 매출 재계산 완료 (N건 갱신)' 토스트 후 목록을 다시 불러오며, 읽기 전용 열람자에겐 [재계산] 버튼이 숨겨진다. 값 없는 요일은 '-', 평균 매출 최고 요일엔 TOP 뱃지 | 확정 (1612:158 유지) | 기간 3~6개월 셀렉트 `:100, 167-176` · 재계산·N건 갱신 토스트 `:121-136` · readOnly 시 버튼 숨김 `:177-185` · '-' 처리 `:64-67` · TOP 뱃지 `:522, 553` |

---

## 4. 1674:2 — AD_SALES_DT_OLD (매출 상세 · 오래된순 정렬) · 그룹 태그 «연결»

| 노드ID | 슬롯 | 현재 | 대체 텍스트 (그대로 삽입) | pill | 코드 근거 |
|---|---|---|---|---|---|
| 1674:135 | 인트로 본문 | test | 이 변형의 심화 포인트는 정렬이 '화면 재배열'이 아니라 '조회 조건'이라는 점. 토글을 누르면 정렬 방향을 서버 조회 파라미터로 바꿔 다시 조회하며 항상 첫 페이지로 돌아간다 — 깊은 페이지를 보다가 정렬을 바꾸면 보던 위치를 잃는다. 정렬 기준 날짜는 별도 선택이 아니라 상단 '날짜 기준'(거래일/지급예정일)을 그대로 따라가며, 금액순 등 다른 정렬 축은 없다. 조회 조건 초기화 시 최신순으로 복귀. 엑셀 다운로드도 현재 정렬을 포함한 동일 조건으로 전체 건을 다시 받으므로 파일의 행 순서가 화면 정렬과 같다. | — | 토글=서버 재조회·첫 페이지 `app/sales/[bizNo]/page.tsx:884-891` · 기본 최신순·리셋 시 복귀 `hooks/usePurchaseLedger.ts:169, 261-270` · 날짜 기준 연동 라벨 `page.tsx:891` · 엑셀 동일 파라미터 `page.tsx:214-228` |
| 1674:143 | 그룹 제목 (태그 «연결» 옆) | h | 정렬 상태가 조회·다운로드로 이어지는 방식 | — | 아래 행 근거와 동일 |
| 1674:148 | 행 제목 | a | 서버 재조회 파라미터 | — | `hooks/usePurchaseLedger.ts:196-224` |
| 1674:152 | 행 본문 | b | 오래된순 전환 = 정렬 방향 파라미터만 바꾼 서버 재조회(항상 첫 페이지부터). 이 파라미터는 엑셀 다운로드 요청에도 그대로 실려 전체 건(현재 총건수만큼)을 한 번에 재조회하므로, 내려받은 엑셀도 화면과 동일하게 오래된순으로 담긴다 | 확정 (1674:155 유지) | sortDir 파라미터 전송 `hooks/usePurchaseLedger.ts:223-224` · 토글 시 page 0 `page.tsx:884-886` · 엑셀에 sortDir·size=totalElements `page.tsx:223-225` |

---

## 5. 1702:2 — AD_PARTNER (회원 관리) · 그룹 태그 «설정»

※ 1702:451(«k»)·1702:455(«v»)는 지시 목록엔 없지만 같은 행의 placeholder — 행 완성을 위해 함께 제시.

| 노드ID | 슬롯 | 현재 | 대체 텍스트 (그대로 삽입) | pill | 코드 근거 |
|---|---|---|---|---|---|
| 1702:438 | 인트로 본문 | test | 심화 포인트는 권한 판정이 두 갈래로 갈린다는 것. [+ 등록] 노출과 등록 모달의 유형 선택지는 서버가 내려준 '생성 가능 유형' 목록으로 정해지는 반면(기본 선택은 파트너 우선), [프로필]·[삭제] 노출은 브라우저에 저장된 내 유형(관리자·페이허그)으로 화면에서 판정한다 — 후자는 서버 재검증이 전제돼야 안전(가설). 가입 링크 후보를 '자기/하위'로 한정하는 이유는 코드 주석에 명시돼 있다: 상위 조직 코드로 링크를 만들면 파트너 체인이 상위부터 시작돼 로그인한 파트너 본인이 계약·수수료 배분에서 빠지기 때문. 검색은 이름만 대소문자 무시 비교이고 아이디/전화번호는 원문 비교라, 대문자가 든 아이디는 소문자로 치면 검색되지 않는다 — 동작은 확정, 의도인지는 확인필요. | — | 생성 가능 유형 서버 조회·[+ 등록] 조건·기본 파트너 `app/partners/page.tsx:1078-1089, 1110-1118, 1249-1256, 872` · isAdmin=로컬 저장 유형 판정 `:1073, 1398, 1420` · 자기/하위 한정 주석 `:694-699` · 검색 대소문자 비대칭 `:1101-1108` |
| 1702:446 | 그룹 제목 (태그 «설정» 옆) | h | 비활성화·삭제의 실제 효과 | — | 아래 행 근거와 동일 |
| 1702:451 | 행 제목 (목록 외 보너스) | k | 비활성화 ≠ 삭제 | — | `app/partners/page.tsx:1191-1227` |
| 1702:455 | 행 본문 (목록 외 보너스) | v | 비활성화는 '해당 사용자가 로그인할 수 없게' 막는 조치 — 회원 데이터·추천 체인은 남고, [수정] 모달의 상태 값을 활성으로 되돌려 복구할 수 있다. 삭제는 '되돌릴 수 없는' 영구 제거이며 확인 모달에 '정산 상품에 배정된 경우 삭제할 수 없습니다'가 명시 — 배분 체인에 걸린 회원 보호 가드(실제 차단은 서버 검증) | 확정 (1702:458 유지) | 비활성화 확인문구 `:1191-1208` · 수정 모달 상태 셀렉트(활성/비활성, 수정 시에만) `:1012-1024` · 삭제 확인문구·가드 안내 `:1210-1227` |

---

## 작성 원칙 준수 메모
- C1(수수료율)·C2(지급 캘린더)·C4(예상 지급 차액) 어느 값도 단정하지 않음 — 1612 인트로는 C1·C4를 미확정으로만 지목.
- 모든 확정 서술은 fresh-admin(develop HEAD) 코드 라인으로 역추적 가능. 서버 소관(원장 기록 개념, 바이트 검증, 예상정산 배너의 집계 출처, 삭제 가드 실집행)은 가설/확인필요로 구분.
- 어드민 '매출 회수'(락계좌 스윕)와 정책 '회수' 구분 유지 — 1613 인트로에서 수동 이체를 스윕의 반대 방향 '지급'으로만 서술.
