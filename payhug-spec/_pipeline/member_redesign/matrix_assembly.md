# 상태 매트릭스 조립 계획 (Figma 쓰기 직렬)

## 공통
- 파일 Tcf69tIciGxmlqCIuRb0iI, 페이지 **2807:2212**. 기존 시트 9장(y=0~3335)·기존 임포트 원본은 불변.
- 매트릭스 시작: **y=4200, x=0**. 소스 = `state_import_map.json`(46 노드) + `states_catalog.md`(라벨 문구).
- 임포트 노드는 **직접 이동**(appendChild→페이지, clone 불필요 — 매트릭스 전용 임포트).
- 폰트: Noto Sans KR(신규 텍스트 전부). 텍스트는 생성 후 좌표 지정.

## 플로우 섹션 구조 (A~G 순서)
1. **섹션 헤더 밴드**: 프레임(다크 네이비 #1B2537, 폭 8220 × 높이 120, 라운드 16) + 좌측 텍스트(백색 32px Bold): "FLOW A — 회원 목록 · 상태 관리 (7)" 식으로 카탈로그 섹션명·케이스 수. Flow G는 부제 텍스트 추가(우측, 20px, #7FE141): "노출 위치 비교 — 채택 결정 대기".
2. **케이스 그리드**: 행당 4개, x 간격 180 (열 x = 0/2100/4200/6300), 행 안 프레임 상단 정렬.
   - 각 케이스 = 라벨판 + 임포트 프레임.
   - **라벨판**: 텍스트 2줄 — 1줄 "[A1] 목록 기본"(28px Bold, #222222), 2줄 = 카탈로그 '화면 조건' 문구(20px, #5C5D62, 폭 1800 랩). 라벨판 높이 ~100, 프레임 위 20px 간격.
   - 임포트 프레임 폭 1922 → 열 폭 1920 초과 시 그대로 두고 열 x를 2100 피치로(위 좌표가 이미 반영).
3. **행 높이** = 해당 행 프레임 height 최대값 + 라벨 120 + 세로 간격 240. 다음 행 y로 누적.
4. **섹션 간격**: 이전 섹션 마지막 행 하단 + 400 에 다음 헤더.
5. 케이스 순서 = 카탈로그 표 순서 그대로 (A1..A7 / B1..B10 / C1..C6 / D1..D7 / E1..E4 / F1..F6 / G1..G6).
6. 이동한 프레임 rename: "[<id>] <제목>".

## 검수 기준 (doc-qa)
- 46 프레임 전부 존재·순서·라벨 문구 카탈로그 일치, 겹침 0, 기존 시트 무변동, G2·G3 라벨에 "비교안" 포함.

## 로그
- 완료 후 `matrix_apply.md`에 섹션별 y 범위·프레임 node_id 표 기록.

## v2 최종 구성 (2026-08-19 갱신 — 이 절이 조립 정본)

- **본편 매트릭스 = v2 프레임 51장** (`state_import_map_v2.json`): Flow A(7)~B(10)~C(6)~D(7)~E(4)~F(6)~G(6)~H(5) 순, 배치 규칙은 상단 계획과 동일(y=4200 시작, 행당 4, 라벨판, 섹션 헤더 밴드).
  - Flow H 헤더: "FLOW H — 병렬 배분 · 계정 자동 생성 (5)".
  - Flow G 헤더 부제 유지: "노출 위치 비교 — 채택 결정 대기".
- **v1 비교 밴드 1개 추가** (Flow B 섹션 마지막 행 뒤): 소제목 밴드(회색 톤, 높이 100) 텍스트 "비교안 — 계약 위치 라디오 방식 (이전판 index.html, 결정 P5로 대체됨을 라벨에 쓰지 말고 '비교안'으로만)". 프레임 3장 = **v1 임포트** `state_import_map.json`의 B6·B7·B8 노드. 라벨판: "[B6·v1] 파트너 — 계약 위치 라디오(비교안)" 식.
- v1 임포트 46장 중 위 3장 외 43장은 배치하지 않는다 — 조립 완료·QA 통과 후 보관 페이지(2822:2294)의 미사용 v1 노드(2886:2~2932:2 중 B6·B7·B8 제외)와 x=33107 나열분을 **삭제**해 백업 페이지를 원상 복구한다(사용된 3장은 매트릭스로 이동됐으므로 원위치에 없음). v1 목업 원본 9장(2829:2~2837:2)은 유지.
- 라벨 규칙 보강: v2 프레임 라벨판의 조건 문구는 카탈로그 v2 정의(교체분은 "v2 개정" 표) 우선.
