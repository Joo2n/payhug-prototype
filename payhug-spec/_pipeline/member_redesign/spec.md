# 회원관리 계약 체인 TO-BE 화면설계 — 작업 명세 (SSOT)

> 근거: `source_v2.txt`(개편안 v2 — 채택본) · `source_v1.txt`(v1 — 참고). 사용자 결정(2026-08-19):
> ① 수수료율 UI = **요율 세트 전체**(채권매입·시스템이용료·이체수수료 + VAT + 납부·수취 주체를 관계(Edge)에 저장)
> ② 범위 = **정산상품 관리 제거 영향까지 9장**
> 대상 Figma: `Tcf69tIciGxmlqCIuRb0iI` 페이지 `2807:2212`([페이허그_어드민] 회원관리). 시트 템플릿 = `2103:2` clone.
> 기존 시트(303:173)·프론트 레포는 읽기 전용 근거. 목업은 독립 HTML(자체완결 CSS) → generate_figma_design 네이티브 임포트.

## 0. 채택 구조 (v2 요약)

- 정산상품 선택 없음. 회원 유형 선택 → 직속 상위 유형 자동 결정 → 직속 상위 1명 검색 선택 → **관계 수수료율 세트** 입력 → 계약구조 Preview → 등록.
- 수수료율은 회원(Node)이 아니라 **관계(Edge)** 속성. 체인은 parent 관계 재귀 조회로 자동 조합.
- 데이터 모델(안): `MemberContractRelation`(parent_member_id, child_member_id, 요율세트, effective_from/to, status).
- 유형→직속 상위: 영업→영업조직 / 영업조직→하위파트너 / 하위파트너→상위파트너 / 상위파트너→없음(최상위).

## 1. 요율 세트 정의 (사용자 결정 ①, 현행 정산상품 필드의 관계 이식)

관계(Edge) 1건이 갖는 값 — 현행 코드 근거 `app/settlement/policies/page.tsx:51~98`:

| 필드 | 형식 | 비고 |
|---|---|---|
| 채권매입 수수료율 | % 소수 | 예시 표기만(값 채택 금지 — C1) |
| 시스템이용료율 | % 소수 | 〃 |
| 이체수수료 | 정액 원 | 〃 |
| VAT 방식 | 별도/포함 (요율별) | 현행 SEPARATE 등 |
| 납부 주체 | 상위/하위/가맹점 부담 (시스템료·이체료 각각) | '대납' 개념 |
| 수취 주체 | 상위 수취/하위 수취 토글 (시스템료·이체료 각각) | |

- **예시 요율은 반드시 '예시'라고 명기.** 수수료율 실값은 C1 미확정 — 어떤 값도 확정 서술 금지.
- 투자자·페이허그 배분(현행 상품 participants의 채권매입 배분)은 체인 Edge에 자리가 없음 → **확인 필요**로 명기 (P2).

## 2. 시트 목록 (9장) — page_id는 manifest와 중복 없음 확인됨

| # | page_id | 시트 제목 | 목업 파일 | 화면부 내용 |
|---|---|---|---|---|
| 1 | `AD_MEMBER` | 회원 관리 목록 | m1_list.html | 목록 TO-BE. 기존 11컬럼 유지 + '상위 회원'→'직속 상위' 명칭 + '직속 계약 수수료율'(대표값=채권매입 %, 예시)·'계약 구조'(보기 ›) 컬럼. 사이드바는 TO-BE 메뉴(정산 상품관리 없음) |
| 2 | `AD_MEMBER_ADD` | 회원 등록 (계약 관계) | m2_add_sales.html | 등록 모달, 유형=영업 선택 상태. [기본 정보] 이름·아이디/휴대폰·비밀번호·유형·법인·사업자·추천코드(현행 유지) + [계약 관계] 직속 상위 유형(자동·읽기전용 "영업조직")·직속 상위 회원(검색형 Select)·계약 수수료율 세트(§1)·계약 구조 미리보기(Edge에 요율) |
| 3 | `AD_MEMBER_ADD_TOP` | 회원 등록 (최상위·파트너 위치) | m3_add_top.html | 변형 2상태 나란히: (A) 유형=상위파트너 → 계약 관계 섹션 비활성 + "최상위 회원으로 등록됩니다" 안내(페이허그와의 계약율 필요 여부=확인 필요) (B) 유형=파트너 → '계약 위치' 라디오(상위파트너/하위파트너) 노출(판별 규칙=확인 필요) |
| 4 | `AD_MEMBER_EDIT` | 회원 수정 (관계 변경) | m4_edit.html | 수정 모달. 직속 상위·요율 변경 영역 + "변경 시 기존 관계는 종료 처리되고 새 이력이 생성됩니다" 안내(이력 정책=확인 필요) |
| 5 | `AD_MEMBER_PROFILE` | 회원 프로필 (직속 계약) | m5_profile.html | 프로필 모달: 회원 정보(현행) + [직속 계약 관계] 직속 상위·상위 유형·요율 세트 표·적용일 + [전체 구조 보기 ›] |
| 6 | `AD_MEMBER_CHAIN` | 계약 구조 조회 | m6_chain.html | 전체 체인 View: 세로 노드(역할+회원명) + **연결선에 요율 칩**(Edge 표기 원칙) + 하단 관계 테이블(상위 회원|하위 회원|관계|요율 세트 요약|적용일) |
| 7 | `AD_MEMBER_EXCEPT` | 등록 예외 상태 | m7_except.html | 4패널: ① 비활성 상위 — 검색 결과에서 제외 표기 ② 순환 구조 — 등록 차단 에러 ③ skip-level 시도 — 허용 여부 확인 다이얼로그(정책=확인 필요) ④ 다중 상위 — 기존 상위 존재 안내(정책=확인 필요) |
| 8 | `AD_MEMBER_GNB` | 메뉴 구성 | m8_gnb.html | TO-BE 사이드바 전체(정산 그룹=정산 현황·정산 시뮬레이션, 관리자 그룹=모 계좌·회원 관리·약관·스크래핑·활동 로그·AI 모니터링) — 정산 상품관리 부재 상태. 우측에 총판 시점 메뉴 |
| 9 | `AD_MERCHANT_FEE2` | 가맹점 상세 수수료 연결 | m9_merchant_fee.html | 가맹점 상세의 수수료 영역 TO-BE: 정산상품 배정 대신 '적용 계약 체인'(담당 영업의 체인 + 요율) 표시 + 거래 유형(카드/배달)별 차등=확인 필요 배지 |

## 3. 공통 규율 (위반 금지 — screen-add 6원칙 동일)

1. **현재(TO-BE 목표) 상태만 서술** — 키노트·심화에 이력체("제거/개편/기존/변경/신설/이전/AS-IS") 금지. TO-BE 화면 자체를 서술하고, 전환·마이그레이션 쟁점은 심화 [정책·주의] 행에 '확인 필요'로.
2. **순한글** — 본문에 영문 식별자 금지(코드 근거 줄만 허용).
3. **미확정 단정 금지** — C1 수수료율 값 채택 금지(예시 명기), 6대 개념 혼용 금지. 확인 필요 항목: 투자자·페이허그 배분 자리(P2) / 가맹점 연결·거래유형별 계약(P3) / 다우 트랙 체인(P4) / 파트너 상·하위 판별(P5) / §17 예외 8건 / 정산 계산 rate source 전환·마이그레이션(P6).
4. **Figma 쓰기는 직렬**, 쓰기 범위 = 서준 파일 페이지 2807:2212 한정.
5. 프론트 레포 읽기 전용. 산출물은 이 디렉토리(`_pipeline/member_redesign/`)에 보존.
6. 심화 13행 구성 = [데이터 출처]3 / [계산·판단 로직]3 / [설정·연결]2 / [정책·주의]5. pill 상태 4종(확정/확인필요/가설/신규).

## 4. 목업 제작 규칙 (HTML)

- **자체완결**: 외부 CDN·이미지 금지, CSS 인라인 `<style>`. 폭 1920 고정(`body{width:1920px}`), 폰트 `"Apple SD Gothic Neo","Noto Sans KR",sans-serif`.
- **실제 어드민 디자인 언어 복제**: `payhug-admin-web`의 Tailwind 클래스에서 값 역산 — 사이드바 다크(`#0f172a` 계열, 실코드 확인), primary 색(globals.css/tailwind 설정 확인), 배지·라운드(`rounded-lg/xl`)·표 스타일(`text-xs uppercase` 헤더) 동일하게.
- 모달 목업은 실제처럼 딤 배경 + 뒤에 목록 흐릿하게.
- 데이터는 v2 문서 예시 인물(A파트너·B파트너·투게더페이·안현준) 사용, 요율은 0.50%/0.30%/0.10% 등에 '예시' 병기.
- 목업 안에 상태 라벨·주석 넣지 않음(설명은 시트 마커·키노트 몫). 단 m3/m7의 다패널은 패널 상단에 작은 회색 캡션 허용.

## 5. 산출물 경로

- 코드 분석: `code_analysis.md` (에이전트 A)
- 시트 원고: `manuscripts/AD_MEMBER*.md` 9개 (에이전트 B)
- 목업: `mockups/m1~m9.html` (에이전트 C)
- 조립 로그: `figma_apply.md` / QA: `qa.md` / 최종 리포트: Artifact
