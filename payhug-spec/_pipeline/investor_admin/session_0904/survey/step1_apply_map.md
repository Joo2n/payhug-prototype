# 채택 11곳 — 고칠 자리 지도

대상
- 아티팩트 `/private/tmp/claude-501/-Users-semi-cursor-payhug/9aed3429-fc00-4785-9abd-c254e437cf03/scratchpad/ceo_review.html` (188줄)
- 원고 `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/final_terms.json`
- 생성기 `build_symreview.py` · 검사기 `verify_final_terms.py` · 갈래표 `alias_table.py`
- 대표님 워드 원문(변경추적 해석) `scratchpad/ceo_docx/ceo/word/document.xml` · 우리가 보낸 판 `ceo_docx/ours` (= `~/Downloads/payhug_용어정의서/기호정리표_20260903_2329.docx`)

## (가) 아티팩트 11곳 — 줄 · 현재 · 바꿀 것

| # | 줄 | 표 | 현재 문구 | 바꿀 문구 |
|---|---|---|---|---|
| 1 | 86 | 표1 첨자 r · 의미 칸 | `예상 연환산수익률을 나타낼 때 쓴다` | `예상 수익률을 나타낼 때 쓴다. Y<sub>r</sub> 는 연환산 예상 수익률 (Y 가 연환산을 뜻하므로)` |
| 2 | 88 | 표1 첨자 a · 의미 칸 | `투자실행금 기준 연환산수익률을 나타낼 때 쓴다` | `투자실행금 기준 관찰된 값을 나타낼 때 쓴다. PY<sub>a</sub> 는 표본에서 관찰된 투자실행금 기준 연환산 수익률 (P 는 표본으로 선택된 기간)` |
| 3 | 90 | 표1 첨자 t · 의미 칸 | `투자자산 기준 연환산수익률을 나타낼 때 쓴다` | `투자자산 기준 관찰된 값을 나타낼 때 쓴다. PY<sub>t</sub> 는 표본에서 관찰된 투자자산 기준 연환산 수익률` |
| 4 | 102 | 표2 r · 용어 이름 | `유동화투자자의 할인율` | `계약된 할인율` |
| 5 | 110 | 표2 L · 용어 이름 | `입금부족` | `입금부족액` |
| 6 | 112 → 80 뒤 | 표2 Y 행 → 표1 접두 행 | 112: `<tr><td></td><td><span class="sym">Y</span></td><td></td><td>연환산수익률</td><td></td></tr>` | 112 삭제. 80 뒤에 삽입: `<tr><td>접두 <span class="sym">Y</span></td><td>연환산을 뜻한다</td><td><span class="sym">Y<sub>r</sub></span> <span class="sym">PY<sub>a</sub></span> <span class="sym">PY<sub>t</sub></span></td></tr>` |
| 8 | 130 | 표2 EC · 산식 | `d 시점 쿠콘 가상계좌의 현금 잔액` | `d 마감시점 쿠콘 가상계좌의 현금 잔액` (원문 45행 「마감시점(자정)」 — `(자정)` 은 선택) |
| 8′ | 153 | 표2 PEC · 산식 | `PEC = P 안 각 날의 EC 를 더한 값` | 변경 0건 (「각 일자」 뜻이 이미 있음). 대표님 낱말로 맞추려면 `PEC = P 에 속하는 각 일자의 EC 를 더한 값` — 선택 |
| 9 | 132 | 표2 Σ Ai + EC · 산식 둘째 줄 | `i 는 정산예정일이 d 보다 뒤인 채권` | `i 는 정산예정일이 d 보다 뒤인 대상정산금채권` |
| 11 | 142 | 표2 PA · 산식 | `PA = Σ A<sub>i</sub><br>i 는 정산예정일이 P 안에 든 채권` | `PA = Σ A<sub>i</sub> &nbsp; i ∈ P` (144·146 과 같은 꼴) |
| 12 | 144 · 146 | 표2 PM · PB · 산식 | `PM = Σ M<sub>i</sub> &nbsp; i ∈ P` / `PB = Σ B<sub>i</sub> &nbsp; i ∈ P` | 변경 0건. 겹친 `i ∈ P` 는 대표님 워드 사본에만 있다(PM 행 w:ins 안에 `i ∈ P⏎i ∈ P`). 우리가 보낸 20260903_2329 판도 한 번이다 |
| 13 | 183 | 표3 넷째 행 · 변경점 | `연환산수익률 기호 변경` | `관찰된 연환산수익률 기호 변경` |

대표님 워드에서 확인한 자리 차이
- 1·2·3 번: 대표님은 의미 칸을 `예상 수익률을 …` / `… 관찰된 값을 …` 으로 고치고, 「Yr는 연환산예상수익율을 의미 …」 설명 문장은 **예시 칸**(`Y_r` 옆)에 덧붙였다. 채택 목록이 「첨자 r 의미」 자리로 묶었으므로 위 표는 의미 칸 한 곳에 두 문장을 넣는 안이다. 예시 칸은 `Y_r` · `PY_a` · `PY_t` 그대로.
- 8 번: 대표님은 `d ` 를 지우고 `조회대상기간에 속하는 각 일자의` + ` 마감` 을 넣었다. 반만 채택하므로 `d` 는 남기고 「마감」만 붙는다.
- 6 번: 대표님은 용어 이름 칸에서 `수익률` 을 지워 `연환산` 만 남기고 산식 칸에 `접두어로 이동 필요` 라고 적었다. 「접두어로 이동 필요」 는 지시문이라 문면에 넣지 않는다.

철자
- 대표님 「연환산예상수익율」「연환산수익율」(율) ↔ 아티팩트 112·138·155·158 「연환산수익률」(률). 아티팩트 철자 「연환산수익률」 유지 제안. `투자수익율`(MR) 은 원문대로 율.
- 띄어쓰기: 「연환산 예상 수익률」(채택 목록 표기) 과 「연환산수익률」(아티팩트) 가 한 표 안에 같이 서면 두 벌이 된다. 한쪽으로 통일 — 아티팩트 기존 표기 「연환산수익률」 권장.

## (나) #6 「연환산 접두어로 이동」 의 구조

| 항목 | 현재 |
|---|---|
| Y 가 있는 곳 | 표2 「전체 기호」 112 줄. 분류 빈칸 · 개선 `Y` · 기존 빈칸 · 용어 이름 `연환산수익률` · 산식 빈칸. A·M·B·L·MR 개념 글자들과 한 묶음 |
| P 가 접두로 적힌 꼴 | 표1 「이름 짓는 규칙」 79-80 줄. `<tr><td>접두 <span class="sym">P</span></td><td>조회기간을 뜻한다</td><td>PA PD PY<sub>a</sub></td></tr>` |
| 같은 꼴 제안 | 80 줄 뒤에 `접두 Y` 행. 의미 `연환산을 뜻한다`, 예시 `Y<sub>r</sub> PY<sub>a</sub> PY<sub>t</sub>`. 112 줄은 지운다 |
| 대안 | 112 줄을 남기고 용어 이름만 `연환산` 으로 바꾸는 안. 표2 는 「값」 기호 표라 P 도 여기 없으므로 Y 도 빼는 쪽이 대표님 말(P 처럼 값이 아니라 표시)과 맞는다 |

표1 아래첨자 행(81-83) 예시 `A_i Y_r PY_a` 는 그대로다.

## (다) final_terms.json 대응 키

| # | 키 경로 | 현재 값 | 바꿀 것 |
|---|---|---|---|
| 1 | 직접 대응 없음 | `rules[0..2]` 는 i · w · P 규칙 셋뿐. 첨자 r·a·t 규칙 없음 | 넣으려면 `rules[]` 에 n:4 항(첨자 r·a·t) 신설, 또는 `vars[8].plain`(Y) 문장에 「예상」「관찰된」 낱말 |
| 2 | 직접 대응 없음 | `vars[27].term` `투자실행금액 대비 연환산수익률` · `vars[27].plain` 「채권으로 나간 돈만 놓고 잰 수익률이다. …」 | 「관찰된」 을 넣을 자리는 `vars[27].plain` 첫 문장 — 선택 |
| 3 | 직접 대응 없음 | `vars[28].term` `투자자산 대비 연환산수익률` · `vars[28].plain` | 2 번과 같은 꼴 — 선택 |
| 4 | `vars[1].term` | `유동화투자자의 할인율` | `계약된 할인율` (`vars[1].plain` 「투자자가 채권을 살 때 순지급액에서 떼는 비율이다.」 유지) |
| 5 | `vars[6].term` | `입금부족` | `입금부족액` (`vars[11].term` `i번째 채권의 입금부족액` 과 같아진다) |
| 6 | `vars[8].term` · `vars[8].plain` | term `연환산수익률` · plain 「수익률을 1년으로 늘려 잰 값이다. 산식에는 할인율로 잰 예상치가 `Y_r` 로, … `PY_a` 로, … `PY_t` 로 나온다.」 | term `연환산`. plain 은 「값이 아니라 1년으로 늘려 잰다는 표시다. P 처럼 앞에 선다」 로 재서술하되 백틱 `Y_r` `PY_a` `PY_t` 셋은 남긴다. `rules[]` 에 「Y 는 연환산 표시라 P 처럼 앞에 선다」 항 신설 후보. **`vars[8]` 을 지우거나 kind 를 바꾸지 않는다** (아래 K1) |
| 8 | `vars[17]` EC | `formula: null` · plain 「채권으로 나가지 않고 쿠콘 가상계좌에 남아 있는 현금이다. 투자 자산 화면 카드 「순현금」 20,000,000원이 이 값이다. 투자 수익 화면의 `PEC` 140,000,000원은 …」 | plain 첫 문장에 「기준일 마감시점」: 「기준일 마감시점에 쿠콘 가상계좌에 남아 있는 현금이다.」 또는 formula `EC = d 마감시점 쿠콘 가상계좌의 현금 잔액`. `vars[26].formula` `PEC = P 안 각 날의 EC 를 더한 값` 은 그대로 |
| 9 | `vars[18].formula` | `( Σ A_i ) + EC      i 는 회수되지 않은 대상정산금채권` | 「대상정산금채권」 이미 있음. 조건 문구가 아티팩트(정산예정일이 d 보다 뒤인)와 다르므로 `vars[15].formula` 꼴로 맞추는 것은 선택: `( Σ A_i ) + EC      i 는 정산예정일이 기준일보다 뒤인 대상정산금채권` |
| 11 | `vars[21].formula` | `PA = ( Σ PA )      기간 안 레코드 전부` | `PA = Σ A_i      i ∈ P` |
| 12 | `vars[23].formula` · `vars[22].formula` | `PM = ( Σ PM )` · `PB = ( Σ PB )` | `PM = Σ M_i      i ∈ P` · `PB = Σ B_i      i ∈ P` (원고는 아티팩트와 달리 옛 꼴이라 여기는 실제 변경) |
| 13 | 대응 없음 | `final_terms.json` 에 변경내역 표가 없다. `alias_table.rows()` 제목은 `기존 표기 → 바뀐 기호` | 변경 0건 |

vars 번호 대조 (0 기준): 0 d · 1 r · 2 D개념 · 3 A · 4 M · 5 B · 6 L · 7 MR · 8 Y · 9 D_i · 10 A_i · 11 L_i · 12 채권매입수수료_i · 13 M_i · 14 B_i · 15 Σ A_i · 16 D집계 · 17 EC · 18 Σ A_i + EC · 19 LR · 20 Y_r · 21 PA · 22 PB · 23 PM · 24 PMR · 25 PD · 26 PEC · 27 PY_a · 28 PY_t

## (라) build_symreview.py 가 읽는 표식 · 이번 판 표식 제안

| 항목 | 사실 (`build_symreview.py`) |
|---|---|
| 읽는 태그 | `h1` `h2` `h3` `p`(class `note` 면 회색 주) · `div.formula` · `table`(thead 행은 머리) · `td`/`th`(class `n`·`f` 면 등폭) · `sub` · `br` · `span` (62-127 줄) |
| span 색 판정 | class 토큰만 본다. `new` → \x02 파랑(2C4470) 굵게 · `old` → \x03 빨강(A63D2F) · `ok`/`p-ok` → \x04 초록 굵게 · `hi`/`p-no` → \x05 빨강 굵게 (85-92 · 183-184 줄). `sym` 이 없어도 `<span class="new">` 만으로 워드에서 파랑이 된다 |
| HTML 쪽 | CSS 는 `.sym.new` `.sym.old` `.formula .ok` `.formula .hi` `.p-ok` `.p-no` 뿐 (56-65 줄). 맨 `<span class="new">` 는 HTML 에서 표식이 없다. `html_doc()` 이 `<style>` 블록을 그대로 복사하므로(343-349 줄) `ceo_review.html` 에 `.new{color:var(--pick);font-weight:600}` 한 줄을 넣으면 워드·HTML 이 같아진다 |
| 워드 색 사용 현황 | 대표님께 간 판: 2C4470(파랑) 18 run · A63D2F(빨강) 32 run · 6E6A63(회색) 12 run — 「개선」 기호가 파랑, 「기존」 기호가 빨강 |

제안
- 채택 문구는 바뀐 낱말만 `<span class="new">…</span>` 으로 감싼다. 파랑 = 「개선」 열과 같은 뜻이라 새 색을 들이지 않는다. 지운 문구는 남기지 않는다(`old` 미사용) — 옛 기호는 표3 이 이미 보인다.
- `.new{}` CSS 한 줄을 56 줄 옆에 둔다.
- 대안: 표식 0건. 이 판이 확정 정본이면 이쪽이 맞다. 대표님 재확인용이면 앞 안.

## (마) verify_final_terms.py 영향

| 판정 | 검사 내용 | 채택 후 |
|---|---|---|
| G9 | `vars[19].formula`(LR) 에 `20일 전` · `11일 전` | 영향 없음 — LR 은 대상 밖 |
| K1 | 갈래 글자(Y 등)가 홀로 서지 않으면 개념 항목에 있어야 함 (`alias_table.split`: `Y_r` `PY_a` `PY_t` → 글자 `Y`) | **`vars[8]` Y 를 지우거나 kind 를 개념에서 바꾸면 FAIL**. term 을 `연환산` 으로 바꾸는 것은 통과 |
| J.개념.Y삭제 · J2 | 자기시험 — Y 를 지운 사본에서 K1 이 새로 나야 함 | 실물에서 Y 를 지우면 base_fail 에 K1 이 들어가 「새로 난 FAIL」 이 안 잡힌다 → J 케이스 FAIL · J2 FAIL. Y 유지 시 영향 없음 |
| K0 | 개념 항목(vars[2..8])에 formula 가 없어야 함 | Y 에 「접두」 설명을 `formula` 로 넣으면 FAIL. `plain` 이나 `rules[]` 로 |
| K2 · K3 | 개념 항목 plain 이 자기 갈래 기호를 백틱으로 전건 인용 · 인용 기호가 vars 에 실재 | `vars[8].plain` 재서술 시 `Y_r` `PY_a` `PY_t` 백틱 유지. `vars[6].plain`(L) 은 `L_i` `LR` 유지 |
| K13 | `vars[17].plain`(EC) 에 `20,000,000` · `140,000,000` · `` `PEC` `` | 「마감시점」 문장을 덧붙일 때 셋을 지우지 않으면 통과 |
| K4 · K5 | 갈래 표 행 수 · 기호 덮음 | term 변경은 라벨(`L  입금부족액` · `Y  연환산`)만 바뀜 → 통과 |
| E1 · E3 · E5 · E9 · H0 · G2 · G3 · G10 · K7~K12 · K14 | A_i · L_i · M_i · B_i · PD · D · LR · Σ A_i · PA plain · PMR · PY_a · Y_r 값·문구 | 대상 항목 아님 → 영향 없음. `vars[21..23].formula`(PA·PM·PB) 는 문자열 검사 없음 |
| J3 | 실물 원고 md5 무변조 | 검사 중 원고를 바꾸지 않으면 통과 |

다른 검사·생성기
- `verify_finaledit.py` 311-312: 워드 규칙 수·범위 수 = 시드 수. `rules[]` 에 항을 더하면 워드(`build_final.py`)를 같이 다시 내야 한다.
- `build_final.py` 97·232 / `build_finaledit.py` / `read_wordedit.py`: rules·scopes·vars 를 순서대로 조판·역파싱. 문자열 고정 검사 없음.
- `verify_banned.py`(`dm_0901/banned_words.md`): 「관찰된」「계약된」「표본」「접두」 금지어 아님. 「~자리」「~꼴」은 지시 대상이 흐릴 때만 금지.

## (바) 주의 · 동반 후보

| 항목 | 내용 |
|---|---|
| 2 번의 「표본」 | 표1 79 줄 `접두 P 조회기간을 뜻한다` · `scopes[2]` 표본 = 선정산일 d−20~d−11(LR 전용) 과 나란히 서면 P 정의가 둘이 된다. 10 번(D 의 「표본」) 되물음과 같은 낱말. `(P 는 조회기간)` 으로 적거나 10 번 답을 받은 뒤 넣는 것을 권한다 — 부모 판단 |
| 12 번 | 아티팩트·원고 docx 모두 겹침 없음. 아티팩트 변경 0건. 원고 JSON 의 PM·PB formula 는 옛 꼴이라 (다) 12 번은 실제 변경 |
| 동반 후보 (채택 목록 밖) | 128 줄(Σ Ai 행) `…d 보다 뒤인 채권` · 137 줄(LR) `…까지인 채권` 도 「대상정산금채권」 으로 맞출지. 대표님은 132 줄만 고쳤다 |
| 다른 산출물 파급 (대상 아님) | 「유동화투자자의 할인율」: `build_glossary.py:593` · `glossary.html` · `symbol_glossary.json` · `termsdoc_seed.json` · `build_audit_xlsx.py:651·1825·2055`(대표 정의서 regex 라 무관) · `final_terms.fragment.html`. PA 옛 산식 문자열: `build_calc.py:109` `PA = ( Σ A_{d−1} )` · `testcase_table.py:221` `PA = ( Σ A_d )` |
