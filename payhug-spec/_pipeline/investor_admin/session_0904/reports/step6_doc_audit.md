# 기호 정리표 확정본 감사 (2026-09-04 21:26 판)

검토 대상: 아티팩트 `ceo_review.html` · 워드 `기호정리표_20260904_2126.docx` · HTML `기호정리표_20260904_2126.html` · 원고 `final_terms.json` · 검사기 `verify_final_terms.py`. 셀 텍스트·수치는 모두 산출물에서 직접 뽑아 다시 셌습니다. 빌더 보고서는 마지막 대조에만 썼습니다.

## 불일치·위반

| # | 구분 | 확인한 사실 | 근거 | 판정 | 조치 |
|---|---|---|---|---|---|
| 1 | 워드 줄바꿈 4셀 | 표1 아래첨자 예시 · 표3 `w 삭제` 기존·개선 · 표3 `d−1 제거` 기존 4셀이 워드에서는 하드 줄바꿈 + 공백 8자, HTML·아티팩트에서는 한 줄입니다. 텍스트는 같습니다. 직전 판 워드(20260903_2329)도 같은 꼴입니다 | 생성기가 원본 HTML 의 `<span>` 사이 개행을 그대로 줄바꿈으로 옮깁니다 (`build_symreview.py:212–213` `add_break()`) | 내용 불일치 아님 · 조판 차이 · 직전 판부터 있던 생성기 동작 | 생성기에서 태그 사이 개행을 공백으로 접으면 세 벌이 같은 줄 수가 됩니다 |
| 2 | 화면·원장 ⑤ 옛 산식 | 화면 툴팁 `( PY_a × PA ) ÷ ( PA + PEC )` · 값 2.32% (`~/cursor/payhug-investor-admin/invest-profit.html:174–175`), 원장 `TY5_EXPR = 'ty4 * psa / tot'` (`daily_ledger.py:178`), `ledger_facts.json` `weekTyAsset 2.32`. 문서 세 벌은 3.30% 입니다 | 직접 grep · `daily_ledger.ty_asset(4.129577, PA, PEC)` = 2.3224 | 문서와 화면·원장이 다름 · 검사기 FAIL 3건의 원인 · 빌더가 범위 밖으로 고지한 것과 일치 | `TY5_EXPR` 을 새 산식으로 바꾸고 `ty_asset` 에 PD 인자를 넣은 뒤 `ledger_facts.json` → 화면 재생성 |
| 3 | 하위 산출물 미재생성 | `final_terms.json` 을 읽는 용어기호정리 워드·HTML 은 20260902_2345 판이 최신이라 원고와 어긋나 있습니다 | `~/Downloads/payhug_용어정의서/` 목록 | 원고와 하위 산출물 불일치 · 빌더 고지와 일치 | `build_final.py` 재실행 |
| 4 | 검사기 I15 라벨 | 제목이 「화면 ③ = 원고 「화면 3.99%」」인데 실제 비교값은 4.13 입니다 | `verify_final_terms.py:719` · 실행 출력 `4.13% ↔ 4.13` | 판정식은 원고 값을 동적으로 읽어 유효 · 제목 문자열만 낡음 | 제목에서 `3.99%` 제거 |
| 5 | 원고 D 개념 항 문장 | 「정산예정일이 전일자인 채권들의 가중평균이 `PD` 로, 정산예정일이 선택한 기간에 해당하는 채권들의 가중평균이 `PD` 로」 PD 가 두 번 나옵니다. 앞 것은 d−1 시절 문장입니다 | `final_terms.json:62` | 빌더 변경 목록에 없는 자리 · 이 조 변경분 아님 (확인 필요) | 전일자 구절 삭제 |
| 6 | 표 밖 문단 1건 | `.note` 「일별 표의 한 행은 P 가 1일인 경우다. 주별은 7일, 월별은 그 달의 일수다.」 가 워드·HTML·아티팩트 모두에 있습니다. prev3 에도 같은 문장이 있습니다 | 아티팩트 165행 · 워드 본문 4번째 문단 | 이 조 변경분 아님 · 지시 여부 확인 필요 | 지시에 없으면 삭제 |
| 7 | 생성기 원고 경로 | 생성기 원고가 세션 스크래치패드의 `ceo_review.html` 이고 (`build_symreview.py:29–31`), 생성기 자체는 git 미추적(`??`)입니다 | `git status --short` | 스크래치패드가 지워지면 재생성 원고가 사라지는 구조 · 소멸위험 (구조적 · 이 조가 만든 구조인지 확인 필요) | 원고를 저장소 안으로 옮기고 생성기를 커밋 |

문서 세 벌의 텍스트 자체에서 지시와 어긋난 곳은 없습니다.

## 1. 세 벌 대조

| 항목 | 아티팩트 | 워드 | HTML |
|---|---|---|---|
| 표 수 | 3 | 3 | 3 |
| 표1 행 수 (머리 포함) | 8 | 8 | 8 |
| 표2 행 수 | 29 | 29 | 29 |
| 표3 행 수 | 6 | 6 | 6 |
| 셀 수 | 187 | 187 | 187 |
| 텍스트가 다른 셀 | 0 | 0 | 0 |
| 줄바꿈만 다른 셀 | 0 | 4 | 0 |

HTML 은 아티팩트에 `<!doctype>`·`<head>`·`<body>` 껍데기만 씌운 것이고 본문은 바이트 단위로 같습니다 (`diff` 결과 머리 4줄·꼬리 2줄만 차이).

| 표 | 행 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|
| 1 | 아래첨자 예시 | `A_i Y_r PY_a` 한 줄 | `A_i Y_r` ⏎ `        PY_a` | 아티팩트와 같음 |
| 3 | w 삭제 기존 | `wD PwD wPY_MR` 한 줄 | `wD PwD` ⏎ `        wPY_MR` | 같음 |
| 3 | w 삭제 개선 | `D PD PY_t` 한 줄 | `D PD` ⏎ `        PY_t` | 같음 |
| 3 | d−1 제거 기존 | 7개 기호 한 줄 | 2개씩 4줄 | 같음 |

## 2. 채택 11곳

| 번호 | 문구 | 자리 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|---|
| 1 | 예상 수익률을 나타낼 때 쓴다. Y_r 는 연환산 예상 수익률 (Y 가 연환산을 뜻하므로) | 표1 첨자 r | 있음 | 있음 | 있음 |
| 2 | 투자실행금 기준 관찰된 값을 나타낼 때 쓴다. PY_a 는 조회기간에서 관찰된 투자실행금 기준 연환산 수익률 (P 는 조회기간) | 표1 첨자 a | 있음 | 있음 | 있음 |
| 3 | 투자자산 기준 관찰된 값을 나타낼 때 쓴다. PY_t 는 조회기간에서 관찰된 투자자산 기준 연환산 수익률 | 표1 첨자 t | 있음 | 있음 | 있음 |
| 4 | r · 계약된 할인율 | 표2 2행 | 있음 | 있음 | 있음 |
| 5 | L · 입금부족액 | 표2 7행 (L_i 행 11행도 입금부족액) | 있음 | 있음 | 있음 |
| 6 | 접두 Y · 연환산을 뜻한다 · 예시 Y_r PY_a PY_t | 표1 2행 | 있음 | 있음 | 있음 |
| 6 | 표2 에 Y 단독 행 | 표2 | 없음 | 없음 | 없음 |
| 8 | d 마감시점 쿠콘 가상계좌의 현금 잔액 | 표2 EC 행 | 있음 | 있음 | 있음 |
| 9 | Σ A_i + EC ⏎ i 는 정산예정일이 d 보다 뒤인 대상정산금채권 | 표2 17행 | 있음 | 있음 | 있음 |
| 11 | PA = Σ A_i   i ∈ P | 표2 21행 | 있음 | 있음 | 있음 |
| 12 | PM = Σ M_i   i ∈ P · PB = Σ B_i   i ∈ P (각 1회) | 표2 22·23행 | 있음 | 있음 | 있음 |
| 13 | 관찰된 연환산수익률 기호 변경 | 표3 4행 변경점 | 있음 | 있음 | 있음 |

옛 문구 잔존: 「유동화투자자의 할인율」「d 시점 쿠콘」「PA ÷ (PA + PEC)」「i 는 정산예정일이 P 안에 든 채권」「예상 연환산수익률을 나타낼」 세 벌 모두 0건.

## 3. ⑤ 산식

| 항목 | 확인한 것 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|
| 표2 PY_t 행 1줄 | `PY_t = PY_a × PA × PD ÷ ( PA × PD + PEC )` | 있음 | 있음 (줄바꿈 run 확인) | 있음 |
| 표2 PY_t 행 2줄 | `= PM × 365 ÷ ( PA × PD + PEC )` | 있음 | 있음 (공백 5자 들여쓰기 유지) | 있음 |
| 표2 PY_t 기존 칸 | `wPY_MR` | 있음 | 있음 | 있음 |
| 표3 행 | `⑤ 분모 │ PA + PEC │ PA × PD + PEC` + 이유 | 있음 | 있음 | 있음 |

이유 문장 사실 검증

| 문장 | 근거 | 판정 |
|---|---|---|
| PEC 는 P 안 각 날의 EC 를 더한 값이라 원·일 | 대표 원문 55행 「PSC = 위 선택한 기간의 EC들의 합」 · 45행 EC 는 하루 마감 잔액. 날마다 잔액(원)을 일수만큼 더하므로 원·일 | 맞음 |
| PA 는 채권마다 한 번 더한 값이라 원 | 원문 47행 「투자 실행금(PSA) = 투자실행금들 합계」 · 52행 A_pi 는 채권별 순지급액 × (1−r). 채권당 한 번 | 맞음 |
| PA × PD 가 채권이 묶여 있던 원·일 | PD = Σ(A_i × D_i) ÷ PA (원문 51행) 이므로 PA × PD = Σ(A_i × D_i). 원장 재계산 556,626,436 = 179,916,643 × 3.0938018 | 맞음 |
| 두 줄 항등 | PY_a × PA × PD = (PM ÷ PA) × (365 ÷ PD) × PA × PD = PM × 365 | 맞음 |

표3 「기존」 칸 `PA + PEC` 는 원문 54행 「(④ × PSA) / (PSA + PSC)」 그대로입니다.

## 4. 유지 2곳

| 자리 | 있어야 할 문구 | 없어야 할 문구 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|---|
| 표2 Σ A_i 행 | i 는 정산예정일이 d 보다 뒤인 채권 | 조회대상기간 중 누계 | 유지 | 유지 | 유지 |
| 표2 D 행 | i 는 대상정산금채권 전체 | 표본에 속하는 | 유지 | 유지 | 유지 |

## 5. 금지 사항

| 패턴 | 아티팩트 | 워드 | HTML | final_terms.json |
|---|---|---|---|---|
| 신설 · 추가 · 기존에는 · 종전 · 이번에 · 신규 | 0 | 0 | 0 | 0 |
| `class="new"` 낱말 표식 | 0 | (해당 없음) | 0 | (해당 없음) |
| 대시(—) · 같은 말 두 번 | 0 | 0 | 0 | 0 |
| 상환예정일 · 조회일자 · 조회 기준일 | 0 | 0 | 0 | 0 |
| 정산예정일 · 기준일 표기 | 정산예정일 5 · 기준일 2 | 같음 | 같음 | 사용 |

`sym new` · `sym old` 는 개선·기존 기호의 색 스타일이고 직전 판과 같은 클래스입니다 (표2 D·PD·PY_a·PY_t·d 행, 표3 전 행).

조어 점검: 직전 판 대비 새로 들어온 한글 낱말 중 대표 원문에 없는 것은 `관찰된` `계약된` `연환산` `분모` `채권마다` `묶여 있던` `원이다` 뿐이며, 모두 채택 문구 1~4 와 ⑤ 이유 문장 안의 낱말입니다. 「한편 넣기」는 원문 43행에 있습니다. 「누적 순현금」「조회대상기간」은 직전 판부터 있던 표기입니다.

표기 두 벌: 표1 의미 칸은 채택 문구 그대로 「연환산 예상 수익률」「연환산 수익률」(띄어쓰기 있음, 2건), 표2·3 은 「연환산수익률」(4건). 빌더 보고와 같습니다.

## 6. 원고 `final_terms.json`

| 키 | 확인한 값 | 아티팩트와 | 판정 |
|---|---|---|---|
| `vars` PY_t formula | `PY_t = PY_a × PA × PD ÷ ( PA × PD + PEC ) = PM × 365 ÷ ( PA × PD + PEC )` | 두 줄을 한 줄로 이은 같은 식 | 일치 |
| `vars` r term | 계약된 할인율 | 같음 | 일치 |
| `vars` L term | 입금부족액 | 같음 | 일치 |
| `vars` Y term · plain | 연환산 · 「값이 아니라 수익률을 1년으로 늘려 잰다는 표시다. P 처럼 기호 앞에 선다 …」 | 표1 접두 Y 「연환산을 뜻한다」와 같은 뜻 · 개념 항이라 표2 행 아님 | 일치 |
| `vars` EC plain | 「기준일 마감시점에 … 남아 있는 현금이다」 | 표2 「d 마감시점」 | 일치 |
| `vars` PA · PM · PB formula | `PA = Σ A_i      i ∈ P` · `PM = Σ M_i      i ∈ P` · `PB = Σ B_i      i ∈ P` | 같음 | 일치 |
| `rules[1].body` ⑤ 인용 | `× PA × PD ÷ ( PA × PD + PEC )` | 같음 | 일치 |

`calc` 재계산 (PA 179,916,643 · PM 62,977 · PEC 140,000,000 · PD 3.093802 · ③ 4.129577)

| 항목 | 원고 표기 | 재계산 | 판정 |
|---|---|---|---|
| Σ(A_i × D_i) | 556,626,436 | 원장 조회기간 2,240건 합 = 556,626,436 (`daily_ledger.RECEIVABLES` 직접 합산) | 일치 |
| ④ 비중 | 0.799031 | 556,626,436 ÷ 696,626,436 = 0.7990315 | 일치 |
| ⑤ PY_t | 3.299662% · 화면 3.30% | 4.129577 × 556,626,436 ÷ 696,626,436 = 3.2996619% | 일치 |
| 연환산 수익금 | 7,429,796원 | 4.129577% × 179,916,643 = 7,429,796.31 | 일치 |
| 검산 PY_t | 7,429,796 ÷ ( PA + PEC ÷ PD ) = 3.299662% | 분모 225,168,407.66 · 몫 3.2996618% | 일치 · ⑤ 와 같은 값 |
| 검산 PY_a | 7,429,796 ÷ PA = 4.129577% | 4.1295768% | 일치 |

③ 4.129577 은 ① 을 여섯 자리로 끊고 곱한 값이며 (한 줄로 되짚으면 4.129628), 직전 판부터의 규칙이라 이 조 변경분이 아닙니다.

## 7. 검사기 실행

| 항목 | 실측 |
|---|---|
| 명령 | `python3 verify_final_terms.py` (pipeline 폴더에서 직접 실행) |
| 판정 | 136건 · PASS 133 · FAIL 3 |
| FAIL 목록 | A10 · I16 · I17 |
| A10 실측 | `4.129577% → 4.13% ↔ 4.13%  ·  3.299662% → 3.30% ↔ 2.32%` |
| I16 실측 | `2.32% ↔ 3.30` |
| I17 실측 | `화면 2.32% ↔ 원고산식 3.30 (두 자리로 끊으면 3.30)` |
| 빌더 보고와 | FAIL 목록·실측 문면 동일 |
| 자기시험 J | 7/7 · J3 원고 md5 무변조 |
| 판정 완화 여부 | 없음. `SAD` 는 채권 단위 Σ a_i × d_i (`verify_final_terms.py:164`), I16·I17 은 화면 값을 원고 표기·원고 산식과 대조 (`:721–727`). FAIL 이 나는 것 자체가 완화되지 않았다는 근거 |

FAIL 3건의 원인은 불일치 표 2번(화면·원장 ⑤ 옛 산식)입니다.

## 8. 워드 서식 (직전 판 `이전판/기호정리표_20260903_2329.docx` 대비)

| 항목 | 20260904_2126 | 20260903_2329 | 판정 |
|---|---|---|---|
| 표 스타일 | Table Grid ×3 | Table Grid ×3 | 같음 |
| 머리 셀 음영 | F4F1EC (표1 3셀 · 표2 5셀 · 표3 3셀) | 같음 | 같음 |
| 본문 폰트 | 맑은 고딕 · 산식 셀 D2Coding | 같음 | 같음 |
| 글자 크기 | 본문 9pt(sz 18) · 머리 8.5pt(sz 17) | 같음 | 같음 |
| 색 | 본문 1C1B19 · 머리 6E6A63 · 개선 2C4470 · 기존 A63D2F | 같음 | 같음 |
| 제목 | 기호 정리표 20pt 굵게 · 절 제목 14pt 굵게 | 같음 | 같음 |
| 페이지 | A4 세로 · 여백 같음 | 같음 | 같음 |
| 아래첨자 run | 표1 15 · 표2 42 · 표3 13 | 표1 9 · 표2 42 · 표3 13 | 표1 차이는 접두 Y 행·첨자 r/a/t 문구의 첨자(Y_r · PY_a · PY_t) 6개 |
| 굵게 run | 표1 3 · 표2 13 · 표3 14 | 표1 3 · 표2 13 · 표3 13 | 표3 차이는 ⑤ 분모 행의 개선 기호 1개 |
| 표2 PY_t 산식 셀 | 줄바꿈 run + 공백 5자 + 첨자 t·a run | | 조판 정상 |

## 판정

확정본으로 낼 수 있습니다. 문서 세 벌 자체에 고칠 것은 0건이고, 문서 밖 후속 3건(화면·원장 ⑤ 산식, 용어기호정리 재생성, 검사기 I15 라벨)과 정비 권고 2건(원고 D 개념 항 중복 구절, 생성기·원고 저장 위치)이 남습니다.
