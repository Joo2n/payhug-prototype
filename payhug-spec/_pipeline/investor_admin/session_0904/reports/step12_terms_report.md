# step12 — 기호 정리표 원고·검사기 V1.3 정합

대상 폴더 `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin`
기준 실물 `session_0904/artifact/투자자어드민 기호정리표_V1.3.html` · `.docx`
손댄 파일 세 개 — `final_terms.json` · `verify_final_terms.py` · `build_symreview.py`. 커밋 없음. 화면 생성기·출력 레포 손대지 않음.

## 1. 원고 변경 자리 (키 · 전 · 후)

`final_terms.json`. 표 2 의 term·formula·group·prev, 표 1 rules, 표 3 changes, 표 4 screen_terms, 각주 footnote 는 V1.3 html 을 파싱해 글자 그대로 넣었다(아래첨자는 원고 표기 `_i` · `_{d−1}`, `&nbsp;` 는 공백). plain·scopes·calc 는 손으로 고쳤다. 옛 판 사본 `/private/tmp/claude-501/-Users-semi-cursor-payhug/9aed3429-fc00-4785-9abd-c254e437cf03/scratchpad/final_terms.v11.json`.

| 키 | 전 | 후 |
|---|---|---|
| `basis` | 대표 용어 정의(2026-08-27) · 2·3차 미팅 · 8/31 DM · 9/1 논의 | 투자자어드민 기호정리표 V1.3 (2026-09-08) · 대표 용어 정의(2026-08-27) · 2·3차 미팅 · 8/31 DM · 9/1 논의 |
| `asof` | 2026-09-07 | 2026-09-08 |
| `rules` (3항 head/body) | 1 「i 가 있으면 채권 한 건…」 · 2 「w 는 가중치와의 곱이…」 · 3 「범위는 P 로만 표시한다」 | V1.3 표 1 의 8행 그대로 — 접두 P · 접두 Y · 아래첨자 · 첨자 i · 첨자 d · 첨자 r · 첨자 a · 첨자 t (키 n·head·body·example, head=분류 · body=의미 · example=예시) |
| `screen_terms` (11행 internal/screen/tooltip) | 내부 용어 ↔ 화면 용어 11행 (「같음」 「보유 채권」 「투자실행액 (원)」 …) | V1.3 표 4 의 15행 그대로 — 키 sym·screen·tooltip (「기호 \| 화면 \| 툴팁」) |
| `changes` | (없음) | V1.3 표 3 「기호 변경내역」 5행 — 키 point·before·after (w 삭제 · d−1 제거 · d 재정의 · 관찰된 연환산 수익률 기호 변경 · ⑤ 분모). 옛 표기가 여기에만 남는다 |
| `footnote` | (없음) | 일별 표의 한 행은 P 가 1일인 경우다. 주별은 7일, 월별은 그 달의 일수다. |
| `vars` 순서 | d · r · D · A · M · B · L · MR · Y · D_i … Σ A_i · D · EC · Σ A_i + EC · LR · Y_r · PA · PB · PM … | V1.3 표 2 행 순서 — r · D · A · M · B · L · MR · D_i · A_i · L_i · 채권매입수수료_i · M_i · B_i · Σ A_i · EC_d · Σ A_i + EC_d · D · LR · Y_r · PA · PM · PB · PMR · PD · PEC · PY_a · PY_t (27항) |
| `vars.d` | 항 전체 | 삭제 (V1.3 표 2 에 행 없음 → 표 1 규칙) |
| `vars.r.formula` | (없음) | 0.11% 예정 |
| `vars.D#개념.term` | 가중평균 금융일수 | 가중평균 금융일수 (Duration) |
| `vars.D#개념.plain` | 선정산일로부터 정산예정일까지 한편 넣어 센 일수다. 산식에는 ID가 i인 대상정산금채권의 것이 `D_i` 로, 정산예정일이 선택한 기간에 해당하는 채권들의 가중평균이 `PD` 로, 회수된 것을 포함한 대상정산금채권 전체의 가중평균이 `D` 로 나온다. | 선정산일로부터 정산예정일까지 한편 넣어 센 일수다. 산식에는 ID가 i인 보유 채권의 것이 `D_i` 로, 정산예정일이 선택한 기간에 해당하는 채권들의 가중평균이 `PD` 로, 회수된 것을 포함한 보유 채권 전체의 가중평균이 `D` 로 나온다. |
| `vars.D#개념.prev` | (없음) | wD |
| `vars.A.plain` | 투자자가 채권을 산 금액이다. 산식에는 낱건이 `A_i` 로, 어제 마감까지 회수되지 않은 채권의 합이 `Σ A_i` 로, 정산예정일이 조회기간에 든 채권들의 합이 `PA` 로 나온다. | 투자자가 채권을 산 금액이다. 산식에는 낱건이 `A_i` 로, 정산예정일이 d 보다 뒤인 채권의 합이 `Σ A_i` 로, 정산예정일이 검색대상기간에 든 채권들의 합이 `PA` 로 나온다. |
| `vars.M.plain` | 채권매입수수료에서 입금부족액을 뺀 값이다. 산식에는 낱건이 `M_i` 로, 정산예정일이 조회기간에 든 채권들의 합이 `PM` 으로 나온다. | 채권매입수수료에서 입금부족액을 뺀 값이다. 산식에는 낱건이 `M_i` 로, 정산예정일이 검색대상기간에 든 채권들의 합이 `PM` 으로 나온다. |
| `vars.B.plain` | 순지급액에서 입금부족액을 뺀, 실제로 돌아온 금액이다. 산식에는 낱건이 `B_i` 로, 정산예정일이 조회기간에 든 채권들의 합이 `PB` 로 나온다. | 순지급액에서 입금부족액을 뺀, 실제로 돌아온 금액이다. 산식에는 낱건이 `B_i` 로, 정산예정일이 검색대상기간에 든 채권들의 합이 `PB` 로 나온다. |
| `vars.L.plain` | 약속보다 덜 들어온 금액이다. 산식에는 ID가 i인 대상정산금채권의 것이 `L_i` 로 나오고, 표본집합에서 이 금액을 투자실행금으로 나눈 비율이 `LR` 이다. | 약속보다 덜 들어온 금액이다. 산식에는 ID가 i인 보유 채권의 것이 `L_i` 로 나오고, 표본집합에서 이 금액을 투자실행금으로 나눈 비율이 `LR` 이다. |
| `vars.MR.term` | 투자수익율 | 투자수익율 (Margin) |
| `vars.MR.plain` | 투자실행금 대비 투자수익의 비율이다. 산식에는 정산예정일이 조회기간에 든 채권들의 것이 `PMR` 로 나온다. | 투자실행금 대비 투자수익의 비율이다. 산식에는 정산예정일이 검색대상기간에 든 채권들의 것이 `PMR` 로 나온다. |
| `vars.Y` | 항 전체 | 삭제 (V1.3 표 2 에 행 없음 → 표 1 규칙) |
| `vars.D_i.term` | i번째 채권의 금융일수 | 금융일수 |
| `vars.D_i.formula` | (없음) | 선정산일로부터 정산예정일까지의 한편 넣기 일수⏎08-24 선정산 · 08-26 정산예정이면 2 |
| `vars.D_i.plain` | ID가 i인 대상정산금채권이 며칠 묶여 있는지를 센 값이다. 정수다. | ID가 i인 보유 채권이 며칠 묶여 있는지를 센 값이다. 정수다. |
| `vars.D_i.group` | (없음) | 채권 한 건 |
| `vars.A_i.term` | i번째 채권의 투자 실행액 | 투자실행액 |
| `vars.A_i.plain` | 투자자가 ID가 i인 대상정산금채권을 산 금액이다. 돈이 실제로 오갔는지와 무관하게 원장에 이 금액으로 적힌다. | 투자자가 ID가 i인 보유 채권을 산 금액이다. 돈이 실제로 오갔는지와 무관하게 원장에 이 금액으로 적힌다. |
| `vars.A_i.group` | (없음) | 채권 한 건 |
| `vars.L_i.term` | i번째 채권의 입금부족액 | 입금부족액 |
| `vars.L_i.plain` | ID가 i인 대상정산금채권에 약속보다 덜 들어온 금액이다. 더 들어왔으면 음수가 된다. | ID가 i인 보유 채권에 약속보다 덜 들어온 금액이다. 더 들어왔으면 음수가 된다. |
| `vars.L_i.group` | (없음) | 채권 한 건 |
| `vars.채권매입수수료_i.term` | i번째 채권의 채권매입수수료 | 채권매입수수료 |
| `vars.채권매입수수료_i.plain` | 투자자가 채권을 살 때 순지급액에서 떼는 몫이다. 원장은 원 단위 절사를 건별로 걸지 않고 정산예정일이 같은 채권을 묶어 한 번만 건다. 건별로 끊으면 절사가 겹쳐 조회기간 7일에서 1,145원이 어긋난다. | 투자자가 채권을 살 때 순지급액에서 떼는 몫이다. 원장은 원 단위 절사를 건별로 걸지 않고 정산예정일이 같은 채권을 묶어 한 번만 건다. 건별로 끊으면 절사가 겹쳐 검색대상기간 7일에서 1,145원이 어긋난다. |
| `vars.채권매입수수료_i.group` | (없음) | 채권 한 건 |
| `vars.M_i.term` | i번째 채권의 투자수익 | 투자수익 |
| `vars.M_i.group` | (없음) | 채권 한 건 |
| `vars.B_i.term` | i번째 채권의 상환액 | 상환액 |
| `vars.B_i.plain` | ID가 i인 대상정산금채권에서 실제로 돌아온 금액이다. | ID가 i인 보유 채권에서 실제로 돌아온 금액이다. |
| `vars.B_i.group` | (없음) | 채권 한 건 |
| `vars.Σ A_i.term` | 투자 실행액 | 투자실행액 |
| `vars.Σ A_i.formula` | Σ A_i      i 는 선정산일이 d 전날 이전이고 정산예정일이 d 이후인 대상정산금채권 2,240건 (d 전날 마감까지 회수되지 않은 채권) | Σ A_i⏎i 는 정산예정일이 d 보다 뒤인 채권 |
| `vars.Σ A_i.plain` | 투자자가 채권으로 들고 있는 돈이다. 어제 마감까지 회수되지 않은 채권만 센다. 투자 자산 화면이 「투자실행액」 80,000,000원으로 띄운다. 투자 수익 화면의 「투자실행금」 `PA` 는 정산예정일이 조회기간에 든 채권을 세므로 179,970,919원으로 다르다. | 투자자가 채권으로 들고 있는 돈이다. 정산예정일이 d 보다 뒤인 채권만 센다. 투자 자산 화면이 「투자실행액」 80,000,000원으로 띄운다. 투자 수익 화면의 「투자실행금」 `PA` 는 정산예정일이 검색대상기간에 든 채권을 세므로 179,970,919원으로 다르다. |
| `vars.Σ A_i.group` | (없음) | 투자 자산 |
| `vars.D#집계.formula` | D = Σ( A_i × D_i ) ÷ Σ A_i      i 는 대상정산금채권 전체 61,760건 (회수된 것 포함) · 발생 기준 | i 는 보유 채권 전체 |
| `vars.D#집계.plain` | 투자한 돈이 평균 며칠 묶여 있는지를 잰 값이다. 가중치가 건수가 아니라 금액이라 큰 채권의 날짜가 더 세게 반영된다. 옆 칸 투자 실행액은 회수되지 않은 채권만 세지만 이 값은 회수된 것을 포함한 대상정산금채권 전체를 센다. 투자 자산 화면의 「가중평균 금융일수」 3.04일이 이 값이고, 투자 수익 화면의 같은 라벨 3.11일은 정산예정일이 조회기간에 든 채권만 센 `PD` 다. 산식의 가중치 `A_i ÷ Σ A_i` 도 비중이라 부르지만 화면 「비중」 열과는 다른 값이다. | 투자한 돈이 평균 며칠 묶여 있는지를 잰 값이다. 비중이 건수가 아니라 금액이라 큰 채권의 날짜가 더 세게 반영된다. 옆 칸 투자실행액은 정산예정일이 d 보다 뒤인 채권만 세지만 이 값은 회수된 것을 포함한 보유 채권 전체 61,760건을 센다. 투자 자산 화면의 「가중평균 금융일수」 3.04일이 이 값이고, 투자 수익 화면의 같은 라벨 3.11일은 정산예정일이 검색대상기간에 든 채권만 센 `PD` 다. 산식의 `A_i ÷ Σ A_i` 도 비중이라 부르지만 화면 「비중」 열과는 다른 값이다. |
| `vars.D#집계.note` | 대상정산금채권 전체 | 보유 채권 전체 |
| `vars.D#집계.group` | (없음) | 투자 자산 |
| `vars.D#집계.prev` | (없음) | wD |
| `vars.EC.formula` | (없음) | d 일 마감시점 쿠콘 가상계좌의 현금 잔액 |
| `vars.EC.plain` | 어제 마감시점에 채권으로 나가지 않고 쿠콘 가상계좌에 남아 있는 현금이다. 투자 자산 화면 카드 「순현금」 20,000,000원이 이 값이다. 투자 수익 화면의 `PEC` 140,000,000원은 조회기간 안 각 날 마감시점의 이 값을 더한 것이다. | d 일 마감시점에 채권으로 나가지 않고 쿠콘 가상계좌에 남아 있는 현금이다. 투자 자산 화면 카드 「순현금」 20,000,000원이 이 값이다. 투자 수익 화면의 `PEC` 140,000,000원은 검색대상기간 안 각 날의 이 값을 더한 것이다. |
| `vars.EC.sym` | EC | EC_d |
| `vars.EC.group` | (없음) | 투자 자산 |
| `vars.Σ A_i + EC.formula` | Σ A_i + EC      i 는 선정산일이 d 전날 이전이고 정산예정일이 d 이후인 대상정산금채권 (d 전날 마감까지 회수되지 않은 채권) | Σ A_i + EC_d⏎i 는 정산예정일이 기준일(d) 보다 뒤인 보유 채권 |
| `vars.Σ A_i + EC.plain` | 어제 마감 시점의 채권과 현금을 합친 금액이다. 화면 「합계」 행이 이 값이다. 현황표 「비중」 열은 이 합을 분모로 재서 투자실행액이 80.0%, 순현금이 20.0% 가 된다. 가맹점별 표의 「비중」 열은 분모가 투자실행액이라 같은 이름에 분모가 둘이다. | d 일 마감시점의 채권과 현금을 합친 금액이다. 화면 「합계」 행이 이 값이다. 현황표 「비중」 열은 이 합을 분모로 재서 투자실행액이 80.0%, 순현금이 20.0% 가 된다. 가맹점별 표의 「비중」 열은 분모가 투자실행액이라 같은 이름에 분모가 둘이다. |
| `vars.Σ A_i + EC.sym` | Σ A_i + EC | Σ A_i + EC_d |
| `vars.Σ A_i + EC.group` | (없음) | 투자 자산 |
| `vars.LR.formula` | LR = Σ L_i ÷ Σ A_i      i 는 선정산일이 d 의 20일 전부터 11일 전까지인 채권 · 분모 Σ A_i 는 같은 표본의 투자 실행액 합 | LR = Σ L_i ÷ Σ A_i⏎i 는 선정산일이 오늘(d 의 다음 날) 기준 20일 전부터 11일 전까지인 채권 |
| `vars.LR.plain` | 약속보다 덜 들어온 비율이다. 대표 정의서가 「표본집합」으로 적은, 선정산일이 오늘 기준 20일 전부터 11일 전까지인 열흘치 대상정산금채권으로 잰다. 분모 Σ A_i 는 같은 표본의 투자 실행액 합이다. | 약속보다 덜 들어온 비율이다. 대표 정의서가 「표본집합」으로 적은, 선정산일이 오늘 기준 20일 전부터 11일 전까지인 열흘치 보유 채권으로 잰다. 분모 Σ A_i 는 같은 표본의 투자실행액 합이다. |
| `vars.LR.group` | (없음) | 투자 자산 |
| `vars.Y_r.group` | (없음) | 투자 자산 |
| `vars.PA.term` | 기간 투자실행금 | 검색대상기간의 투자실행금 |
| `vars.PA.formula` | PA = Σ A_i      i ∈ P | PA = Σ A_i   i ∈ P |
| `vars.PA.plain` | 정산예정일이 선택한 기간에 해당하는 대상정산금채권들의 투자 실행액 합계다. 투자 수익 화면이 「투자실행금」 179,970,919원으로 띄운다. 투자 자산 화면의 「투자실행액」 `Σ A_i` 는 어제 마감까지 회수되지 않은 채권을 세므로 80,000,000원으로 다르다. | 정산예정일이 선택한 기간에 해당하는 보유 채권들의 투자실행액 합계다. 투자 수익 화면이 「투자실행금」 179,970,919원으로 띄운다. 투자 자산 화면의 「투자실행액」 `Σ A_i` 는 정산예정일이 d 보다 뒤인 채권을 세므로 80,000,000원으로 다르다. |
| `vars.PA.group` | (없음) | 투자 수익 |
| `vars.PB.term` | 기간 상환액 | 검색대상기간의 상환액 |
| `vars.PB.formula` | PB = Σ B_i      i ∈ P | PB = Σ B_i   i ∈ P |
| `vars.PB.plain` | 정산예정일이 선택한 기간에 해당하는 대상정산금채권들의 상환액 합계다. | 정산예정일이 선택한 기간에 해당하는 보유 채권들의 상환액 합계다. |
| `vars.PB.group` | (없음) | 투자 수익 |
| `vars.PM.term` | 기간 투자수익 | 검색대상기간의 투자수익 |
| `vars.PM.formula` | PM = Σ M_i      i ∈ P | PM = Σ M_i   i ∈ P |
| `vars.PM.plain` | 정산예정일이 선택한 기간에 해당하는 대상정산금채권들의 투자수익 합계다. | 정산예정일이 선택한 기간에 해당하는 보유 채권들의 투자수익 합계다. |
| `vars.PM.group` | (없음) | 투자 수익 |
| `vars.PMR.term` | 기간 투자수익율 | 검색대상기간의 투자수익율 |
| `vars.PMR.group` | (없음) | 투자 수익 |
| `vars.PD.term` | 기간 가중평균 금융일수 | 검색대상기간의 가중평균 금융일수 |
| `vars.PD.formula` | PD = Σ( A_i × D_i ) ÷ Σ A_i      i ∈ P | PD = Σ( A_i × D_i ) ÷ Σ A_i   i ∈ P |
| `vars.PD.plain` | 정산예정일이 선택한 기간에 해당하는 대상정산금채권을 한 뭉치로 모아 금융일수를 투자실행금으로 가중한 값이다. 대표 정의서 [2번 이미지] 가 「PSD = (Σ Api x Dpi) / PSA」 로 적은 그대로다. 날마다 낸 `PD` 을 다시 가중해도 같은 값이 나오나, 그 길은 중간에 한 번 더 끊어 소수 여섯째 자리가 밀릴 수 있다. 투자 수익 화면의 「가중평균 금융일수」 합계 3.11일이 이 값이고, 투자 자산 화면의 같은 라벨 3.04일은 회수된 것을 포함한 대상정산금채권 전체를 센 `D` 다. | 정산예정일이 선택한 기간에 해당하는 보유 채권을 한 뭉치로 모아 금융일수를 투자실행금으로 가중한 값이다. 대표 정의서 [2번 이미지] 가 「PSD = (Σ Api x Dpi) / PSA」 로 적은 그대로다. 날마다 낸 `PD` 을 다시 가중해도 같은 값이 나오나, 그 길은 중간에 한 번 더 끊어 소수 여섯째 자리가 밀릴 수 있다. 투자 수익 화면의 「가중평균 금융일수」 합계 3.11일이 이 값이고, 투자 자산 화면의 같은 라벨 3.04일은 회수된 것을 포함한 보유 채권 전체를 센 `D` 다. |
| `vars.PD.group` | (없음) | 투자 수익 |
| `vars.PD.prev` | (없음) | PwD |
| `vars.PEC.term` | 기간 순현금 | 검색대상기간의 누적 순현금 |
| `vars.PEC.formula` | P 안 각 날 마감시점 EC 를 더한 값 | PEC = Σ EC_d   d ∈ P |
| `vars.PEC.plain` | 조회기간 각 날 마감시점의 순현금을 더한 값이다. 대표 정의서 55행 「PSC = 위 선택한 기간의 EC들의 합」 그대로다. 예시 원장은 잔액을 20,000,000 상수로 두어 조회기간 일수만큼 곱한 값이 된다. | 검색대상기간 각 날 마감시점의 순현금을 더한 값이다. 대표 정의서 55행 「PSC = 위 선택한 기간의 EC들의 합」 그대로다. 예시 원장은 잔액을 20,000,000 상수로 두어 검색대상기간 일수만큼 곱한 값이 된다. |
| `vars.PEC.group` | (없음) | 투자 수익 |
| `vars.PY_a.term` | 투자실행금액 대비 연환산 수익률 | 검색대상기간의 투자실행금액 대비 연환산 수익률 |
| `vars.PY_a.group` | (없음) | 투자 수익 |
| `vars.PY_a.prev` | (없음) | PY_{MR} |
| `vars.PY_t.term` | 투자 자산 대비 연환산 수익률 | 검색대상기간의 투자 자산 대비 연환산 수익률 |
| `vars.PY_t.formula` | PY_t = PM × 365 ÷ ( Σ( A_i × D_i ) + PEC )      i ∈ P⏎     = PY_a × Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC ) | PY_t = PY_a × 채권 비중 + 순현금 수익률 × 순현금 비중⏎채권 비중 = Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )   i ∈ P⏎순현금 비중 = PEC ÷ ( Σ( A_i × D_i ) + PEC )⏎순현금 수익률 = 0 (계좌에 둔 돈은 수익을 만들지 않는다)⏎PY_a = PMR × 365 ÷ PD = ( PM ÷ PA ) × 365 ÷ ( Σ( A_i × D_i ) ÷ PA ) = PM × 365 ÷ Σ( A_i × D_i )⏎PY_t = PM × 365 ÷ Σ( A_i × D_i ) × Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC ) + 0 × PEC ÷ ( Σ( A_i × D_i ) + PEC )⏎     = PM × 365 ÷ ( Σ( A_i × D_i ) + PEC ) |
| `vars.PY_t.group` | (없음) | 투자 수익 |
| `vars.PY_t.prev` | (없음) | wPY_{MR} |
| `calc.steps[3]` | PD 기간 가중평균 금융일수 | PD 검색대상기간의 가중평균 금융일수 |
| `calc.steps[7]` | ④ 투자실행금 비중 = Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )   Σ( A_i × D_i ) = 559,275,516 | ④ 채권 비중 = Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )   Σ( A_i × D_i ) = 559,275,516 |
| `calc.기준` | 어제 마감 2026-08-27 · 조회기간 2026-08-21 ~ 08-27 | 기준일(d) 2026-08-27 마감 · 검색대상기간(P) 2026-08-21 ~ 08-27 |
| `scopes[없음].def` | 범위 표시가 없으면 그 값이 실린 화면 칸의 모집단을 따른다. 투자 실행액은 어제 마감까지 회수되지 않은 채권 2,240건을 세고, 가중평균 금융일수는 회수된 것을 포함한 대상정산금채권 전체 61,760건을, 입금부족률은 표본 3,200건을 센다. 같은 행에 나란히 있어도 세는 집합이 다르다. | 범위 표시가 없으면 그 값이 실린 화면 칸의 모집단을 따른다. 투자실행액은 정산예정일이 d 보다 뒤인 채권 2,240건을 세고, 가중평균 금융일수는 회수된 것을 포함한 보유 채권 전체 61,760건을, 입금부족률은 표본 3,200건을 센다. 같은 행에 나란히 있어도 세는 집합이 다르다. |
| `scopes[P].name` | 기간 | 검색대상기간 |
| `scopes[P].def` | 정산예정일이 사용자가 선택한 기간에 해당하는 대상정산금채권이다. 기간은 어제까지이고 오늘은 포함하지 않는다. 일별 표의 한 행은 이 기간이 1일인 경우이고, 주별은 7일, 월별은 그 달의 일수다. | 정산예정일이 사용자가 선택한 기간에 해당하는 보유 채권이다. 기간은 기준일(d) 까지이고 오늘(d 의 다음 날)은 포함하지 않는다. 일별 표의 한 행은 P 가 1일인 경우다. 주별은 7일, 월별은 그 달의 일수다. |
| `scopes[조건 문장].def` | 선정산일이 오늘 기준 20일 전부터 11일 전까지인 대상정산금채권이다. 대표 정의서가 「표본집합」으로 적었고 입금부족률에만 쓴다. | 선정산일이 오늘(d 의 다음 날) 기준 20일 전부터 11일 전까지인 보유 채권이다. 대표 정의서가 「표본집합」으로 적었고 입금부족률에만 쓴다. |

**plain 에서 낱말만 바꾼 규칙** — 대상정산금채권 → 보유 채권 · 조회기간 → 검색대상기간 · 투자 실행액 → 투자실행액 · 가중치 → 비중(D 집계 plain 「산식의 가중치 `A_i ÷ Σ A_i` 도 비중이라」 는 「산식의 `A_i ÷ Σ A_i` 도 비중이라」 로 낱말만 뺌) · 「어제 마감까지 회수되지 않은 채권」 → V1.3 조건 문장 「정산예정일이 d 보다 뒤인 채권」 · 「어제 마감시점」 → 「d 일 마감시점」.

**값** — 179,970,919 · 61,175 · 3.11(3.107588) · 559,275,516 · 140,000,000 · 3.99(3.992511) · 3.19(3.193182) · 80,000,000 / 20,000,000 · 13.21 · 3.04 · 2,240건 · 61,760건 · 3,200건 전부 그대로. `ledger_facts.json` 과의 일치는 A·B·D·G·K 절이 판정한다.

## 2. 검사기 변경 — `verify_final_terms.py`

### (가) 추가 절 T — 실물 대조 (`verify_final_terms.py:1127–1302`, `main()` 등록 `:1419`)

V1.3 html·docx 를 각각 파싱해 표 1~4 를 원고와 행마다 견준다. 판정 135건.

| 판정 | 무엇 |
|---|---|
| `T0` | html·docx 실물 존재 |
| `T1` | `ceo_review.html` 본문 = V1.3 html 본문 (`build_symreview.py` 원고가 확정본과 같은가) |
| `T.h.h1` `T.h.h2` `T.d.h2` | 제목 「기호 정리표」 · 절 제목 4개 |
| `T.h.note` `T.d.note` | 각주 = `footnote` |
| `T.{h,d}.tables` | 표 4개 |
| `T.{h,d}.t{1..4}.head` | 머리행 = 「분류·의미·예시」 「분류·개선·기존·용어 이름·산식」 「변경점·기존·개선」 「기호·화면·툴팁」 |
| `T.{h,d}.t{1..4}.rows` | 행 수 = 원고 (대상 0건 아님) |
| `T.{h,d}.t1.r1..8` | 표 1 행 = `rules[].head·body·example` |
| `T.{h,d}.t2.r1..27` | 표 2 행 = `vars[].group·sym·prev·term·formula` |
| `T.{h,d}.t3.r1..5` | 표 3 행 = `changes[].point·before·after` |
| `T.{h,d}.t4.r1..15` | 표 4 행 = `screen_terms[].sym·screen·tooltip` |

읽는 법 — html 은 `<sub>x</sub>` → `_x`(한 글자 i r a d t) 또는 `_{x}`, `<br>` → 줄바꿈. docx 는 run 의 `font.subscript` 로 같은 표기를 되살린다(`v13_docx` `:1218`). 비교 전 공백만 접는다(`tnorm` `:989` — 줄바꿈·nbsp·연속 공백 → 공백 하나). 그 밖의 글자는 한 자라도 다르면 그 행이 FAIL 이고 detail 에 「n번째 칸 실물 「…」 ↔ 원고 「…」」 를 찍는다.

### (나) 뒤집은 판정 — V1.1 전제 → V1.3 기대값

| 판정 | 줄 | 전 (V1.1 기대) | 후 (V1.3 기대) |
|---|---|---|---|
| `A6` | 214 | 제목 「wD(대상정산금채권 전체)」 | 제목 「D(보유 채권 전체)」 (판정식 같음) |
| `F.*.EC` | 441 | 제목 「EC(d−1) = 전일자 자정 잔액」 · detail 「원고도 formula 없음」 | 「EC_d = d 일 마감시점 잔액」 · 「원고 산식 칸은 정의 문장(V1.3)」 (판정식 같음) |
| `G3` | 481 | D 항에 「전체」 또는 「발생 기준」 | D 항에 「보유 채권 전체」 |
| `I27` | 793 | 표 4 「같음」 행은 internal 을 쪼개고, 그 밖은 screen 칸 통째로 라벨 | screen 칸 「화면 이름 · 라벨」 에서 라벨만(`_label`), 「(일별 표)」 뗌, 라벨 뒤 「(원)」 단위 허용 |
| `I28` | 797 | 「오늘 (d) → 표시하지 않음」 | 「d → 표시하지 않음」 (판정식 같음) |
| `K0` | 858 | 개념 항 Y 는 산식 없어야 함(특례) | Y 특례 삭제 — Y 는 vars 에 없고 표 1 접두 규칙 |
| `K1` | 869 | 홀로 서지 않는 갈래 글자(A L M B MR Y) 가 전건 개념 항목 | 개념 항목 ∪ 표 1 접두 글자(P Y). EC 는 예외 `NO_CONCEPT_ROW={'EC'}` — V1.3 표 2 에 EC 개념 행이 없고 EC_d 행 하나가 순현금 정의를 진다(표 3 「잔액 EC_d 만 첨자 d 를 둔다」). EC 가 갈래 글자로 있고 홀로 서지 않는 것도 함께 판정 |
| `K10` | 932 | D 집계 formula 에 61,760건 | D 집계 formula == 「i 는 보유 채권 전체」 이고 61,760 은 plain 에 |
| `K13` | 955 | `_plain('EC')` | `_plain('EC_d')` |
| `K14` | 963 | 제목 「wD 가중치」 | 제목 「D 산식의 A_i ÷ Σ A_i」 (판정식 같음) |
| `S0` | 999 | ≥11행 · 키 internal·screen·tooltip | ==15행 · 키 sym·screen·tooltip |
| `S1` | 1015 | (Y_r)·(PY_t) 행 screen == term | Y_r·PY_a·PY_t 행 screen 이 「투자 자산 · 」/「투자 수익 · 」 로 시작하고 라벨 == term 에서 「검색대상기간의 」 뗀 것 |
| `S2` | 1028 | internal 괄호 안 기호가 vars·scopes 에 실재 | sym 칸 낱말이 vars 기호·scopes 표시·표 1 접두/첨자 글자·vars 용어 이름(채권매입수수료)에 실재 |
| `S3` | 1047 | 「같음」 행 내부 용어가 term 에 실재 | 화면 라벨 == term(P 접두 행은 「검색대상기간의 」 뗀 것 · P 는 scopes 이름) 13행 전건 |
| `S4` | 1051 | screen 「투자실행액 (원)」 「검색대상기간」 「표시하지 않음」 ← internal 「투자 실행액」 「조회기간」 「오늘」 | Σ A_i → 「투자 자산 · 투자실행액」 · P → 「투자 수익 · 검색대상기간」 · d → 「표시하지 않음」(툴팁 빈 칸) · 마지막 행 「PMR PEC L 채권매입수수료 \| 화면에 쓰지 않음 \| 툴팁 값으로만」 |
| `S4b` | 1059 | 「보유 채권」 행 툴팁에 「회수된 것 포함」 | D 행 툴팁 == 「보유 채권 전체」 · LR 행 툴팁 == 「선정산일이 오늘 기준 20일 전 ~ 11일 전인 표본」 |
| `S5` | 1078 | d term 「오늘」 · plain 「화면을 보는 날 … P 는 d 전날까지」 · EC formula None · plain 「어제 마감시점」 시작 · Σ A_i/투자자산 조건 「선정산일이 d 전날 이전이고 정산예정일이 d 이후 … d 전날 마감까지 회수되지 않은」 · D formula 「회수된 것 포함」 · LR formula 「분모 Σ A_i 는 같은 표본」 · PEC formula 「P 안 각 날 마감시점 EC 를 더한 값」 · PY_t 2줄 | rules 「첨자 d」 body == 「하나의 일 (d = 기준일, d−1 = 기준일의 전일, d−2 = 기준일의 전전일)」 · example == 「EC_d EC_{d−1} EC_{d−2} …」 · vars 에 d·Y·EC 없음 · EC_d formula == 「d 일 마감시점 쿠콘 가상계좌의 현금 잔액」 · Σ A_i == 「Σ A_i / i 는 정산예정일이 d 보다 뒤인 채권」 · Σ A_i + EC_d == 「… / i 는 정산예정일이 기준일(d) 보다 뒤인 보유 채권」 · D == 「i 는 보유 채권 전체」 · LR == 「LR = Σ L_i ÷ Σ A_i / i 는 선정산일이 오늘(d 의 다음 날) 기준 20일 전부터 11일 전까지인 채권」 · LR plain 에 「분모 Σ A_i 는 같은 표본」 · PEC == 「PEC = Σ EC_d d ∈ P」 · 네 산식 칸에 어제·오늘·전날 0 |
| `S5c` (새) | 1090 | (없음) | PY_t formula 7줄 — 1줄 「PY_t = PY_a × 채권 비중 + 순현금 수익률 × 순현금 비중」 … 7줄 「= PM × 365 ÷ ( Σ( A_i × D_i ) + PEC )」 · 「가중치」 0 · 「비중」 ≥4 |
| `S6` | 1093 | PY_a term 「투자실행금액 대비 연환산 수익률」 · PY_t 「투자 자산 대비 연환산 수익률」 | 「검색대상기간의 투자실행금액 대비 연환산 수익률」 · 「검색대상기간의 투자 자산 대비 연환산 수익률」 · 투자 수익 8행 전부 「검색대상기간의 」 로 시작 · Σ A_i·A_i term 「투자실행액」 · 투자자산 · 순현금 |
| `S7` | 974 (`OLD_WORDS`) · 1117 | 금지어 기준일 · 연환산수익률 · 투자자산 대비 · 투자자산 기준 · d 마감시점 · EC_d · 첨자 d | 뺀 것 — 기준일 · d 마감시점 · EC_d · 첨자 d (V1.3 이 쓰는 말). 더한 것 — 대상정산금채권 · 조회기간 · 조회대상기간 · 조회시점 · 투자 실행액 · 가중치 · 회수된 것 포함 · d 전날 · d 0시. 검사 자리에 rules example · footnote 추가. 예외 — vars alias·prev(표 2 「기존」 열) · changes(표 3) |
| `S8` (새) | 1121 | (없음) | 원고 파일 전체(alias·prev·changes 포함) 「가중치」 0건 · 「비중」 ≥6 |
| `main()` 출력 | 1428 | 「어제 마감 %s」 | 「기준일(d) 마감 %s」 |

### (다) 판별력 시험 J (`:1343–`)

기존 11건 중 사라진 항목을 바꾸고 T 절용 시험을 붙였다. 기대 판정을 튜플로 여러 개 걸면 전부 새로 FAIL 로 나야 통과한다. 26건 전건 PASS.

| 시험 | 변조 | 잡힌 판정 |
|---|---|---|
| calc.PA · calc.③ · 검산.연환산 · 겹침.투자실행액 | (기존 그대로) | B2 · B7 · D1 · K7 |
| vars.A_i | `(1 + r)` | E1 · T.h.t2.r9 · T.d.t2.r9 |
| vars.LR | 표본 구간 없는 산식 | G9 · T.h.t2.r18 |
| rules.접두Y삭제 (전 「개념.Y삭제」) | 표 1 「접두 Y」 행 삭제 | K1 · T.h.t1.rows |
| 개념.D산식설명 | 산식 칸에 설명 문장 | K0 · T.d.t2.r2 |
| screen_terms.Y_r | 「예상 연환산수익률」 | S1 · S7 · T.h.t4.r6 |
| rules.첨자d (전 「vars.d기준일」) | 「전일」→「전날」 한 낱말 | S5 · T.h.t1.r5 · T.d.t1.r5 |
| vars.PEC옛문장 (전 「vars.PEC첨자」 — 방향 반대) | 「P 안 각 날 마감시점 EC 를 더한 값」 | S5 · T.h.t2.r25 · T.d.t2.r25 |
| vars.EC첨자없음 | sym EC_d → EC | S5 · T.h.t2.r15 |
| vars.⑤한줄 | PY_t 마지막 줄만 | S5c · T.d.t2.r27 |
| vars.가중치 · vars.조회기간 · vars.대상정산금채권 | plain 낱말 하나 | S7(+S8) |
| vars.term띄어쓰기 | 「투자자산 대비」 | S6 · S7 · T.h.t2.r27 |
| changes.⑤분모 · changes.행삭제 | 「PA − PEC」 · 마지막 행 삭제 | T.{h,d}.t3.r5 · T.{h,d}.t3.rows |
| screen_terms.LR툴팁 · D툴팁옛말 | 「21일」 · 「(회수된 것 포함)」 | S4b · T.{h,d}.t4.r5 / S4b · S7 · T.d.t4.r4 |
| footnote | 「8일」 | T.h.note · T.d.note |
| vars.순서 · 기존열 · 분류열 | 행 순서 · prev · group 한 칸 | T.{h,d}.t2.r1 / r24 / r20 |

## 3. 최종 판정

`python3 verify_final_terms.py` (화면 렌더 + 자기시험 포함) — **판정 302건 · PASS 302 · FAIL 0 · 종료코드 0**. 절별 A12 B12 C5 D6 E11 F17 G12 H9 I29 J28 K15 S11 T135. 화면 빼고(`FT_NOSCREEN=1`) 245건 전건 PASS. 출력 전문 `/private/tmp/claude-501/-Users-semi-cursor-payhug/9aed3429-fc00-4785-9abd-c254e437cf03/scratchpad/step12_verify_full.txt`.

`build_symreview.py` — 원고 경로를 `session_0904/artifact/ceo_review.html` 로(`:29–30`, 머리 주석 `:5`). `HOME` 을 스크래치패드로 돌려 `/private/tmp/claude-501/-Users-semi-cursor-payhug/9aed3429-fc00-4785-9abd-c254e437cf03/scratchpad/step12_symreview/Downloads/payhug_용어정의서/기호정리표_v13_regen.docx` · `.html` 로 냈다(Downloads 에 안 씀 — 확인함). 대조 결과 — docx 표 4개 233칸 원문 그대로 비교 다른 칸 0 · 문단 6개 같음 · `word/document.xml` 바이트 동일(130,000B) · html 바이트 동일(md5 766527efab).

## 4. V1.3 본문 안 모순·확인 필요 (고치지 않음)

| # | 자리 | 무엇 |
|---|---|---|
| 1 | 표 4 PY_t 툴팁 「… PEC · EC 값」 · 표 3 ⑤ 분모 「PEC 는 P 안 각 날의 EC 를 더한 값」 | 기호표는 `EC_d` 만 두는데(표 3 「잔액 EC_d 만 첨자 d 를 둔다」) 산문에는 첨자 없는 `EC` 가 남아 있다. 검사기는 기호 칸만 보고(S2) 툴팁 산문은 T 절이 글자 그대로 견주기만 하므로 통과. 대표 확인 필요 |
| 2 | 표 2 Σ A_i 「i 는 정산예정일이 d 보다 뒤인 채권」 vs Σ A_i + EC_d 「i 는 정산예정일이 기준일(d) 보다 뒤인 보유 채권」 | 같은 집합을 두 문장으로 적었다(채권/보유 채권 · d/기준일(d)). 원고는 두 칸을 각각 글자 그대로 둠 |
| 3 | 표 2 EC_d 「d 일 마감시점」 vs 대표 원문 `ceo_definitions.md` 12행 「순현금: 조회시점 현재의 …」 | NEXT_SESSION 5번 그대로 대표 확인 대기. V1.3 은 마감시점 |
| 4 | 표 2 D_i 산식 예 「08-24 선정산 · 08-26 정산예정이면 2」 | 대표 원문 [2번] 과 같음. [1번] 「선정산일 =260827, 정산예정일 = 260808 이면 1」 은 원문 자체 오기로 보임 — V1.3 문제 아님 |

## 5. 하지 않은 것 · 다르게 한 것

| 항목 | 내용 |
|---|---|
| 「회수된 것을 포함한」 | plain 네 곳(D 개념·D 집계·PD plain · scopes 없음)에 「회수된 것을 포함한 보유 채권 전체」 를 남겼다. 금지 문자열 「회수된 것 포함」 과는 다른 글자이고, D(3.04일)가 Σ A_i(미회수 2,240건)와 갈리는 뜻을 설명하는 자리라 뺐다가는 뜻이 준다. 빼야 하면 네 곳 낱말 삭제로 끝난다 |
| 새 키 | `vars[].group`(표 2 분류) · `vars[].prev`(표 2 기존) · `rules[].example`(표 1 예시) · 최상위 `changes`(표 3) · `footnote`(각주) · `screen_terms` 키 internal → sym. 표 1~4 를 행 단위로 대조하려면 원고에 그 칸이 있어야 해서 넣었다. 기존 소비자(`build_final.py` · `build_finaledit.py` · `read_finaledit.py` · `verify_finaledit.py`)가 읽는 head·body·mark·name·def·term·sym·formula·plain·alias·kind 는 그대로. 그 넷은 돌리지 않았다(NEXT_SESSION 9번 「09-02 판에 멈춤」 상태 그대로) |
| `vars.note` · `vars.alias` | V1.3 표에 없는 원고 전용 칸이라 그대로 뒀다(D 집계 note 만 「보유 채권 전체」 로). LR alias 「입금부족률」(term 과 같음)은 손대지 않음 |
| `alias_table.branch_rows` 부작용 | Y 가 개념 항에서 빠져 갈래 표 Y 행 이름 칸이 빈다. `alias_table.py` 는 손대지 않았고 `build_final.py` 출력에만 보인다 |
| 문장을 바꾼 세 자리 | `calc.기준` 「어제 마감 …·조회기간 …」 → 「기준일(d) 2026-08-27 마감 · 검색대상기간(P) …」 · `scopes[P].def` 「기간은 어제까지이고 오늘은 포함하지 않는다」 → 「기간은 기준일(d) 까지이고 오늘(d 의 다음 날)은 포함하지 않는다」 · `calc.steps[7]` 「④ 투자실행금 비중」 → 「④ 채권 비중」(V1.3 ⑤ 전개의 이름). 새 문장은 아니고 V1.3 낱말로 바꾼 것 |
| `basis` · `asof` | 지시 목록에 없는 메타 키인데 V1.3 (2026-09-08) 로 올렸다 |
| 공백 | 원고 산식 「i ∈ P」 앞은 3칸(V1.3 의 ` &nbsp; `), PY_t 7줄째 앞 5칸. T 절은 공백을 접어 견주므로 칸 수는 판정하지 않는다 |
| `build_symreview.py` OUT | `~/Downloads/payhug_용어정의서` 기본값은 그대로. 이번 실행만 `HOME`+`PYTHONPATH`(사용자 site-packages 의 python-docx) 로 스크래치패드에 냈다 |
| `NEXT_SESSION.md` | 10번 완료를 적지 않았다(지시 밖) |
| 화면·출력 레포 · `ceo_definitions.md` | 읽기만. `git status` 에 다른 조의 `_fig/*` 변경이 있고 손대지 않음 |
