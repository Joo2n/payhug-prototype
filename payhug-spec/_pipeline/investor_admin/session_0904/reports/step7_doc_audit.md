# 기호 정리표 산식 표기 감사 (2026-09-04 21:55 판)

검토 대상: 아티팩트 `ceo_review.html`(21:53:57) · 워드 `기호정리표_20260904_2155.docx` · HTML `기호정리표_20260904_2155.html` · 원고 `final_terms.json`(21:53:57) · 검사기 `verify_final_terms.py`(21:54:47) · 원장 `ledger_facts.json`(21:58:17). 셀 텍스트·수치는 산출물에서 직접 뽑아 다시 셌습니다. 빌더 보고 `step7_notation_report.md`(21:58:12)는 마지막 대조에만 썼습니다.

## 위반·불일치

| # | 구분 | 확인한 사실 | 근거 | 판정 | 고칠 것 |
|---|---|---|---|---|---|
| 1 | 검사기 I26 FAIL | `verify_final_terms.py` 실행 결과 **137건 · PASS 136 · FAIL 1 (I26)**. I26 은 화면 툴팁에 `PA 179,916,643` 이 있는지 보는데, 화면 `invest-profit.html:174`(21:58:25 재생성)의 툴팁은 `Σ( A_i × D_i ) 556,626,436원` · `PEC 140,000,000원` 을 댑니다. 화면이 새 산식으로 바뀌었고 검사기 기준이 옛 화면(PSA = PA)에 남았습니다 | `verify_final_terms.py:766` `format(PA, ',') in h['text']` · 제목 「PSA = PA · PSC = PEC 를 그대로 댄다」 · `_audit7/verify_full.txt:259` | 검사기 기준이 낡음 · 표기 조는 I26 을 건드리지 않았고(before/after diff 에 없음) ty5 조는 보고 217행에서 「다른 조 담당」으로 넘김 · **담당 공백** | `:766` 의 `PA` 를 `SAD`(Σ A_i × D_i, 556,626,436)로 바꾸고 제목을 `Σ( A_i × D_i ) · PEC 를 댄다` 로 |
| 2 | 빌더 보고 검사기 수치 | 보고 「135 / 137 · FAIL I16 I17」 ↔ 실행 「136 / 137 · FAIL I26」. 보고 시각 21:58:12, 화면 재생성 21:58:25 · 원장 21:58:17. 보고 당시엔 맞는 수치였고 13초 뒤 화면이 바뀌어 I16·I17 이 통과로, I26 이 FAIL 로 뒤집혔습니다 | `stat` 수정 시각 · `verify_full.txt:239–241` I16 `3.30% ↔ 3.30` · I17 `화면 3.30% ↔ 원고산식 3.30` | 보고가 낡음 · 오류 아님 | 없음 (1번을 고치면 137 / 137) |
| 3 | 소멸위험 (전 판 7번과 같음) | 생성기 원고가 세션 스크래치패드 `ceo_review.html` 이고(`build_symreview.py:29–31`), 생성기는 git 미추적(`??`)입니다. 개념 행 산식 6개·PD·PEC·PY_t 두 줄·표 1 Y 행 괄호는 전부 그 파일에만 있습니다 | `git status --short build_symreview.py` → `??` | 소멸위험 · 구조적 · 이 조가 만든 구조 아님 | 원고를 저장소 안으로 옮기고 생성기 커밋 |
| 4 | 전 판 이월 (이 조 범위 밖) | (a) `final_terms.json` 을 읽는 용어기호정리 워드·HTML 은 09-02 23:45 판이 최신 · (b) `.note` 문단 「일별 표의 한 행은 P 가 1일인 경우다 …」 지시 여부 확인 필요 · (c) 검사기 I15 제목 `3.99%` · (d) 워드 하드 줄바꿈 4셀(표 1 첨자 예시 · 표 3 w 삭제 2셀 · d−1 제거) | `~/Downloads/payhug_용어정의서/` 목록 · 아티팩트 163행 · `verify_full.txt:237` · 워드 `w:br` 실측 t0r3c2 · t2r1c1 · t2r1c2 · t2r2c1 | 전 판 판정 그대로 | 전 판 조치 그대로 (`build_final.py` 재실행 · 지시 없으면 note 삭제 · I15 제목 · 생성기 `add_break()`) |

문서 세 벌(아티팩트·워드·HTML)의 텍스트에서 규칙 셋·지시와 어긋난 곳은 **0건**입니다.

## 1. 표 2 전 산식 (28행 · 셀에서 직접 추출)

규칙: ① 정의 줄 `이름 = Σ 낱건, 범위` ② 이름 있는 합은 이름으로만 ③ 한 산식 안에서 풀면 다 풀고 접으면 다 접기.

| # | 기호 | 산식 (아티팩트 108~160행) | 판정 | 근거 |
|---|---|---|---|---|
| 1 | d | (없음) | 규칙 밖 | 기준일 상수 |
| 2 | r | `0.11% 예정` | 규칙 밖 | 값 |
| 3 | D (개념) | `D = Σ( A_i × D_i ) ÷ Σ A_i` | 적합 | ③ 분자·분모 다 풀림 · 범위 없음 · 첨자는 Σ 안에만 |
| 4 | A (개념) | `A = 순지급액 × ( 1 − r )` | 적합 | 첨자 없는 일반형 |
| 5 | M (개념) | `M = 채권매입수수료 − max( 0, L )` | 적합 | 일반형 |
| 6 | B (개념) | `B = 순지급액 − max( 0, L )` | 적합 | 일반형 |
| 7 | L (개념) | `L = 미지급금 − 과지급금` | 적합 | 일반형 |
| 8 | MR (개념) | `MR = M ÷ A` | 적합 | ② 이름만 |
| 9 | D_i | `선정산일로부터 정산예정일까지의 한편 넣기 일수 ⏎ 08-24 선정산 · 08-26 정산예정이면 2` | 규칙 밖 | 기초 항목 정의문 |
| 10 | A_i | `A_i = 순지급액_i × (1 − r)` | 적합 | 낱건 정의 |
| 11 | L_i | `L_i = 미지급금_i − 과지급금_i` | 적합 | 낱건 정의 |
| 12 | 채권매입수수료_i | `채권매입수수료_i = 순지급액_i × r` | 적합 | 낱건 정의 |
| 13 | M_i | `M_i = 채권매입수수료_i − max(0, L_i)` | 적합 | 낱건 정의 |
| 14 | B_i | `B_i = 순지급액_i − max(0, L_i)` | 적합 | 낱건 정의 |
| 15 | Σ A_i | `Σ A_i ⏎ i 는 정산예정일이 d 보다 뒤인 채권` | 적합 | ① 이름 없는 합 + 범위 · 유지 문구 |
| 16 | EC | `d 마감시점 쿠콘 가상계좌의 현금 잔액` | 규칙 밖 | 기초 항목 정의문 · 채택 8 |
| 17 | Σ A_i + EC | `Σ A_i + EC ⏎ i 는 정산예정일이 d 보다 뒤인 대상정산금채권` | 적합 | ① · 채택 9 |
| 18 | D (집계) | `i 는 대상정산금채권 전체` | 규칙 밖 | 범위만 · 산식은 3번 개념 행 · 유지 문구 |
| 19 | LR | `LR = Σ L_i ÷ Σ A_i ⏎ i 는 선정산일이 d 의 20일 전부터 11일 전까지인 채권` | 적합 | ①③ 분자·분모 다 풀림 |
| 20 | Y_r | `Y_r = r × 365 ÷ D` | 적합 | ② 이름만 |
| 21 | PA | `PA = Σ A_i   i ∈ P` | 적합 | ① · 채택 11 |
| 22 | PM | `PM = Σ M_i   i ∈ P` | 적합 | ① · 채택 12 |
| 23 | PB | `PB = Σ B_i   i ∈ P` | 적합 | ① · 채택 12 |
| 24 | PMR | `PMR = PM ÷ PA` | 적합 | ② 이름만 |
| 25 | PD | `PD = Σ( A_i × D_i ) ÷ Σ A_i   i ∈ P` | 적합 | ③ **분모 `Σ A_i` 확인** · 분자 Σ·분모 이름 꼴 아님 |
| 26 | PEC | `PEC = Σ EC   d ∈ P` | 적합 | ① · 목록 밖 자리 (빌더 고지 3번) |
| 27 | PY_a | `PY_a = PMR × 365 ÷ PD` | 적합 | ② 이름만 |
| 28 | PY_t | 1줄 `PY_t = PM × 365 ÷ ( Σ( A_i × D_i ) + PEC )   i ∈ P` ⏎ 2줄 `= PY_a × Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )` | 적합 | **지시 문면 두 줄 확인** · PM·PEC·PY_a 는 이름 있는 합이라 이름, Σ( A_i × D_i ) 는 이름이 없어 Σ |

| 집계 | 건수 |
|---|---|
| 적합 | **23** |
| 규칙 밖 (상수·값·기초 정의문·범위만) | 5 |
| 위반 | **0** |
| `PA × PD` 조립 | **0** (아티팩트·워드·HTML·원고 전부 0) |

판정이 갈리는 자리 하나. 개념 D 행은 `A_i`·`D_i` 첨자가 있어 「첨자 없는 일반형」을 글자대로 읽으면 어긋납니다. 가중평균은 Σ 없이 쓸 수 없고, 지시가 PD 에 준 꼴이 같은 `Σ( A_i × D_i ) ÷ Σ A_i` 이며, 검사기 K0 도 「첨자는 Σ 가 있을 때만」으로 정했으므로 적합으로 뒀습니다.

참고. 괄호 안 띄어쓰기가 두 벌입니다. 낱건 행 `(1 − r)` `max(0, L_i)` · 개념 행 `( 1 − r )` `max( 0, L )`. 규칙 셋이 띄어쓰기를 정하지 않아 위반은 아니며 빌더가 6번으로 고지했습니다. 한 표 안에서 눈에 띄므로 통일 여부는 사용자 결정입니다.

## 2. 개념 행 6개 · 표 1 접두 Y

| 자리 | 있어야 할 것 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|
| 표 2 개념 A | `A = 순지급액 × ( 1 − r )` | 109행 있음 | 있음 | 있음 |
| 표 2 개념 M | `M = 채권매입수수료 − max( 0, L )` | 110행 있음 | 있음 | 있음 |
| 표 2 개념 B | `B = 순지급액 − max( 0, L )` | 111행 있음 | 있음 | 있음 |
| 표 2 개념 L | `L = 미지급금 − 과지급금` | 112행 있음 | 있음 | 있음 |
| 표 2 개념 MR | `MR = M ÷ A` | 113행 있음 | 있음 | 있음 |
| 표 2 개념 D | `D = Σ( A_i × D_i ) ÷ Σ A_i` | 108행 있음 | 있음 | 있음 |
| 표 1 접두 Y | `연환산을 뜻한다 ( × 365 ÷ 금융일수 )` | 81행 있음 | 있음 | 있음 |
| 표 2 Y 단독 행 | 없어야 함 | 없음 | 없음 | 없음 |

개념 6행 모두 `i 는`·`∈`·건수·`d−1` 범위 조건이 없고, 우변 낱말이 원고에 실재하는 기호와 기초 항목(순지급액·미지급금·과지급금·채권매입수수료·max)뿐입니다.

## 3. 세 벌 셀 텍스트 대조

| 항목 | 아티팩트 | 워드 | HTML |
|---|---|---|---|
| 표 수 | 3 | 3 | 3 |
| 행 수 (표1 · 표2 · 표3) | 8 · 29 · 6 | 8 · 29 · 6 | 8 · 29 · 6 |
| 셀 수 | 187 | 187 | 187 |
| 텍스트가 다른 셀 | 0 | **0** | **0** |
| 줄바꿈만 다른 셀 | 0 | 4 (전 판과 같은 자리) | 0 |

HTML 은 아티팩트에 `<!doctype>`·`<head>`·`<body>` 껍데기만 씌운 것이고 본문은 바이트 단위로 같습니다 (`diff` 머리 4줄·꼬리 2줄만). 워드 생성 시각(21:55:02)이 아티팩트 수정(21:53:57) 뒤라 현재 아티팩트에서 나온 판입니다.

전 판(`prev4`) 대비 diff 는 81 · 108–113 · 136 · 152 · 154 · 160 · 189행 8곳이며 빌더 (가) 표와 줄 번호·문면이 일치합니다. 줄 수 192 · `<tr>` 43/43 · `<td>` 176/176 · `<th>` 11/11 · `<sub>` 81/81 · `<span>` 80/80 으로 태그가 맞고, 손대지 않은 행(표 1 다른 행 · 낱건 6행 · 투자 자산 5행 · 표 3 다른 4행 · `.note`)은 전 판과 같습니다. 부수 피해 없음.

## 4. 채택 11곳 · 유지 2곳 (전 판 판정 대비)

| 번호 | 문구 | 자리 | 아티팩트 | 워드 | HTML |
|---|---|---|---|---|---|
| 1 | 예상 수익률을 나타낼 때 쓴다. Y_r 는 연환산 예상 수익률 (Y 가 연환산을 뜻하므로) | 표1 첨자 r | 있음 | 있음 | 있음 |
| 2 | 투자실행금 기준 관찰된 값을 나타낼 때 쓴다. PY_a 는 … (P 는 조회기간) | 표1 첨자 a | 있음 | 있음 | 있음 |
| 3 | 투자자산 기준 관찰된 값을 나타낼 때 쓴다. PY_t 는 … | 표1 첨자 t | 있음 | 있음 | 있음 |
| 4 | r · 계약된 할인율 | 표2 2행 | 있음 | 있음 | 있음 |
| 5 | L · 입금부족액 | 표2 7행 (L_i 행 포함 2회) | 있음 | 있음 | 있음 |
| 6 | 접두 Y · 연환산을 뜻한다 ( × 365 ÷ 금융일수 ) · 표2 Y 단독 행 없음 | 표1 2행 | 있음 | 있음 | 있음 |
| 8 | d 마감시점 쿠콘 가상계좌의 현금 잔액 | 표2 EC 행 | 있음 | 있음 | 있음 |
| 9 | Σ A_i + EC ⏎ i 는 정산예정일이 d 보다 뒤인 대상정산금채권 | 표2 17행 | 있음 | 있음 | 있음 |
| 11 | PA = Σ A_i   i ∈ P | 표2 21행 | 있음 | 있음 | 있음 |
| 12 | PM = Σ M_i   i ∈ P · PB = Σ B_i   i ∈ P | 표2 22·23행 | 있음 | 있음 | 있음 |
| 13 | 관찰된 연환산수익률 기호 변경 | 표3 4행 | 있음 | 있음 | 있음 |
| 유지 | i 는 정산예정일이 d 보다 뒤인 채권 (조회대상기간 중 누계 없음) | 표2 Σ A_i 행 | 유지 | 유지 | 유지 |
| 유지 | i 는 대상정산금채권 전체 (표본에 속하는 없음) | 표2 D 집계 행 | 유지 | 유지 | 유지 |

옛 문구 잔존 (세 벌 + 원고): 「유동화투자자의 할인율」「d 시점 쿠콘」「PA ÷ (PA + PEC)」「i 는 정산예정일이 P 안에 든 채권」「예상 연환산수익률을 나타낼」「조회대상기간 중 누계」「표본에 속하는」「투자실행금으로 가중평균한」「PA + PEC ÷ PD」「× PA ÷ (PA + PEC)」「( Σ A_i × D_i )」「( Σ L_i )」「( Σ A_i )」 전부 **0건**.

## 5. 금지어

| 패턴 | 아티팩트 | 워드 | HTML | final_terms.json |
|---|---|---|---|---|
| 신설 · 추가 · 기존에는 · 종전 · 이번에 · 신규 | 0 | 0 | 0 | 0 |
| 대시(—) | 0 | 0 | 0 | 0 |
| 상환예정일 · 조회일자 · 조회 기준일 | 0 | 0 | 0 | 0 |
| `class="new"` 낱말 표식 | 0 | (해당 없음) | 0 | (해당 없음) |
| 전일자 | 1 | 1 | 1 | **0** |

「전일자」 1건은 표 3 「d 재정의」 행의 기존 칸 `D−1 전일자`(182행)로 옛 기호 설명이며 전 판부터 있던 자리입니다. 원고 D 개념 항의 중복 구절(전 판 5번)은 지워져 0건입니다.

조어 점검. 전 판 대비 새로 들어온 한글 낱말은 표 3 ⑤ 분모 이유 문장의 「정산된」「투자실행액」「금액 × 일수」뿐이며 대표 원문 47·52·55행의 낱말입니다. 표 1 Y 행 「금융일수」는 원문 43행 용어입니다.

## 6. 원고 `final_terms.json`

### 6-1. formula 대조

| 키 | 원고 값 | 아티팩트와 | 판정 |
|---|---|---|---|
| `vars` D(개념) | `D = Σ( A_i × D_i ) ÷ Σ A_i` | 같음 | 일치 |
| `vars` A · M · B · L · MR(개념) | `A = 순지급액 × ( 1 − r )` · `M = 채권매입수수료 − max( 0, L )` · `B = 순지급액 − max( 0, L )` · `L = 미지급금 − 과지급금` · `MR = M ÷ A` | 같음 | 일치 |
| `vars` Y(개념) | `null` | 표2 에 Y 행 없음 | 일치 |
| `vars` PD | `PD = Σ( A_i × D_i ) ÷ Σ A_i      i ∈ P` | 같음 (공백 폭만) | 일치 |
| `vars` PEC | `PEC = Σ EC      d ∈ P` | 같음 | 일치 |
| `vars` PY_t | `PY_t = PM × 365 ÷ ( Σ( A_i × D_i ) + PEC ) = PY_a × Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )      i ∈ P` | 두 줄을 한 줄로 이은 같은 식 · `i ∈ P` 가 끝에 (아티팩트는 1줄 끝) | 일치 · 조판 차이 |
| `vars` D(집계) | `D = Σ( A_i × D_i ) ÷ Σ A_i      i 는 대상정산금채권 전체 61,760건 · 발생 기준` | 아티팩트 집계 행은 범위만 | 의도한 차이 · 빌더 고지 2번 · 검사기 G2·G3·K10 이 이 formula 를 읽음 |
| `vars` D(개념).plain | `… D_i 로, 정산예정일이 선택한 기간에 해당하는 채권들의 가중평균이 PD 로, …` | PD 1회 | 전 판 5번 해소 |
| `rules[1].body` | `× Σ( A_i × D_i ) ÷ ( Σ( A_i × D_i ) + PEC )` | 아티팩트 PY_t 2줄과 같은 꼴 | 일치 |

### 6-2. `calc` 재계산 (Decimal 40자리 · PA 179,916,643 · PM 62,977 · PEC 140,000,000)

| 항목 | 원고 표기 | 재계산 | `ledger_facts.json` | 판정 |
|---|---|---|---|---|
| Σ( A_i × D_i ) | 556,626,436 | (원장 값 그대로) | `weekAD` 556626436 | 일치 |
| PD | 3.093802 | 556,626,436 ÷ 179,916,643 = 3.0938018 → 3.093802 | `weekWRaw` 3.093802 | 일치 |
| ① PMR | 0.035003% | 0.0350034321 → 0.035003 | `weekMR` 0.035003 | 일치 |
| ③ PY_a | 4.129577% | 0.035003 × 117.977815 = 4.1295775 → 4.129577 | `weekTyRaw` 4.129577 | 일치 |
| ④ 비중 | 0.799031 | 556,626,436 ÷ 696,626,436 = 0.7990315 → 0.799031 | (키 없음) | 일치 |
| ⑤ PY_t | 3.299662% · 화면 3.30% | 4.129577 × 0.7990315 = 3.2996619 → 3.299662 | `weekTyAssetRaw` 3.299662 · `weekTyAsset` 3.30 | 일치 |
| 연환산 수익금 | 7,429,796원 | 4.129577% × 179,916,643 = 7,429,796.31 | | 일치 |
| 검산 PY_a | 7,429,796 ÷ PA = 4.129577% | 4.1295768% | | 일치 |
| 검산 PY_t | 7,429,796 ÷ ( ( Σ( A_i × D_i ) + PEC ) ÷ PD ) = 3.299662% | 분모 225,168,410.42 · 몫 3.2996618% | | 일치 · ⑤ 와 같은 값 |

### 6-3. 검산 줄 판정 (빌더 고지 1번)

| 확인 | 결과 |
|---|---|
| 지시 문면 `22,986,605 ÷ 696,626,436 = 3.299662%` 가 성립하는가 | **성립하지 않음.** 22,986,605 ÷ 696,626,436 = **3.299703%** |
| 두 값이 갈리는 자리 | ① PMR 을 여섯 자리로 끊는 곳 하나. 안 끊으면 ③ 4.129628 · ⑤ 3.299703, 끊으면 ③ 4.129577 · ⑤ 3.299662. 차이 0.000041%p 전부 여기서 남 |
| 검산 분모 `( Σ( A_i × D_i ) + PEC ) ÷ PD` | = 696,626,436 ÷ 3.0938018 = 225,168,410.42. 옛 `PA + PEC ÷ PD` 와 같은 값 (`Σ( A_i × D_i ) ÷ PD = PA` 이므로). 검산 값 3.299662 가 ⑤ 사슬 값과 맞음 |
| 빌더 결정 | 타당. PM × 365 꼴로 쓰면 검산이 3.299703 이 되어 ⑤ 3.299662 와 다섯째 자리에서 어긋나고, 그 차이를 설명하는 문장이 따로 필요해짐 |
| 독자에게 산식과 다른 꼴로 보이는가 | 정의 산식 `PM × 365 ÷ ( … )` 와 검산 `연환산수익금 ÷ ( ( … ) ÷ PD )` 는 꼴이 다르지만, 검산 절은 PY_a 줄 `7,429,796 ÷ PA` 와 나란히 「같은 분자를 다른 분모로」 읽히게 짠 구조이고 넷째 줄이 그것을 말합니다. 문제 없음으로 판정. 이 줄은 기호정리표 세 벌에는 없고 용어기호정리(`build_final.py`) 재생성 때 나타납니다 |
| 대안 (참고) | `7,429,796 × PD ÷ ( Σ( A_i × D_i ) + PEC )` 도 3.299662 이고 정의 꼴에 가깝지만 넷째 줄 「같은 분자」가 성립하지 않아 지워야 함. 사용자 선택 사항 |

## 7. 검사기 실행

| 항목 | 실측 |
|---|---|
| 명령 | `python3 verify_final_terms.py` (pipeline 폴더 · 화면 렌더 포함) |
| 판정 | **137건 · PASS 136 · FAIL 1** |
| FAIL 목록 | **I26** 「hover 툴팁이 PSA = PA · PSC = PEC 를 그대로 댄다」 |
| I16 | PASS `3.30% ↔ 3.30` |
| I17 | PASS `화면 3.30% ↔ 원고산식 3.30 (두 자리로 끊으면 3.30)` |
| A10 | PASS `4.129577% → 4.13% ↔ 4.13% · 3.299662% → 3.30% ↔ 3.30%` |
| D3 · D4 | PASS `7429796 ÷ 225168410 = 3.299662%` · 넷째 줄 「같은 분자」 |
| H0 | PASS `Σ( A_i × D_i ) ÷ Σ A_i` 문면 |
| K0 | PASS 개념 6항 일반형 · Y 산식 없음 |
| 자기시험 J | 8 / 8 · J3 원고 md5 `77f843a7…` 무변조 · J.개념.D산식설명 (설명 문장을 넣으면 K0 가 잡음) PASS |
| 화면 ⑤ | `invest-profit.html:174` 툴팁 `PM × 365 ÷ ( Σ( A_i × D_i ) + PEC )` · `Σ( A_i × D_i ) 556,626,436원` · `PEC 140,000,000원` · 값 3.30 |
| 원장 | `daily_ledger.py:181` `TY5_EXPR = 'ty4 * ad / tot'` · `ledger_facts.json` `weekTyAsset "3.30"` |
| 빌더 보고와 | 보고 「135 / 137 · FAIL I16 I17」 ↔ 실행 「136 / 137 · FAIL I26」. 위 불일치 2번 |

검사기 변경(step7 `before` 대비 diff)에 판정 완화는 없습니다. B9·C3·D3·D4·I17 은 제목 문면만, H0 은 `'A_i × D_i' in f` → `'Σ( A_i × D_i ) ÷ Σ A_i' in f` 로 조임, K0 은 「산식 없음」→「일반형 산식 + 재료 검사」로 조임, J 1건 추가. I26 은 diff 에 없습니다.

## 8. 워드 서식 (직전 판 `이전판/기호정리표_20260904_2126.docx` 대비)

| 항목 | 20260904_2155 | 20260904_2126 | 판정 |
|---|---|---|---|
| 표 스타일 · 크기 | Table Grid · 8×3 · 29×5 · 6×3 | 같음 | 같음 |
| 머리 셀 음영 | F4F1EC 11셀 | 같음 | 같음 |
| 글자 크기 | 머리 8.5pt 11 · 본문 9pt 307 | 8.5pt 11 · 9pt 281 | 본문 run 26 증가 = 산식 셀 8칸분 |
| 폰트 | 맑은 고딕 218 · D2Coding 100 | 210 · 82 | 산식 run 18 증가 |
| 색 | 1C1B19 251 · 6E6A63 11 · 2C4470 23 · A63D2F 33 | 229 · 11 · 19 · 33 | 파랑 +4 = 표 3 ⑤ 분모 `Σ( A_i × D_i ) + PEC` 첨자 run |
| 첨자 run (표1·2·3) | 15 · 49 · 17 | 15 · 42 · 13 | 표 2 +7 = D 개념·PD·PY_t 첨자 · 표 3 +4 |
| 굵게 run | 3 · 13 · 18 | 3 · 13 · 14 | 표 3 +4 = 같은 자리 |
| 하드 줄바꿈 | 1 · 5 · 5 | 1 · 6 · 5 | 표 2 −1 = D 집계 행 산식 줄 삭제 |
| 페이지 | A4 세로 · 여백 22/22/20/20mm | 같음 | 같음 |
| 제목 | 기호 정리표 20pt 굵게 · 절 제목 14pt 굵게 3개 | 같음 | 같음 |
| 본문 문단 | `.note` 1개 | 같음 | 같음 |

서식 차이는 전부 내용 변화로 설명되고 스타일·음영·크기·색 코드·페이지는 같습니다.

## 빌더 보고와 다른 점

| 보고 주장 | 실제 | 판정 |
|---|---|---|
| 검사기 135 / 137 · FAIL I16 I17 | 136 / 137 · FAIL I26 | 보고 시각 뒤 화면·원장이 바뀜 · 보고가 낡음 |
| 화면 `invest-profit.html:174` 는 여전히 2.32% | 3.30% · 새 툴팁 (21:58:25) | 같은 이유 |
| 고친 줄 8곳 · 줄 수 192 | diff 로 8곳 · 192줄 확인 | 일치 |
| 워드 표 3개 · 29×5 · 첨자 81 · 파랑 23 | 실측 같음 | 일치 |
| HTML 본문 아티팩트와 바이트 일치 | diff 껍데기 6줄만 | 일치 |
| 검사기 완화 0건 | before/after diff 로 확인 | 일치 |
| 검산 줄 3.299703 ↔ 3.299662 | Decimal 재계산 일치 | 일치 |

## 판정

**문서 세 벌은 확정본으로 낼 수 있습니다.** 표 2 산식 위반 0건 · 세 벌 셀 diff 0건 · 채택 11·유지 2 그대로 · 금지어 0건 · 원고와 원장 값 일치. 문서 밖 고칠 것 **1건**(검사기 I26 기준을 새 화면 툴팁 `Σ( A_i × D_i )` 로) 과 전 판 이월 4건(용어기호정리 재생성 · `.note` 지시 확인 · I15 제목 · 생성기 원고 위치)이 남습니다.
