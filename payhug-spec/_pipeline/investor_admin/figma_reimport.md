# 투자자 어드민 Figma 재임포트 — 내용 변경 9프레임

대상 — `Tcf69tIciGxmlqCIuRb0iI` / 페이지 `3066:328` "[투자자 어드민] 제안서 첨부용"
원본 — `/Users/semi/cursor/payhug-investor-admin` HEAD `6e4c270` (읽기 전용. 워킹트리 변경 0건 · 커밋 0건 · push 0건)
방식 — `generate_figma_design`(DOM 전송) 네이티브 레이어. 래스터 캡처 이미지 0건.

페이지 직계 자식 **32프레임 불변**. 그 중 9프레임을 신규 노드로 교체하고 구 노드 9개를 삭제했다.

착수 시점에 `get_metadata`로 페이지 직계 32건을 전수 반환해 노드ID를 재확인했다. `monthly_fix.md` §4 · `gate_fix.md` §8이 적은 9개 노드ID는 전건 현행과 일치했다.

---

## §1 전처리

### 1-1. 원본 기준

| 항목 | 값 |
|---|---|
| 커밋 | `6e4c270` — Figma 재임포트 대상 9프레임, 투자 수익 3건 추가 |
| 직전 커밋 | `9a2db60` — 투자 수익 일별 원장 단일화 |
| 워킹트리 | 변경 0건 |
| 루트 HTML | 40 |

스테이징 사본 `_fig/`의 32파일 중 **9파일**을 `6e4c270` 시점 원본으로 갈아끼웠다. 나머지 23파일은 원본과 내용이 같아 손대지 않았다(`capture.js` 주입 행 제외 diff 0).

내용이 어긋난 파일은 정확히 9개다. `merchants--filtered` · `merchants--empty` · `password` 3종의 diff는 `value` 필드 치환분뿐이라 대상이 아니다.

### 1-2. `value` 필드 재계수

`prep_fig.py`를 갱신 사본에 다시 돌렸다. 대상 9화면 중 `value` 보유 화면은 **3화면 6필드**다.

| 화면 | 유형 | 필드 | 구운 값 |
|---|---|---|---|
| `invest-profit` | date | 2 | `2026. 08. 21.` · `2026. 08. 27.` |
| `invest-profit--monthly` | date | 2 | `2026. 08. 01.` · `2026. 08. 27.` |
| `invest-profit--empty` | date | 2 | `2026. 02. 01.` · `2026. 02. 07.` |

`invest-profit--empty`의 두 날짜는 원본에서 `2026-07-01`·`2026-07-07` → `2026-02-01`·`2026-02-07`로 바뀐 값이다.

페이지 전체 기준 `value` 필드는 여전히 17개다 — 날짜 6 · 마스킹 9 · 검색어 2. 이번에 다시 구운 것은 그 중 날짜 6이다.

### 1-3. 필드 폭 실측 — 채택값 148

계측을 두 번 했고 값이 갈렸다.

| 계측 | `invest-profit` 시작일 · 종료일 |
|---|---|
| 1차 (갱신 직후 사본) | `152` · `153` |
| 2차 (원본 레포 직접) | `148` · `148` |

152/153을 구운 사본을 원본 렌더와 픽셀 비교하니 날짜 필드가 넓어지며 우측 `검색`·`초기화` 버튼이 약 6px 밀렸다. 148로 다시 구운 뒤 차이 구간이 달력 아이콘 글리프 띠(y 200~214) 하나로 줄었다. **채택값은 148×40**이며, 1차 라운드 실측(`figma_import_result.md` §1-2)과 같다.

### 1-4. 전처리 렌더 대조

| 화면 | 차픽셀(채널차 32 초과) | 차이 구간 |
|---|---|---|
| `invest-profit` | 1,154 (0.055%) | y 200~214 · x 302~603 |
| `invest-profit--monthly` | 1,159 (0.057%) | 동일 |
| `invest-profit--empty` | 1,138 (0.062%) | 동일 |

세 화면 모두 차이가 인라인 SVG 달력 아이콘 영역에만 남는다. 날짜 문자열·버튼 위치는 픽셀 일치다.

### 1-5. 뷰포트 높이 재실측

`figcap_ia.sh`의 87px 보정을 그대로 쓰되, 프레임 높이는 치환 후 사본을 1440폭으로 다시 재어 잡았다.

| 화면 | 종전 프레임 h | 신규 프레임 h | 증분 | 늘어난 요소 |
|---|---|---|---|---|
| `index` | 1739 | **1739** | 0 | 문자열 교체뿐 |
| `certificate` | 1224 | **1356** | +132 | 지면 배너 + 문서 안 고지 |
| `invest-profit` | 1429 | **1458** | +29 | 표 아래 집계 규칙 고지 1줄 |
| `invest-profit--monthly` | 1384 | **1413** | +29 | 동일 |
| `invest-profit--empty` | 1271 | **1271** | 0 | 문자열 교체뿐 |
| `xls-assets-status` | 667 | **729** | +62 | 지면 배너 + 시트 2행 고지 |
| `xls-assets-merchant` | 1059 | **1121** | +62 | 동일 |
| `xls-profit-status` | 723 | **785** | +62 | 동일 |
| `xls-profit-daily` | 807 | **869** | +62 | 동일 |

행 피치 2613에 여유가 있어 격자 재배치는 없다. 각 행 최대 높이는 y=0 행 1536, y=7839 행 1413, y=20904 행 1121로 모두 피치 안이다.

`capture.js` 주입 32/32. 잔존 `<input value=…>` 0건, 치환 `<span data-fig-input>` 17건.

---

## §2 프레임별 교체

캡처는 청크 3건씩 3라운드로 나눴다. 좌표·프레임 이름·`clipsContent`는 구 노드 값을 그대로 승계했다.

| 프레임 | 좌표 | 구 노드(삭제) | 신 노드 | 크기 |
|---|---|---|---|---|
| 02 투자자산 증명서 | (1600, 0) | `3123:2` | **`3156:2`** | 1440×1356 |
| 03 투자 수익 | (3200, 0) | `3126:2` | **`3154:2`** | 1440×1458 |
| 03-a 투자 수익 — 월별 | (0, 7839) | `3136:2` | **`3157:2`** | 1440×1413 |
| 03-b 투자 수익 — 결과 없음 | (1600, 7839) | `3131:2` | **`3158:2`** | 1440×1271 |
| 09 화면 갤러리 | (4800, 18291) | `3142:2` | **`3155:2`** | 1440×1739 |
| 11 엑셀 산출물 서식 — 투자자산 현황 | (1600, 20904) | `3147:2` | **`3159:2`** | 1440×729 |
| 12 엑셀 산출물 서식 — 가맹점별 투자자산 | (3200, 20904) | `3149:2` | **`3160:2`** | 1440×1121 |
| 13 엑셀 산출물 서식 — 투자수익 현황 | (4800, 20904) | `3148:2` | **`3161:2`** | 1440×785 |
| 14 엑셀 산출물 서식 — 일별 투자수익 | (0, 23517) | `3150:2` | **`3162:2`** | 1440×869 |

청크 편성 — A: `index` · `certificate` · `invest-profit` / B: `invest-profit--monthly` · `invest-profit--empty` · `xls-assets-status` / C: `xls-assets-merchant` · `xls-profit-status` · `xls-profit-daily`.

구 노드 삭제는 신 프레임 9건의 렌더·텍스트 검수를 마친 뒤에 했다.

---

## §3 검수 판정

### 3-1. 방법

| 축 | 수단 |
|---|---|
| 크기·좌표·이름·클리핑 | `use_figma` 읽기 전용으로 페이지 직계 32건 전수 반환 |
| 렌더 | `get_screenshot` 9건 전건(URL+curl, 원본 해상도) |
| 원본 대조 | 스테이징 사본을 프레임과 같은 높이로 헤드리스 렌더 → 480px 정규화 후 픽셀 차 |
| 텍스트 | 프레임 텍스트 노드 전량 대 원본 `innerText` 양방향 포함 검사 |
| 잘림 | 텍스트 노드 절대좌표 대 `clipsContent` 조상 경계 비교 |

픽셀 차는 두 문턱으로 읽는다. **텍스트차**(채널차 32 초과)는 Figma와 Chrome의 글리프 힌팅 차이를, **구조차**(110 초과)는 배치 어긋남·잘림을 잡는다.

### 3-2. 프레임별 판정

| 프레임 | 노드 | 크기 | 텍스트차% | 구조차% | 텍스트 대조 | 판정 |
|---|---|---|---|---|---|---|
| 02 투자자산 증명서 | `3156:2` | 1440×1356 | 2.59 | 0.60 | 양방향 0 | 양호 |
| 03 투자 수익 | `3154:2` | 1440×1458 | 1.66 | 0.15 | 의사요소 4 외 0 | 양호 |
| 03-a 투자 수익 — 월별 | `3157:2` | 1440×1413 | 0.99 | 0.05 | 의사요소 4 · 대문자 변환 1 외 0 | 양호 |
| 03-b 투자 수익 — 결과 없음 | `3158:2` | 1440×1271 | 0.59 | 0.04 | 의사요소 4 외 0 | 양호 |
| 09 화면 갤러리 | `3155:2` | 1440×1739 | 2.77 | 0.58 | 접힘 `details` 43 외 0 | 양호 |
| 11 엑셀 산출물 서식 — 투자자산 현황 | `3159:2` | 1440×729 | 2.21 | 0.21 | 양방향 0 | 양호(보수 후) |
| 12 엑셀 산출물 서식 — 가맹점별 투자자산 | `3160:2` | 1440×1121 | 2.55 | 0.16 | 양방향 0 | 양호(보수 후) |
| 13 엑셀 산출물 서식 — 투자수익 현황 | `3161:2` | 1440×785 | 1.98 | 0.18 | 양방향 0 | 양호(보수 후) |
| 14 엑셀 산출물 서식 — 일별 투자수익 | `3162:2` | 1440×869 | 2.42 | 0.18 | 양방향 0 | 양호(보수 후) |

**분포: 양호 9 / 보수 필요 0.** 보수 1건(§5-2)은 이번 라운드 안에서 해소했고, 그 결과를 반영한 수치다.

텍스트 대조에서 걸린 항목은 전부 계측 성격이며 내용 차가 아니다.

- **의사요소 4** — 용어 안내 `dt`의 `::after { content: " —" }`. Figma는 `금융일수 —`로, `innerText`는 `금융일수`로 읽는다.
- **대문자 변환 1** — 월별 표 머리 `TY수익율`. 원본 문자열은 `Ty수익율`이고 `text-transform: uppercase`가 걸려 있다. `innerText`는 변환 후를, Figma는 원문자를 담는다.
- **접힘 `details` 43** — `index.html`의 `<details class="state-fold">` 안 상태 목록. 접힌 상태라 `innerText`가 제외하고, Figma는 높이 0인 조상 프레임 안에 클립된 채로 담는다. 렌더 결과에는 나타나지 않는다(§4-3).

### 3-3. 항목별 전수 확인 — 9프레임

| 항목 | 결과 |
|---|---|
| 크기 | 9/9 폭 1440 · 높이 = §1-5 실측값. 자식 경계 상자 `0 ≤ x`, `x+w ≤ 1440`, `y+h ≤ 프레임 h` — **경계 이탈 0건** |
| 좌표 | 9/9 구 노드 좌표 승계. 겹침·음수 0건 |
| 이름 | 9/9 구 노드 이름 승계 |
| `clipsContent` | 9/9 `true` |
| 워드마크 | 사이드바 보유 8프레임 전건 `PayHug Admin`, `PayHug ` 흰색 `255,255,255` · `Admin` **`127,225,65`**(브랜드 그린). `09 화면 갤러리`는 랜딩 지면이라 사이드바·워드마크가 없다 |
| `PayHug Investor` | **0건** |
| `value` 필드 | 날짜 6/6 생존 — `2026. 08. 21.` · `2026. 08. 27.` · `2026. 08. 01.` · `2026. 08. 27.` · `2026. 02. 01.` · `2026. 02. 07.` |
| **상태 수 18** | `09 화면 갤러리` 2자리 전건 검출 — 본문 `화면 14개와 상태 18개`, 섹션 부기 `기본 화면 8 · 상태 18`. `상태 20`·`상태 19` **0건** |
| **예시값 고지** | `02 투자자산 증명서` 3자리 — 지면 배너 1 · 문서 안 고지 1 · 발급 안내 `표기 금액·상호·서명값은 전부 예시이며…` 1 |
| **엑셀 시트 고지 행** | `xls-*` 4프레임 전건 2자리 — 지면 배너 1 · 시트 2행 셀 1 |
| **투자 수익 집계 고지** | `03` `※ 조회 기간에 걸친 일자만 담는다…` 1 · `03-a` `※ 월 행은 그 달 가운데…` 1 |
| **월별 표 값** | `03-a` 6행(`2026-03`~`2026-08`) + 합계 행 전건 원본과 일치. 합계 `32,132,807,250` · `32,097,500,000` · `35,307,250` · `11.3(평균)` · `3.54%(평균)` |
| **기간 이동** | `03-b` 3자리 전건 — 시작일 `2026. 02. 01.` · 종료일 `2026. 02. 07.` · 카드 기간 `2026-02-01 ~ 2026-02-07` |
| 잘림 | **0건**(§5-2 보수 후). 사이드바 메뉴 7항목·그룹 3개가 사이드바 보유 8프레임 전건에 온전히 담김 |
| 폰트 대체 붕괴 | 0건. 표 열 폭·카드 폭 실측 오차는 480px 정규화 기준 육안 무차 |

### 3-4. 판정에 반영하지 않은 미세 차

| 대상 | 내용 |
|---|---|
| 날짜 필드 달력 아이콘 | 전처리에서 인라인 SVG로 대체한 결과라 Chrome 기본 글리프와 획 굵기가 다르다. 위치·크기 동일 |
| 전 프레임 세로 오프셋 | Figma 렌더가 Chrome 렌더보다 4~5px 위에서 시작한다. 구조차 수치의 대부분이 여기서 나온다 |
| `09 화면 갤러리` 본문 줄바꿈 | Figma 텍스트 계측이 Chrome보다 좁게 잡혀 `화면 14개와 상 / 태 18개가`로 끊긴다. 문자열은 동일 |
| `02 투자자산 증명서` 발급 안내 줄바꿈 | 같은 계측 차. 문구·색 동일 |

---

## §4 전수 최종 점검 — 32프레임

### 4-1. 로스터

| 항목 | 결과 |
|---|---|
| 프레임 수 | **32** — `figma_map_investor.json` 32건과 일치 |
| 이름 | 32/32 매핑과 일치. 중복 0건 |
| 좌표 | 32/32 매핑과 일치. 열 `x = 0 / 1600 / 3200 / 4800`, 행 피치 2613 |
| 크기 | 32/32 폭 1440 · 높이 = 매핑값(533~1739) |
| `clipsContent` | 32/32 `true` |
| 텍스트 노드 총계 | 2,454 |
| 페이지 점유 범위 | x 0~6240 · y 0~24386 |

### 4-2. 잔존 문자열

| 문자열 | 잔존 | 비고 |
|---|---|---|
| `PayHug Investor` | **0** | |
| `상태 20` | **0** | |
| `상태 19` | **0** | |
| `4,210,000` | **0** | |
| `3.31%` | **0** | |
| `0.336` · `92.42` · `33,700,000` | **0** | `gate_fix.md` §3 구값 목록 |
| 프로필 뱃지 `투자자` | **0** | 전 페이지 정확 일치 텍스트 노드 2건은 `02 투자자산 증명서`의 증명서 본문 항목 라벨(`투자자` / `㈜테스트인베스트`)이며 사이드바 뱃지가 아니다 |
| 프로필 `님` | **0** | 부분 일치 8건은 `단순평균 아님` · `진입점 아님`의 `아님`이다 |
| 구 노드 `3123:2` `3126:2` `3131:2` `3136:2` `3142:2` `3147:2` `3148:2` `3149:2` `3150:2` | **0** | |
| 1차 라운드 구 노드 `3068:2`~`3075:2` 8건 | **0** | |

### 4-3. `3.32%` — 구값 아님

지시서의 구값 목록에 `3.32%`가 들어 있으나, 이 값은 원본 `6e4c270`의 **현행 값**이다.

| 자리 | 원본 |
|---|---|
| `certificate.html` | 가맹점별 표 `성호분식 2호점` 행의 `Ty수익율` |
| `xls-assets-merchant.html` | 같은 데이터의 시트 6행 |
| `invest-assets.html` · `--download` · `--cert-confirm` | 같은 데이터의 화면 표 |

Figma 검출 5프레임은 위 원본 5자리와 1:1로 대응한다. `gate_fix.md` §3이 정리한 구값은 `0.336` · `92.42` · `3.31%` · `33,700,000` · `3배`이고 그 대상은 `capability.html`(미임포트)이다. 프레임 잔존 판정은 **정상**이다.

### 4-4. `09 화면 갤러리`의 접힘 목록

`index.html`의 `<details class="state-fold">` 9개는 접힌 상태로 캡처됐다. Figma에는 높이 0인 `Details` 프레임 안에 상태 목록 텍스트 36노드가 클립된 채 들어 있다.

- 프레임 높이 1739 = 접힌 상태의 원본 문서 높이와 일치
- 클립 판정에서 걸린 36건은 전부 `overBottom > 0` · `overRight < 0` — 세로로만 잘려 있고 가로로는 여유가 있다. 접힘을 재현하는 기전 그대로다
- 구조차 0.58%가 지면 전역에 고르게 퍼져 있어 국소적으로 펼쳐진 자리가 없다

---

## §5 실패·재시도

### 5-1. 임포트 실행

| 라운드 | 대상 | 캡처 | 임포트 | 실패 |
|---|---|---|---|---|
| 청크 A | `index` · `certificate` · `invest-profit` | 3 | 3 | 0 |
| 청크 B | `invest-profit--monthly` · `invest-profit--empty` · `xls-assets-status` | 3 | 3 | 0 |
| 청크 C | `xls-assets-merchant` · `xls-profit-status` · `xls-profit-daily` | 3 | 3 | 0 |
| **누계** | | **9** | **9** | **0** |

captureId 한도초과 0건, 409 재사용 0건, 폴링 재발급 0건, 재캡처 0건.

### 5-2. 보수 1건 — `.xlsx` 파일명 끝 글자 잘림

`xls-*` 4프레임에서 파일명 텍스트가 조상 컨테이너 경계를 넘어 마지막 `x` 글자가 잘렸다.

원인은 폰트 대체다. 원본 `.fb-name`은 `font-family: var(--font-mono)`이고 Chrome이 고르는 face 기준으로 컨테이너 폭이 잡혔는데, Figma는 **Menlo Medium**으로 대체하면서 같은 문자열이 더 넓어졌다. `.fb-name`에 `overflow: hidden`이 걸려 있어 초과분이 그대로 잘렸다.

| 프레임 | 텍스트 노드 | 클리핑 컨테이너 | 텍스트 폭 | 종전 컨테이너 폭 | 초과 | 보정 폭 |
|---|---|---|---|---|---|---|
| `3159:2` | `3159:32` | `3159:31` | 305 | 300.25 | 4.75 | **306** |
| `3160:2` | `3160:32` | `3160:31` | 331 | 324.47 | 6.53 | **332** |
| `3161:2` | `3161:32` | `3161:31` | 305 | 300.25 | 4.75 | **306** |
| `3162:2` | `3162:32` | `3162:31` | 305 | 300.25 | 4.75 | **306** |

컨테이너를 `ceil(텍스트 폭) + 1`로 넓혔다. 세로 오토레이아웃 안의 자식이라 형제 위치가 밀리지 않고, 프레임 폭 1440도 그대로다. 보수 후 4프레임 전건 클립 텍스트 0건이며, 파일명이 `.xlsx`까지 전부 보인다.

재임포트로는 같은 결과가 되풀이되므로 폭 보정으로 처리했다. 텍스트 내용은 손대지 않았다.

### 5-3. 중간 시행착오 1건 — 날짜 필드 폭 계측

§1-3 참조. 갱신 직후 사본을 계측해 얻은 `152`·`153`으로 한 차례 구웠다가, 원본 직접 계측값 `148`과 어긋나는 것을 픽셀 대조로 확인하고 되돌렸다. 임포트 전에 잡아 캡처 재실행은 없었다.

---

## §6 미해소

| # | 항목 | 상태 |
|---|---|---|
| G-1 | **`xls-*` 화면 제목 라벨 불일치** | 프레임 이름은 `엑셀 산출물 서식 —`이나 화면 안 `h1`은 `엑셀 미리보기 —` 그대로다. `behavior_fix.md` §3이 갤러리 표기만 바꾼 결과이며 이번 라운드에서도 원본이 그대로라 프레임에도 그대로 담긴다. 종전 `figma_import_result.md` F-2 승계 |
| G-2 | **계약기록 zip 파일명 규칙 미적용** | `07-b`(`3144:2`)의 `재양도합의서_전체16건_20260827.zip`은 구 규칙이다. 엑셀 4종만 `{내용}_{YYYY-MM-DD}_{YYYY-MM-DD}`로 바뀌었다. 종전 F-3 승계 |
| G-3 | **`invest-profit--datepicker` 미임포트** | 폐기 결정 화면. `glossary.html` 링크가 사라지면 파일도 삭제 대상. 종전 F-4 승계 |
| G-4 | **`app.html` 미임포트** | JS 표시 전환 구조라 DOM 캡처가 초기 화면 1개만 담는다. 종전 F-5 승계 |
| G-5 | **요율 C1 미확정** | 프레임 안의 `0.11%`는 `예시값` 표기 그대로다. 이번 라운드가 더한 예시값 고지가 그 사실을 지면에 명시한다. 확정 전까지 투자수익·상환액·투자실행금 열은 확정값이 아니다. 종전 F-6 승계 |
| G-6 | **`figcap.sh` 원본 미보정** | 87px 편차 보정은 `figcap_ia.sh`에만 들어 있다. 화면설계서용 `figcap.sh`는 그대로다. 종전 F-7 승계 |
| **G-7** | **모노 폰트 대체 폭 초과** | Figma가 `--font-mono`를 Menlo로 대체하면서 같은 문자열이 Chrome보다 넓어진다. §5-2에서 4자리를 보수했으나 근본은 폰트 대체다. 모노 텍스트를 고정폭 컨테이너에 넣는 자리가 늘면 재발한다 |
| **G-8** | **텍스트 계측 차로 인한 줄바꿈 위치** | `09 화면 갤러리` 본문 1자리, `02 투자자산 증명서` 발급 안내 1자리에서 Figma 줄바꿈이 Chrome보다 한 어절 이르다. 문자열·색·폭은 동일하며 잘림은 없다 |
| **G-9** | **`03-a` 월별 카드 기간 기준 미결** | `gate_fix.md` §9-2가 남긴 판단 대기 항목이다. 프레임은 현행 원본(조회 기간만큼 잘라 합산)을 그대로 담고 있으므로, 판단이 뒤집히면 `03`·`03-a` 재임포트가 다시 필요하다 |

---

## 산출·수정 파일

| 절대경로 | 성격 |
|---|---|
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/figma_reimport.md` | 이 문서 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/figma_map_investor.json` | 화면 → 노드ID 매핑 32건. 9건을 신규 노드ID·신규 높이로 갱신, `action = 재임포트 교체`, `replaced_node_id = 구 노드` |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/_fig/` | 스테이징 사본 32 HTML + `assets/`. 9파일을 `6e4c270` 시점으로 갱신 후 재전처리 |

`prep_fig.py` · `figcap_ia.sh`는 손대지 않았다. 원본 레포 `/Users/semi/cursor/payhug-investor-admin`은 읽기만 했다 — HEAD `6e4c270` 불변 · 워킹트리 변경 0건 · 커밋 0건 · push 0건.
