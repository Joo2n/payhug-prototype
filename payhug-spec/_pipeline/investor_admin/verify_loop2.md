# 투자자 어드민 — 2차 검증 루프

기준 문서: `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/request_register.md`
1차 산출물: `verify_loop1.md` (29항목 · 미충족 G-1·G-6 · 부분 12)
판정 범위: D-1~D-10 · R-1~R-18 · S · X-1 · **게이트 G-1~G-9 전수**
판정 방식: 파일 열람·문자열 카운트·HTTP 응답·노드 조회·헤드리스 실행으로 실측한 것만 판정한다. 열어보지 않은 것은 `확인불가`.

---

## §0 판독 시각

| 대상 | 판독 시각 (KST) | 상태 |
|---|---|---|
| `request_register.md` · `verify_loop1.md` | 20:35 | 전문 |
| `/Users/semi/cursor/payhug-investor-admin/` | 20:35 · 20:41 · 20:48 · 20:54 | `HEAD` = `5ccfa3f`, 루트 HTML **39개** 고정 |
| 문체·근거·개념 문자열 스캔 | 20:38~20:47 | 레포 HTML 39 + `assets/` 4 + 파이프라인 md 27 + html 2 |
| Figma `Tcf69tIciGxmlqCIuRb0iI` node `3066:328` | 20:35~20:45 | `get_metadata`·`get_screenshot` 읽기만, **쓰기 0건** |
| 라이브 `joo2n.github.io/payhug-investor-admin/` | 20:36~20:47 | 커밋 `5ccfa3f` 시점 구판 |
| 라이브 `payhug-investor-demo.vercel.app` | 20:36~20:47 | 스텁 |
| xlsx 4 · PDF 3 실물 파싱 | 20:37~20:52 | `openpyxl 3.1.5` · `PyMuPDF` |
| `app.html` 헤드리스 재현 | 20:48~20:53 | Chrome `--headless=new --dump-dom`, 로컬 8811 |
| 파이프라인 산출물 64건 | 20:35~20:54 | 전건 목록 대조 |

**판독 중 레포 변동 0건.** `HEAD` 불변, 워킹트리 파일 수·크기 무변동. 이 문서 작성 과정에서 수정·생성한 파일은 이 문서 1개다.

---

## §1 항목별 판정표

**분포 — 총 39항목**

| 판정 | 수 | 항목 |
|---|---|---|
| 충족 | 21 | D-1 D-2 D-4 D-5 D-6 D-7 · R-1 R-2 R-3 R-4 R-6 R-8 R-10 R-12 R-14 R-17 R-18 · G-3 G-5 G-6 G-8 |
| 부분 | 15 | D-3 D-8 D-9 D-10 · R-7 R-9 R-11 R-15 R-16 · S · G-1 G-2 G-4 G-7 G-9 |
| 미충족 | 1 | R-5 |
| 해당없음 | 1 | R-13 |
| 해소 | 1 | X-1 |

1차 대비 이동 — 미충족 2(G-1·G-6) → G-6 `충족`, G-1 `부분`. 진행중 2(R-10·R-14) → 전건 `충족`. 부분 12 중 5건(D-4 R-4 R-7의 Figma축 R-15 R-18) 상향, 7건 잔류.

### D. 확정 결정

| ID | 판정 | 근거 (실측) | 미충족분 |
|---|---|---|---|
| D-1 산식 2모델 병기 | **충족** | `수수료 배분형`(배분 요율 `0.11%`, VAT 면제) · `조달이자형`(조달 이자율 `연 12%`, ÷365) 2카드가 라벨 구분돼 병기. HTML **11파일**(`invest-assets*` 5 · `invest-profit*` 4 · `app.html` · `inquiry.html`), Figma **8프레임**(`3120:2`·`3125:2`·`3128:2`·`3129:2`·`3133:2`·`3126:2`·`3136:2`·`3131:2`) | 조달이자형 카드가 화면의 어느 표와도 연결되지 않는다(현재 데이터는 배분형 0.11%로만 정합). `inquiry.html` Q-6 `조달이자형 카드를 계속 둡니까`로 등재돼 있어 임의 결정은 없다 |
| D-2 성공 경로만 | **충족** | 실패 경로 화면 **0건**. `실패` 문자열은 `components.html` 견본 갤러리·`capability.html`·`glossary.html`·`inquiry.html`의 서술뿐. `login--error`·서명검증 실패·회신전문 검증 실패 전부 0건. `password--error`/`--weak`는 S 계획 등재분(폼 입력 검증)이라 D-2가 지목한 6곳과 층위가 다르다 | — |
| D-3 사이드바 = 어드민 안의 한 뷰 | **부분** | 되는 것 — 스토리보드 7메뉴 순서 유지, 어드민 구조 패턴(`nav-group`·`nav-group-label`·`nav-item`·`sidebar-profile`) 채택, 워드마크 `PayHug <em>Admin</em>`(`assets/template.html:26`), 프로필 `㈜테스트인베스트`(`님`·뱃지 제거) | 안 되는 것 — 어드민 실메뉴 12개(`홈`/가맹점 3/정산 3/관리자 6, `AdminLayout.tsx:29~144`) 잔존 **0건**. 1차 루프 3-3의 `붙인 게 아니라 갈아 끼웠다` 지적이 그대로다. `role-view가 투자자 메뉴만 남기는 동작`이라는 해석이면 현행이 맞으나 그 해석이 확정되지 않았다 |
| D-4 근거 = 스토리보드 단독 | **충족** | `storyboard_coverage.md:14` `플로우차트_투자자 UI_V1.3.pptx는 근거에서 제외됨 — 열람하지 않음`. 스토리보드 인용 표기 `SB s*` **126건**(`capability.html`). 플로우차트 인용 0건(G-6 상세 참조) | — |
| D-5 디자인시스템 = 실제 어드민 | **충족** | `DESIGN_REF.md:3` 실측 소스 `payhug-admin-web @ f79997b` 명기. 색 토큰 66종(`--primary-*` 9 · `--gray-*` · `--navy*` · `--sidebar-bg`), 폰트 `Noto Sans KR` 4파일, 사이드바 240px·`#1B2537` 전부 `AdminLayout.tsx` 라인 근거 병기. `ExcelDownload` 3파일 · `ReconTerm` 1파일 참조 | — |
| D-6 Figma 타깃 `3066:328` | **충족** | 페이지 `[투자자 어드민] 제안서 첨부용` 직계 프레임 **32개** 실측. 비-frame 최상위 노드 0건 | — |
| D-7 PPT 밖 추가 화면 폐기 | **충족** | `new_screens_manuscript.md` 14건은 원고 단계로만 존재. 대응 구현 파일 **0건** — `login--error`·`login--expired`·`login--denied`·`activity`·`notice`·`inquiry-user` 계열 루트 HTML 0건. 오히려 `merchants--filter-open.html`은 근거 소멸로 삭제됨(`behavior_fix.md` §6) | — |
| D-8 출처 3원칙 | **부분** | 되는 것 — 1차 루프 3-7 적발 2건 처리 완료. `하나은행 인증서` 서명 문구는 화면·PDF에서 **0건**(잔존은 `capability.html:772,1428`의 판정 서술과 `inquiry.html`의 락계좌 실측 서술뿐). 쿠콘 리다이렉트 `https://www.we-bank.co.kr/main_00100.act`는 `SB s10` 삽입 이미지 주소창에 축자로 있음이 `capability.html:873`에 등재돼 출처 A 확보 | 안 되는 것 — `capability.html`이 Figma 정산채권DB(`529:2950`)·계약 아카이브(`archive_02_계약약관.md`)·정책서(`02_TERMS_AND_STATUS.md`)를 근거로 쓰는 자리가 있다. 정책서 원천이라 창작은 아니나 A/B/C 3원칙 문언 밖이다 — 원천 등급 확정 필요 |
| D-9 워드마크 `PayHug Admin` | **부분** | 되는 것 — 로컬 `PayHug Investor` **0건**, `PayHug Admin` 34건. Figma도 `Investor` **0건** / `Admin` **31프레임**(랜딩 제외 전건) | 안 되는 것 — **라이브 Pages 8화면이 `PayHug Investor`를 공개 중.** 폐기 결정된 워드마크가 대외 노출면에 그대로 있다. 해소 조건은 push 1회(P-4) |
| D-10 대표 정의서 최상위 | **부분** | 적용된 것 — ⑤ `투자자산 대비 Ty수익율` = **2.24%**(대표 정의 `(④×PSA)÷(PSA+PSC)`, PSC=기간 EC 합. 검산 `3.5543%×1,250,800,000÷1,987,900,000 = 2.2364%`). `입금일자`→`정산예정일`(CI-07), `입금월`→`정산예정월`(CI-08), ⑥ 일별 Ty 7행 전건 일치, 순지급액 용어 등재(`glossary.html` 83건) | 미적용 5건 — CI-11 투자수익 `−max(0, 미지급금−과지급금)` 차감항 부재 · CI-12 상환액이 나눗셈(`÷(1−r)`)이 아닌 곱셈(`×(1+r)`) · CI-15 S입금부족율 각주 분모 `선정산금액` 그대로(`invest-assets.html:373`) · CI-18 산식 카드 기준금액 라벨 `선정산 거래액` 11건 · CI-19 가맹점별 `비중` 축 근거. **5건 전부 `inquiry.html` Q-2·Q-4·Q-5 및 부록 N-9로 등재돼 임의 결정은 0건** |

### R. 요청 항목

| ID | 판정 | 근거 (실측) | 미충족분 |
|---|---|---|---|
| R-1 어드민 기준 UI 제작 | **충족** | 화면 32(기본 13 + 상태 19) + 통합본 1. 디자인 토큰·레이아웃 전건 `payhug-admin-web` 실측 근거 | — |
| R-2 계산식 노출·엑셀·재설계 | **충족** | 산식 카드 2종 11파일 · 용어 안내 각주 4줄 12화면 · 엑셀 4종 실물 배선 | — |
| R-3 쿠콘 = 링크 | **충족** | `coocon.html:122` `<a href="https://www.we-bank.co.kr/…" target="_blank" rel="noopener">`. 확인 모달 경유 후 외부 이동 | — |
| R-4 Figma 제작 | **충족** | `3066:328` 32프레임. 본화면 8 · 상태 18 · xls 4 · 랜딩 1 · 로그인 1. 로컬↔Figma 1:1 대응 32건, Figma 잉여 0건 | — |
| R-5 깃 배포 | **미충족** | 라이브 Pages는 `origin/main` 15파일만 응답. 루트 HTML 39 중 **30건이 404**(`app` `capability` `glossary` `archive` `inquiry` `review` `login` · 상태 19 · `xls-*` 4). `assets/sheet.css`·`assets/xlsx/` 4·`assets/docs/` 22 전건 404. 미푸시 = 수정 14파일(1,265행) + 미추적 57파일(**14.2 MB**) | 커밋 0건. P-4(사용자가 `다 고치고 한 번에`로 결정)에 묶여 있다 |
| R-6 용어 정리·복사 가능 | **충족** | `inquiry.html` 대표 문항 **Q-1~Q-7** + 부록 6절. 복사 버튼 — 전문 1 · 부록 1 · 문항별 7 · 개발 항목별. `<pre class="src" id="src-q*">` 7건이 평문 원천 | — |
| R-7 버튼 동작·상태를 Figma에도 | **부분** | 되는 것 — 1차 루프의 `Figma 상태 0건`이 해소됐다. 상태·모달·토스트 프레임 **18개** 임포트 완료(`01-a`~`08-c`) + 로그인 1 + xls 4 | 안 되는 것 — `app.html` 미임포트(JS 표시 전환이라 DOM 캡처가 초기 화면만 담음, `figma_import_result.md` F-5). 달력 프레임 0건(`invest-profit--datepicker` 폐기 결정, F-4) |
| R-8 엑셀 실물 연결 | **충족** | `assets/xlsx/` 4개 실물(5.5~6.6 KB, `openpyxl` 파싱 성공)에 화면 다운로드 버튼 직결. 배선 16곳 + `app.html` `download` 속성 31건. `xls-*.html` 4종이 서식 미리보기 | — |
| R-9 파일 경로·이름·설명 표 | **부분** | 되는 것 — `archive.html`이 루트 HTML **39/39**, 파이프라인 산출물 **64/64**, `assets/xlsx` 4/4, `assets/docs` 22/22 전건 등재. 1차 루프 3-6이 지적한 누락 4건(`glossary.html`·`build_app.py`·`build_index.py`·`new_screens_manuscript.md`) 해소 | 안 되는 것 — 링크 136개가 전부 `http://localhost:8901`(72) / `8902`(64). **20:52 실측 응답 72 / 실패 64.** 8902가 죽어 파이프라인 문서 링크가 전건 무응답이고, push 후 Pages에서도 localhost는 열리지 않는다 |
| R-10 통합 프로토타입 1개 | **충족** | `app.html` 149,873 B. 화면 섹션 14개(`section.screen[data-screen]`) 단일 파일. 사이드바 7메뉴 전환 · 해시 딥링크 `#화면/상태`. **헤드리스 직접 재현으로 확인**(§2 G-8) | — |
| R-11 용어 설명 HTML 게시 | **부분** | 되는 것 — `glossary.html` 273,331 B, 용어 카드 **50건**, 7단계 학습 구조, 상호참조 앵커, 예시 계산 로그 | 안 되는 것 — 라이브 **404**. 웹에서 열리지 않는다(R-5와 동일 원인) |
| R-12 투자자 뷰·기능 작성 | **충족** | `capability.html` 165,292 B. 행위 **35건**(`a-no`) · 데이터 노출 범위 · 개발 확인 질문 23건 · 부록 C(플로우차트 제외로 강등된 항목) | — |
| R-13 PPT 외 기능·화면 추가 | **해당없음** | **D-7이 상위 결정으로 이 트랙을 폐기했다.** 사용자 원문 `ppt에서 이런거 요청한 적도 없고 … ai 생성 자체판단 삭제해`. `new_screens_manuscript.md`는 발굴 기록으로만 보존 |
| R-14 통합본 = 버튼이 연결되는 하나 | **충족** | `href="#"` 전 레포 **2건**(`app.html:404`·`assets/template.html:24`, 둘 다 사이드바 로고). 1차 루프 기준선 67~70건에서 소멸. `data-act` 32종 전건 핸들러 도달(§2 G-8) | — |
| R-15 억지로 지어내지 않음 | **부분** | 되는 것 — 1차 3-7 적발 2건 처리(D-8 참조). `fabrication_audit.md` 131건 중 임의생성 8건 정정, 오분류 3건 복원 | 안 되는 것 — 집계 숫자 `상태 20`이 6개 파일에 근거 없이 남아 있다(실측 18~19). 정렬 컨트롤 부재는 여전(`sort` 핸들러는 있으나 스토리보드에 정렬 규정 없음) |
| R-16 AI 말투 제거 | **부분** | 되는 것 — `가 핵심`·`중요합니다`·`확인해 주세요`·`드립니다` 전 레포 **0건**. 종결어미 위반 실질 0건 | 안 되는 것 — `review.html` 4곳이 수행보고체(§2 G-1) |
| R-17 검증 루프 상시화 | **충족** | `request_register.md` L절 절차 + `verify_loop1.md` + 이 문서 | — |
| R-18 루프 2회 | **충족** | 1차 20:35 판독분 · 2차 이 문서 | — |

### S. 상태 화면 25종 계획

**판정: 부분**

| 묶음 | 계획 | 실물 | 차이 |
|---|---|---|---|
| 엑셀 결과 화면 | 4 | **4** | `xls-assets-status` · `xls-assets-merchant` · `xls-profit-status` · `xls-profit-daily`. X-1 해소 |
| 투자자산·투자수익 | 7 | **7** | `--page2` `--download` `--cert-confirm` `--empty` `--monthly` `--datepicker` `--empty` |
| 가맹점·양수·쿠콘 | 7 | **6** | `--filtered` `--empty` · `--confirm` `--signing` `--done` · `coocon--confirm`. **`merchants--filter-open` 삭제** — 실제 어드민에 커스텀 드롭다운 0건이라 네이티브 `<select>`로 되돌리면서 존치 근거 소멸(`behavior_fix.md` §6). `선택 없음`은 `acquisition.html` 기본 상태가 겸함 |
| 계약기록·비밀번호·로그인 | 7 | **7** | `--all` `--downloaded` `--empty` · `--error` `--weak` `--done` · `login.html` |
| **합계** | **25** | **24** | 파일 기준 상태 19 + 기본 겸용 1 + 엑셀 4 |

`invest-profit--datepicker.html`은 같은 이유로 **폐기 대상**이나 `glossary.html` 링크 때문에 파일만 남아 있다(`behavior_fix.md` U-5). Figma에는 미임포트. 유효 상태 = **18**.

계획 `9 base + 25 = 34` → 현행 `기본 13 + 상태 19 + 문서·통합 7 = 39`. 모수가 바뀌었으므로 34와의 일치는 더 이상 검증 축이 아니다.

### X. 누락

| ID | 판정 | 근거 |
|---|---|---|
| X-1 엑셀 결과 화면 4종 중 3종 | **해소** | `xls-profit-status.html` 존재(20:35 실측). `투자수익현황_2026-08-21_2026-08-27.xlsx`에 직결. Figma 프레임 `13 엑셀 산출물 서식 — 투자수익 현황`(`3148:2`)도 존재 |

---

## §2 게이트 G-1~G-9 전수 결과

### G-1 문체 — **부분**

검사 범위: 레포 HTML 39 + `assets/` 4 + 파이프라인 md 27 + html 2 = **72파일**, 문자열 카운트.
예외: `inquiry.html`·`ceo_inquiry.md`(대표님께 보내는 글) · 원문 인용 · 대표 정의서 원문 · UI 카피.

**종결어미 `입니다`·`습니다`**

| 파일 | 입니다 | 습니다 | 성격 | 판정 |
|---|---|---|---|---|
| `inquiry.html` | 15 | 46 | 대표님 문의서 본문 | **예외** |
| `ceo_inquiry.md` | 7 | 23 | 동일 | **예외** |
| `assets/components.html` | 4 | 3 | UI 견본 문구(토스트 `저장이 완료되었습니다.` · 용어 `…금액입니다.`) | **예외 — 화면 카피** |
| `behavior_parity.md` | 0 | 1 | 어드민 원문 인용 `조회 결과가 없습니다.` | **예외 — 인용** |
| `fabrication_audit.md` | 0 | 1 | 동일 | **예외 — 인용** |
| `new_screens_manuscript.md` | 1 | 1 | `app/login/page.tsx:70-76` 토스트 문구 인용 | **예외 — 인용** |
| `ui_fix_result.md` | 1 | 1 | 게이트 기준 문장 자체를 서술 | **예외 — 메타** |
| `request_register.md` · `verify_loop1.md` | 3 | 3 | 사용자 원문 인용 + 게이트 문언 | **예외** |
| **그 밖 63파일** | **0** | **0** | | **통과** |

예외를 제외한 실질 위반 **0건**. 1차 루프 §4-3이 판단 보류로 남긴 4종은 위와 같이 갈랐다.

참고 — 게이트 문언 밖 형태 `습니까`/`입니까`는 `inquiry.html` 18 · `capability.html` 16(개발 확인 질문 본문, 복사해 보내는 글). 문의문이라 같은 예외 취급.

**금지어**

| 문자열 | 레포 | 파이프라인 | 실질 위반 |
|---|---|---|---|
| `요청하신` | 0 | 2 (`request_register.md`·`verify_loop1.md` 게이트 문언 자체) | **0** |
| `지시에 따라` | 0 | 2 (동일) | **0** |
| `말씀하신` | 0 | 2 (동일) | **0** |
| `반영` | 7 | 67 | **4** — 아래 |

`반영` 세부 — `glossary.html` 3건은 어드민 배지 설명(`플랫폼 환급이 아직 반영 안 된 상태`)으로 대상 개념 서술. `components.html` 1건은 UI 문구(`차액 반영 대기`). 파이프라인 63건은 내부 작업 문서의 상태 서술. **실질 위반은 `review.html` 2건 + 아래 수행보고체와 겹친다.**

**수행보고체·변경이력체 — `review.html` 4곳이 유일한 위반**

| 위치 | 문장 | 성격 |
|---|---|---|
| `review.html:94` | `앞서 '죽어 있다'고 본 것은 오진이었다 … 눌러보지 않고 DOM만 보고 단정했다. 진짜 결함은 … 그건 수정됐다.` | 자기 경위 서술 |
| `review.html:108` | `대표 정의 반영 완료` | 반영 어필 |
| `review.html:115` | `대표님 정의대로 적용했을 때` | 조건절 — 경계 |
| `review.html:136` | `색 팔레트가 … 정정 중. 대표 정의서 반영과 … 검증도 돌고 있다.` | 진행 보고 |

레포의 다른 38개 HTML은 `했다`·`추가했`·`수정했`·`교체했`·`변경 전` 전건 **0건**. `capability.html:935` `정정했다는 점에서`는 설계안 대 원본 대조 서술로 허용 범위. `password.html`·`app.html`의 `변경 후`는 UI 카피(`비밀번호 변경 후 기존 세션 처리는 확인 대상.`).

**이모지 — 실질 0건**

전 72파일 픽토그램 이모지 **1건**(`verify_loop1.md`의 `⚠`, 1차 루프 자기 문서). 그 밖은 전부 조판 기호다 — `→` 1,173 · `←` 59 · `↔` 58 · `✓` 46 · `✗` 33 · `↓` 12 · `★` 6. `✓`/`✗`는 판정 기호, `★`는 `template.html` 슬라이스 마커. **이모지로 세지 않는다.**

**판정 — 부분.** 종결어미·금지어·이모지 통과, `review.html` 수행보고체 4곳 잔존.

### G-2 근거 — **부분**

표본 추출 검사. **표본 크기 412행**(원고 11파일 × 각 40행 무작위, 40자 초과 실문장만, 시드 고정).

| 파일 | 표본 | 출처 표기 | 비율 | 전체 장문행 |
|---|---|---|---|---|
| `figma_import_result.md` | 40 | 27 | 67% | 132 |
| `capability_manuscript.md` | 40 | 23 | 57% | 312 |
| `ceo_impact.md` | 40 | 22 | 55% | 157 |
| `fabrication_fix.md` | 40 | 20 | 50% | 123 |
| `value_lineage.md` | 40 | 19 | 47% | 320 |
| `behavior_fix.md` | 40 | 14 | 35% | 128 |
| `new_screens_manuscript.md` | 40 | 11 | 27% | 253 |
| `glossary_manuscript.md` | 40 | 10 | 25% | 625 |
| `behavior_parity.md` | 40 | 10 | 25% | 182 |
| `marker_legend.md` | 12 | 3 | 25% | 12 |
| `ui_fix_result.md` | 40 | 7 | 17% | 155 |
| **합계** | **412** | **166** | **40%** | — |

행 단위 비율은 과소평가다 — 표 행·앞 문장 승계·자체 실측 서술이 출처 마커 없이 잡힌다. 그래서 **단정 종결(`~이다.` `~한다.` `~없다.` 등) + 출처 마커 부재** 조건으로 다시 걸렀다.

| 파일 | 단정문(출처 무) | 장문행 | 비율 |
|---|---|---|---|
| `glossary_manuscript.md` | **76** | 530 | 14% |
| `behavior_parity.md` | 5 | 181 | 3% |
| `new_screens_manuscript.md` | 4 | 237 | 2% |
| `ui_fix_result.md` | 4 | 152 | 3% |
| `behavior_fix.md` | **0** | 123 | 0% |

실제 위반 표본 — `ui_fix_result.md:24` `v4는 팔레트를 oklch 기준으로 다시 뽑아 무채색은 미세 이동, 채도 계열은 순색 방향으로 크게 이동한다` (Tailwind 외부 사실, 출처 없음). `glossary_manuscript.md`의 다수는 앞 문장에서 인용한 정의를 풀어 쓰는 해설문이라 위반으로 보기 어렵다.

**판정 — 부분.** 출처 없는 단정 0건은 아니다. 집중 대상은 `glossary_manuscript.md` 해설 블록과 `ui_fix_result.md` 외부 사실 서술.

### G-3 판정 — **충족**

| 표기 | 레포 HTML 건수 |
|---|---|
| `확정` | 97 |
| `가설` | 48 |
| `확인필요` | 75 |
| `확인 필요`(띄어쓰기) | 2 |

3표기 전건 존재. **임의 승격 0건** — 1차 루프가 지목한 부록 C.2 강등 4건을 교차 확인한 결과, `capability.html:1443`·`:1444`가 `정산채권 양수 #22 계약서 열람`·`#23 대상 선택`을 `확정 → 가설`로 표시한 상태 그대로 유지한다. 다른 문서에서 `확정`으로 되살아난 자리 0건. 레포 화면 HTML의 `확정` 문자열은 전부 산식 각주(`산식·요율은 예시이며, 개별 계약 조건에 따라 확정됨.`)로 판정 표기가 아니다.

대표 정의 미적용 5건(CI-11·12·15·18·19)도 전부 `확인필요` 또는 `inquiry.html` 문항으로 남아 있어 임의 확정이 없다.

### G-4 미확정 — **부분**

**C1 수수료율 — 값 채택 0건.** `0.11%`는 전 자리에서 예시로 표기된다. `할인율 0.11% 예정`의 **`예정` 단서 유지 확인**:

| 위치 | 표기 |
|---|---|
| `glossary.html:431` | `할인율 0.11%`+`예정` 태그 — `대표 정의 원문이 "0.11% 예정"으로 적은 값이다. 확정값이 아니다.` |
| `glossary.html:748`·`:1326`·`:1327`·`:1795`·`:2464`·`:3161`·`:3355` | `tag-plan` 클래스로 `예정` 병기, 원문 인용 `유동화투자자의 할인율 = 0.11% 예정` |
| `inquiry.html` Q-7 | `화면의 0.11% 표기를 예시에서 예정으로 올립니까` — **승격 여부를 사용자·대표 판단으로 넘김** |
| 화면 9종 | `수익 산정 기준 (예시)` 제목 + `산식·요율은 예시이며, 개별 계약 조건에 따라 확정됨.` 각주 |

**C2 지급 캘린더 — 값 채택 0건.** D+1/D+2 표기가 산출물 어디에도 없다.
**C4 예상 지급 차액 — 값 채택 0건.** 해당 라벨이 투자자 화면에 없다.

**예시값 표기 — 구멍 있음.** 39개 루트 HTML 중 `예시` 문구 보유 **15건**, 미보유 **24건**.

| 미보유 | 위험도 |
|---|---|
| `certificate.html` | **높음** — 작성자 `㈜페이허그`, 합계 `1,523,100,000`, 가맹점 16건 실명·금액, 서명값 `kd568w7…` 게시. 실제 증명서로 오인 가능 |
| `xls-assets-status` `xls-assets-merchant` `xls-profit-status` `xls-profit-daily` | **높음** — 숫자 밀도 최고 |
| `assets/xlsx/` 4개 `.xlsx` 실물 | **높음** — 화면을 떠나 단독 유통되는 파일인데 `예시` 0건(sharedStrings·sheet 양쪽 스캔) |
| `merchants*` 3 · `acquisition*` 4 · `contracts*` 4 · `coocon*` 2 · `password*` 4 · `login` | 중 |

PDF는 반대로 충족 — 증명서 `지표 값의 성격 — … 예시값이다` 1건 + `견본` 8건, 계약서 `견본` 17건, 재양도합의서 `견본` 23건 + `계약 효력 없음` 3건.

**판정 — 부분.** C1·C2·C4 채택 0건과 `예정` 단서 유지는 통과. 예시 고지가 엑셀 실물 4건·엑셀 미리보기 4화면·증명서 화면에 없다.

### G-5 개념 분리 — **충족**

6대 개념 레포 HTML 출현 — `미지급금` 63 · `선정산 제외금액` 2 · `바로이체` 4 · `과지급` 73 · `미회수금` 7 · `환급` 7.

**병합 0건.** 대표 정의의 `미지급금`·`과지급금`이 정책서 6대 개념과 다른 개념으로 격리돼 있음을 명문화한 자리:

- `glossary.html:2038` — `주의 — 페이허그 정책서의 미지급금과 같은 낱말인데 방향이 반대로 읽힌다. … 6대 개념(미지급금 · 선정산 제외금액 · 바로이체 · 과지급 · 미회수금 · 환급)과 다른 개념이므로 근거 없이 합칠 수 없다.`
- `glossary.html:3157` — 3열 대조표에서 `정책서 6대 개념` / `대표 정의` / `어드민 배지`를 나란히 두고 `방향과 주체가 셋 다 다르다. 합치면 상환액이 반대로 계산된다`로 결론.
- `glossary.html:3160` — `미회수`의 세 갈래(6대 개념 `미회수금` / 어드민 `carryForwardTotal` / 배지)를 `셋 다 실재하고 셋 다 다른 값`으로 분리.
- `glossary.html:2036` — 대표 정의 `미지급금` 카드에 `확인필요` 표기. 정의문이 원문에 없다는 사실을 명시.

1차 루프 §4-2가 지목한 `입금부족액` ↔ `미회수금` 동일 여부는 미확정으로 유지되고, `capability.html:1371`이 `입금부족액은 6대 개념 어디에도 속하지 않는 제7의 축`으로 별도 격리한다. `상환액`은 대표 정의 산식(`순지급액 − max(0, 미지급금−과지급금)`)으로만 서술되고 6대 개념 어디에도 매핑되지 않았다.

### G-6 근거 범위 — **충족** (1차 미충족 → 해소)

플로우차트 전용 용어 문자열 검사. **대표 정의서 원문 용어는 예외**(`ceo_definitions.md` 실측 — `유동화투자자` `할인율` `순현금` `w금융일수` `ty수익율` `S입금부족율` `순지급액` `상환액` `미지급금` `과지급금` `대상정산금채권` `표본집합`).

| 문자열 | 레포 | Figma | 성격 | 판정 |
|---|---|---|---|---|
| `플로우차트` | 3 | 0 | **전부 제외 사실 명시** — `capability.html:1407` `근거 원천은 스토리보드 단독. 플로우차트 PPT는 제외했다. … 제외된 자료의 내용 자체는 옮기지 않는다`, `glossary.html:432` `플로우차트_투자자 UI_V1.3.pptx는 근거에서 제외돼 열지 않았다`, `archive.html` 작업 라벨 | 인용 아님 |
| `V1.3` | 1 | 0 | 위 파일명 표기 1건 | 인용 아님 |
| `보정후양수가액` | 1 | 0 | `capability.html:534` — 출처가 **Figma 정산채권DB 노드 `529:2950`**로 명기. 플로우차트 아님 | 인용 아님 |
| `금융일적수` | 1 | 0 | `capability.html:1418` — **삭제 목록 표**(`항목 삭제. 스토리보드 각주에 대응 개념이 없다`) | 인용 아님 |
| `정산채권투자액` | **0** | 0 | — | 0건 |
| `유동화기관` | 13 | 0 | 출처가 계약(`archive_02_계약약관.md`)·정책서(`02_TERMS_AND_STATUS.md`)·Figma(`257:2436`)·어드민 코드(`app/merchants/[id]/page.tsx:957`). 대표 정의서는 `유동화투자자`를 씀 | 인용 아님 |
| `지급할채무` `현금자산` | 0 | 0 | `ceo_impact.md`에 2건씩 — 대표 정의 영향 분석 내부 문서 | 0건 |

**Figma 32프레임 전체에 플로우차트 전용 용어 0건.**

**판정 — 충족. 플로우차트 인용 0건.** 잔존 문자열은 전부 제외 사실을 기록하는 메타 서술이거나 다른 원천을 명기한 인용이다.

### G-7 파일 표 — **부분**

`archive.html`(59,564 B) 등재율 실측:

| 대상 | 총수 | 등재 | 누락 |
|---|---|---|---|
| 레포 루트 HTML | 39 | **39** | 0 |
| 파이프라인 산출물(md·html·py·json·js·docx·sh·log) | 64 | **64** | 0 |
| `assets/xlsx/` | 4 | **4** | 0 |
| `assets/docs/` | 22 | **22** | 0 |
| 절대경로 문자열 `/Users…` | — | 137회 | — |

1차 루프 3-6의 누락 4건 전건 해소. 작업 목록 36행(완료 31 · 대기 4 · 결정 대기 1).

**결함 — 링크가 열리지 않는다.**

| 포트 | 링크 수 | 20:52 응답 |
|---|---|---|
| `localhost:8901` (레포) | 72 | **72 응답** |
| `localhost:8902` (파이프라인) | 64 | **64 실패** (`Errno 61 Connection refused`) |

고유 URL 136 중 **72 응답 / 64 실패.** 사용자 요청 문언이 `내가 열 수 있게 만들어`이므로, 임시 포트 의존은 요건 미달이다. push 후 Pages에서는 136건 전부 열리지 않는다.

또한 `archive.html`은 19:18 생성분이라 이후 갱신된 파이프라인 파일 4건(`figma_import_result.md` 20:26 · `todo.json` 20:33 · `figma_map_investor.json` 20:24 · `figcap_ia.sh` 20:03)의 **설명이 최신이 아니다**(파일명 등재는 돼 있음).

### G-8 도달성 — **충족**

**직접 헤드리스 재현.** Chrome `--headless=new --disable-gpu --virtual-time-budget --dump-dom`, 로컬 `python3 -m http.server 8811`, 20:48~20:53.

재현 8건 — 전건 성공.

| 조작 | 진입 | DOM 결과 |
|---|---|---|
| 초기 진입 | `app.html` | `section.screen` 14개 중 1개만 `hidden` 없음 |
| 메뉴 전환 (투자 수익) | `#invest-profit` | `data-screen="invest-profit" data-state="default"` 노출, 나머지 13개 `hidden=""` |
| 메뉴 전환 (가맹점) | `#merchants` | 동일 패턴 |
| 메뉴 전환 (계약기록) | `#contracts` | 동일 패턴 |
| 페이지 이동 | `#invest-assets/page2` | `data-state="page2"` |
| 전체 선택 | `#contracts/all` | `data-state="all"` |
| 규칙 미충족 | `#password/weak` | `data-state="weak"` |
| 외부 이동 확인 | `#coocon/confirm` | `data-state="confirm"` |

**죽은 컨트롤 0건.**

| 항목 | 실측 |
|---|---|
| `href="#"` 전 레포 | **2건** — `app.html:404` · `assets/template.html:24`. 둘 다 사이드바 로고 |
| 개별 화면 로고 링크 | `href="index.html"`로 전환 (1차 기준선 67~70건 → 소멸) |
| `data-act` 사용 | 32종 / `<button>` 45 · `<a>` 21 · `data-act` 속성 59개소 |
| `ACT[]` 핸들러 정의 | 30종 |
| 미도달 `data-act` | **0** — `backdrop`·`dock-screen`·`mc-buyer`·`mc-sector`·`pf-date` 5종은 `app.html:2424~2440`의 change/click 분기가 처리. `ct-page`·`ia-page`·`mc-page` 3종은 `pageBar()`가 동적 주입 |
| 깨진 상대 링크 | **0** — 13건 탐지분은 전부 JS 템플릿 문자열(`'…' + c.mid + '.pdf'`)과 `template.html`(슬라이스 원본, 서빙 대상 아님) |

기존 검증기 `verify_app_result.json`(19:24) 대조 — `dead: 0` · `console: 0` · `scanned: 134` · `menus: 7` · `states: **18**` · `downloads: 4` · `layout: 32`. **검증기 자신도 상태를 18로 센다.** 산출물 6개 파일의 `상태 20` 표기와 어긋난다(§3-2).

**상태 전건 도달** — 파일 19종 중 18종이 `app.html` 상태로 도달 가능. 미도달 1종은 `invest-profit--datepicker`(폐기 결정으로 `app.html`에서 제거, 네이티브 `input[type=date]`로 대체).

### G-9 임의생성 — **부분**

**`PayHug Investor` 잔존 — 로컬 0 / Figma 0 / 라이브 8화면.**

| 축 | Investor | Admin |
|---|---|---|
| 로컬 레포 | **0** | 34 |
| Figma `3066:328` | **0** | 31프레임 |
| 라이브 Pages | **8화면** | 0 |

로컬·Figma는 통과, 대외 노출면은 미통과. push 1회로 해소되나 그 push가 P-4로 묶여 있다.

**출처 3원칙 밖 요소** — 1차 적발 2건 처리 완료(D-8). 잔여 위반 2군:

| # | 요소 | 위치 | 성격 |
|---|---|---|---|
| 1 | 집계 `상태 20` | `index.html:135` · `app.html:393,977` · `README.md`(3곳) · `review.html:76` · `capability.html:440` | 실측 18~19와 어긋나는 숫자. 근거 없음 |
| 2 | 집계 `화면 14` vs `기본 화면 13` vs `기본 화면 8` | 같은 파일군 | 셈 기준 3종 병존 |

예시 데이터의 실존 오인 표기 — 사업자번호 `123-45-67890`~`890-12-34567` 순차 더미로 오인 가능성 낮음. 대표자명 8건 중 `조해민`·`백미순`은 상호 무관 실명형이나 예시 범위. 다만 `certificate.html`이 예시 고지 없이 서명값·합계·실명 16건을 게시하는 구성은 G-4·G-9 양쪽에 걸린다.

---

## §3 미충족·부분 목록 (우선순위)

### 우선 1 — 배포를 막는 것

| # | 항목 | 게이트 | 내용 | 해소 방법 |
|---|---|---|---|---|
| 1-1 | **라이브 구판이 폐기 산출물을 공개 중** | R-5 · D-9 · G-9 | Pages 8화면이 워드마크 `PayHug Investor`, 재현 불가한 투자수익 `4,210,000`(0.11%의 3배), 근거 없는 `투자자산 대비 3.31%`(실산출 0.303%, 10배 괴리), `검증 회신 완료` 문구, 예시 고지 없는 `certificate`·`merchants`를 노출 | push 1회 — **P-4 사용자 결정 대기** |
| 1-2 | **집계 숫자 `상태 20`이 6파일에 근거 없이 잔존** | G-9 · R-15 | 실측 상태 파일 19(유효 18), 검증기 18, Figma 프레임 18. `index.html` 한 파일 안에서만 `상태 20개`(:135) · `상태 19`(:166) · 카드 열거 합 18의 3중 불일치 | 18로 통일 후 `index`·`app`·`README`·`review`·`capability` + Figma `3142:2` 재임포트 |
| 1-3 | **예시 고지 부재 — 엑셀 실물 4 · 엑셀 미리보기 4 · 증명서 화면** | G-4 | 화면을 떠나 단독 유통되는 `.xlsx` 4건에 `예시` 0건. `certificate.html`은 서명값·억대 금액·실명 16건을 고지 없이 게시 | xlsx 각주 행 + `xls-*.html`·`certificate.html` 고지 삽입 |

### 우선 2 — 산출물 내부 정합

| # | 항목 | 게이트 | 내용 |
|---|---|---|---|
| 2-1 | **월별 요약 카드 ↔ 표 기간 충돌** | 숫자 정합 | `invest-profit--monthly.html` 카드는 `이번달`(2026-08-01~08-27, 투자실행금 5,108,900,000), 표는 6개월 6행 합계 32,097,500,000. 같은 파일 안에서 어긋난다. `app.html`은 월별 전환 시 기간을 2026-03-01~08-27로 강제해(`:1460`·`:2273`) 카드 32,097,500,000 · ④ 3.54% · ⑤ 2.23%를 낸다 — 정적 HTML과 5값 불일치. 어느 쪽이 기준인지 미결 |
| 2-2 | **`capability.html`이 해소된 결함을 재게시** | G-2 · 숫자 정합 | `:661` `:1226` `:1281` 3개소가 구값 `0.336%`와 `3배 어긋난다`를 `확인필요`로 표시. 실제 데이터는 7행 전건 `0.110000%`로 재생성돼 해소됐다. `0.336`이 남은 유일한 파일. 같은 파일의 `3.31%`·`92.42%`·`3.58%`도 현행 화면값(2.24%·93.5%·3.55%)과 어긋난다 |
| 2-3 | **`archive.html` 링크 64/136 무응답** | G-7 · R-9 | `localhost:8902` 죽음. push 후 Pages에서는 136건 전부 무응답 |
| 2-4 | **`review.html` 수행보고체 4곳** | G-1 · R-16 | `:94` `:108` `:115` `:136` |
| 2-5 | **`glossary_manuscript.md` 출처 없는 단정 76행** | G-2 | 장문행 530 중 14%. `ui_fix_result.md:24` Tailwind v4 외부 사실 서술 |

### 우선 3 — 판단·정의 대기 (임의 처리 금지)

| # | 항목 | 게이트 | 대기 사유 |
|---|---|---|---|
| 3-1 | D-3 사이드바 해석 | D-3 | 어드민 실메뉴 12개 병기인가, role-view 대체인가 |
| 3-2 | 대표 정의 미적용 5건 | D-10 | CI-11 차감항 · CI-12 나눗셈 · CI-15 S분모 · CI-18 기준금액 라벨 · CI-19 비중 축. 전건 `inquiry.html` Q-2·Q-4·Q-5·부록 N-9로 등재. 미지급금·과지급금 정의문이 와야 계산 가능 |
| 3-3 | 조달이자형 카드 존치 | D-1 | 어떤 표와도 연결되지 않는다. `inquiry.html` Q-6 |
| 3-4 | 할인율 `예시` → `예정` 승격 | G-4 | `inquiry.html` Q-7 |
| 3-5 | `보정후양수가액`·`유동화기관` 원천 등급 | D-8 | Figma 선정산시스템·계약 아카이브가 A/B/C 3원칙 안인가 |
| 3-6 | 현황 표 `Ty수익율 3.57%` ↔ 각주 역산 3.55% | G-4 | 4자 전부 3.57%로 동일해 대조 불일치는 아니나, 바로 아래 각주 산식으로 역산하면 0.02pp 어긋난다. 원인은 `비율의 가중평균 ≠ 가중평균의 비율` |
| 3-7 | 가맹점 비중 최상단 행 `20.4%` | 숫자 정합 | 실산출 20.5108%. `app.html:1330~1340` `ratios()`가 잔차를 최대 금액 행에 흡수해 합계 100.0을 닫는 설계(주석 명시). 4자 동일값이라 대조 불일치 아님 |

---

## §4 특별 조사

### 4-1. Figma ↔ 로컬 정합

**32프레임 = 본화면 8 · 상태 18 · xls 4 · 랜딩 1 · 로그인 1.** 페이지 직계 비-frame 노드 0건.

| 대조 | 결과 |
|---|---|
| Figma에만 있는 것 | **0건** |
| 로컬에만 있는 것 | **7건** — `app.html`(JS 전환이라 DOM 캡처 불가) · `invest-profit--datepicker.html`(폐기) · `glossary` `capability` `inquiry` `review`(문서) · `archive`(생성물) |
| 1:1 대응 | **32건 전건** |
| 값 대조 | `01 투자 자산` = 투자자산 1,628,400,000 / 투자실행액 1,523,100,000(93.5%) / 순현금 105,300,000(6.5%) / Ty 3.57%, 비중 합 100.0% — 로컬과 일치. `03 투자 수익` = 투자실행금 1,250,800,000 / 투자수익 1,375,880 / ④ 3.55% / ⑤ **2.24%** — 일치. 가맹점 16행 — 일치. `fabrication_fix.md` §4-2가 기록한 `Figma 8건 / 1,284,500,000` 로스터 불일치는 **재임포트로 해소** |

**`figma_import_result.md` §6 미해소 7건 — 전건 `실재`, 해소 0건.**

| # | 항목 | 판정 | 실측 |
|---|---|---|---|
| F-1 | `index.html` 상태 수 표기 낡음 | **실재 · 범위 확대** | `3142:29`에 `화면 14개와 상태 20개` 육안 확인. 같은 프레임 `3142:77`은 `기본 화면 8 · 상태 19`, 하위 카드 열거 합은 18. **한 프레임에 세 값 병존** — 고칠 자리가 한 문장이 아니라 둘 |
| F-2 | `xls-*` 제목 라벨 불일치 | **실재** | 프레임명 `엑셀 산출물 서식 —` / 화면 `h1` `엑셀 미리보기 —` 4건 전부(`3147:13`·`3149:13`·`3148:13`·`3150:13`). 슬러그(`xls-assets-merchant.html` 등) 노출은 `09 화면 갤러리` 카드 메타행 4건뿐으로 서식상 의도 |
| F-3 | 계약기록 zip 파일명 구규칙 | **실재** | `3144:336` `재양도합의서_전체16건_20260827.zip`. 엑셀 4종만 `{내용}_{YYYY-MM-DD}_{YYYY-MM-DD}` 신규칙 |
| F-4 | `invest-profit--datepicker` 미임포트 | **실재** | 달력 성격 프레임 0건. 로컬 파일만 잔존 |
| F-5 | `app.html` 미임포트 | **실재** | 대응 프레임 0건. `09 화면 갤러리` 카드 링크로만 참조 |
| F-6 | 요율 C1 미확정 | **실재 · 서술 부정확** | `0.11%`·`연 12%` 각 8프레임, `2.24` 3프레임. 단 §6이 적은 `예시값 표기`는 문자열로 부정확 — Figma에 `예시값`은 **0건**이고 실제 표기는 `수익 산정 기준 (예시)` + `산식·요율은 예시이며…` 각주 |
| F-7 | `figcap.sh` 원본 미보정 | **실재 · 범위 확대** | 87px 보정이 `figcap_ia.sh`에만 있다. 미보정 스크립트가 1개가 아니라 **3개** — `_pipeline/figcap.sh` · `_pipeline/member_redesign/figcap_state.sh` · `figcap_mock.sh` 전부 `--window-size=1922,1600` 고정 |

### 4-2. 대표 정의 반영도 — 마커 ③④⑤⑥

`marker_legend.md`가 푼 대응이 화면·엑셀·Figma에 일관 적용됐다.

| 마커 | 대표 정의 | 구현 | 판정 |
|---|---|---|---|
| ③ 투자수익(현황) | `PSM = SM 합` | 1,375,880 | **일치** |
| ④ 투자실행금액 대비 Ty | `PSMR × 365 ÷ PSD` | 대표식 3.5548% / 구현 `wavg(ty,exec)` 3.5543% → 둘 다 표기 **3.55%** | **일치** |
| ⑤ 투자자산 대비 Ty | `(④ × PSA) ÷ (PSA + PSC)`, `PSC` = 기간 EC **합**(유량) | `3.5543% × 1,250,800,000 ÷ (1,250,800,000 + 105,300,000×7) = 2.2364%` → **2.24%** | **일치** |
| ⑥ 일별 Ty | `(④÷③) × 365 ÷ ⑤` = (수익÷실행금)×365÷W | 7행 전건 소수 2자리 일치 (3.52/3.62/3.68/3.46/3.55/3.58/3.49) | **일치** |

`marker_legend.md` 말미의 `현재 화면은 스톡으로 계산해 3.32%가 나온다 — 화면 쪽 오류로 확정, 정정 대상`은 **해소됐다.** `app.html` `tyAssetOf()`가 `ecDays()`로 유량 합산(주간 PSC = 737,100,000)을 구현했고, 스톡 방식이면 3.28%가 나올 자리에 2.24%가 나온다.

`2.24%` 출현 — `invest-profit.html`(주간) · `invest-profit--monthly.html`(이번달, PSC 27일로 별도 산출인데 2.2422%로 우연히 동값) · `xlsx 투자수익현황` B8(0.0224) · Figma 3프레임(`3126:130`·`3136:133`·`3148:197`). `app.html`은 주간 2.24% / 월별 2.23%(기간 정의가 달라서다 — §3 2-1).

**미적용 5건**은 §3 3-2에 정리했다. 전건 문항으로 등재돼 있어 **임의 적용 0건**.

### 4-3. 숫자 정합 5자 대조

정적 HTML ↔ `app.html` ↔ `.xlsx` ↔ **PDF** ↔ **Figma**.

**PDF는 스텁이 아니다** — `투자자산증명서_20260827.pdf` 372,443 B / 2p, `재양도합의서_M2026-0001.pdf` 498,226 B / 3p, `계약서_서명대기_M2026-0001.pdf` 557,995 B / 4p. 전부 HeadlessChrome+Skia 생성 실문서.

| 값 | HTML | app.html | xlsx | PDF | Figma | 판정 |
|---|---|---|---|---|---|---|
| 투자자산 총액 | 1,628,400,000 | 동일 | 동일 | 해당없음 | 동일 | **일치** |
| 투자실행액 | 1,523,100,000 | 동일 | 동일 | 1,523,100,000 | 동일 | **일치** |
| 순현금 | 105,300,000 | 동일 | 동일 | 해당없음 | 동일 | **일치** |
| W금융일수 / S입금부족율 / Ty | 11.3일 / 0.42% / 3.57% | 동일 | 동일 | 해당없음 | 동일 | **일치** |
| 비중 | 93.5 / 6.5 | 동일 | 동일 | 해당없음 | 동일 | **일치** |
| 가맹점별 표 | **16행 × 6열 = 96셀** | 동일 | 동일 | 동일(+가맹점ID 열) | 동일 | **96셀 전건 일치** |
| 일별 투자수익 | 7행 + 합계 = 48셀 | 동일 | 동일 | 해당없음 | 동일 | **48셀 전건 일치** |
| 수익 현황 ④/⑤ | 3.55% / 2.24% | 3.55% / 2.24% | 동일 | 해당없음 | 동일 | **일치** |
| 할인율 | 0.11% | 0.11% | 미표기 | 계약서 제8조 `_______ %` 공란 | 0.11% | **부분** — 계약서는 C1 미확정을 채우지 않은 처리로 별개 개념(채권매입수수료 ≠ 유동화투자자 할인율) |

지시서가 전제한 `가맹점별 9행`은 구버전이다. 현행 원장은 `M2026-0001`~`M2026-0016` **16건**(`roster16_apply.py` 적용 후).

증명서 PDF ↔ `certificate.html` ↔ `app.html#certificate` — 투자자·작성자·작성일자·16행 96셀·합계·비중·서명값 문자열까지 **전건 일치**.

**1차 루프 §3-1 결함 — 해소.**

| 정산예정일 | 투자실행금 | 투자 수익(현행) | 수익÷실행금 | 1차 구값 | 구 비율 |
|---|---|---|---|---|---|
| 08-21 | 178,900,000 | 196,790 | **0.110000%** | 601,000 | 0.3359% |
| 08-22 | 172,600,000 | 189,860 | **0.110000%** | 578,000 | 0.3349% |
| 08-23 | 165,300,000 | 181,830 | **0.110000%** | 552,000 | 0.3339% |
| 08-24 | 187,700,000 | 206,470 | **0.110000%** | 634,000 | 0.3378% |
| 08-25 | 183,100,000 | 201,410 | **0.110000%** | 617,000 | 0.3370% |
| 08-26 | 176,600,000 | 194,260 | **0.110000%** | 596,000 | 0.3375% |
| 08-27 | 186,600,000 | 205,260 | **0.110000%** | 632,000 | 0.3387% |
| 합계 | 1,250,800,000 | 1,375,880 | **0.110000%** | — | — |

고친 방식 — `투자 수익 = 버림(투자실행금 × 0.11%)`, `상환액 = 투자실행금 + 투자 수익`, `Ty수익율 = 0.11% × 365 ÷ W금융일수`. 세 열이 한 요율로 재생성됐다(`rate_apply_result.md:184`). 구값 8종(`601,000` 등 · `3.59%`) 저장소 HTML·xlsx·PDF **잔존 0건**. **다만 지적 문서 `capability.html`이 미갱신**(§3 2-2).

잔여 불일치 — §3 2-1(월별 카드), 3-6(Ty 표시-산식), 3-7(비중 20.4%), 일별 합계행 라벨 3종 상이(`(평균)` / `(가중평균)` / 각주 `단순평균 아님` — 값은 동일).

### 4-4. 라이브 2곳의 위험

**Pages `joo2n.github.io/payhug-investor-admin/` — 구판 9화면 공개 중.**

| 경로군 | 응답 |
|---|---|
| 루트 HTML 9종(`index` `invest-assets` `invest-profit` `certificate` `contracts` `merchants` `acquisition` `coocon` `password`) | **200** |
| 루트 HTML 30종 | **404** |
| `assets/base.css` `template.html` `components.html` `logo-icon.png` | **200** |
| `assets/sheet.css` · `assets/xlsx/` 4 · `assets/docs/` 22 | **404** |

공개 중인 위험 — 정확히 8건.

1. **폐기 워드마크 `PayHug Investor`** 8화면.
2. **자기 산식으로 재현 불가한 투자수익 `4,210,000`** — 배분형 0.11%로는 1,412,950(3배 차), 조달이자형으로는 4,729,775. 어느 쪽으로도 안 맞는다.
3. **근거 없는 `투자자산 대비 3.31%`** — 실산출 `4,210,000 / 1,389,800,000 = 0.303%`, 약 10배 괴리.
4. **예시 고지 없는 `certificate.html`** — 작성자 `㈜페이허그` + 서명값 + `서명 검증: 인증서 발행기관 검증 회신 완료`(로컬은 `형식·전달 경로 미정의 — 확인 대상`으로 강등됨) + 실명 8건 + 억대 금액.
5. **예시 고지 없는 `merchants.html`** — 상호·사업자번호·대표자명 8건 전량.
6. **금액·건수 전면 불일치** — 투자자산 1,389,800,000(로컬 1,628,400,000), 계약기록 8건(로컬 16건).
7. **근거 없는 단정** `전 기기에서 재로그인 필요`(로컬은 `확인 대상`).
8. **소문자 `ty수익율` 혼재** 3곳(로컬은 전 7곳 `Ty수익율` 통일).

**Vercel `payhug-investor-demo.vercel.app` — 위험 없음.**

Deployment Protection **미관측**(401·403 0건, 전 경로 200 또는 404 `NOT_FOUND`). 실체는 한글 파일명 서빙 점검용 스텁이며 루트 506 B 본문이 `정본 사이트 아님. 경로·MIME·다운로드 동작 확인용 프리뷰.`로 자기 성격을 명시한다. `assets/docs/*.pdf` 200 응답 본문은 `PROBE-PDF-1`(12 B), xlsx는 13 B. **실데이터·실숫자 0건.** P-3(더미 11건)의 실체가 이것이며 대외 위험은 없다. 다만 스텁이 링크한 xlsx 파일명(`가맹점별_투자자산_20260827.xlsx`)이 실파일명(`가맹점별투자자산_2026-08-27_2026-08-27.xlsx`)과 다르다.

### 4-5. D-1~D-10 전건 — 특히 D-2 · D-7 · D-9

| ID | 판정 | 요지 |
|---|---|---|
| D-2 성공 경로만 | **충족** | 실패 경로 화면 0건. `new_screens_manuscript.md`가 설계한 실패 화면군(`login--error` 등)은 원고에만 있고 구현 0건 |
| D-7 추가 화면 폐기 | **충족** | 14건 전건 미구현. 오히려 근거 소멸분 1건(`merchants--filter-open.html`)을 삭제해 폐기 방향으로 움직였다 |
| D-9 워드마크 | **부분** | 로컬 34/0 · Figma 31/0으로 정정 완료. 라이브만 구 워드마크. 사이드바 7메뉴는 D-3 불변으로 유지 |
| 나머지 | §1 표 참조 | D-1 D-4 D-5 D-6 충족 · D-3 D-8 D-10 부분 |

---

## §5 배포 가능 여부 판정

### 판정 — **배포 불가.** L절 4단계 `Loop 2에서 미충족 0건` 요건 미달.

| 축 | 상태 |
|---|---|
| 미충족 | **1건** (R-5) |
| 게이트 통과 | **4 / 9** — G-3 G-5 G-6 G-8 |
| 게이트 부분 | **5 / 9** — G-1 G-2 G-4 G-7 G-9 |

### 무엇이 막는가

| 막는 것 | 게이트 | 성격 | 해소 주체 |
|---|---|---|---|
| **A. `상태 20` 오표기 6파일 + Figma `3142:2`** | G-9 | **자체 해소 가능.** 실측 18로 통일 후 Figma 09 프레임 재임포트 | 작업조 |
| **B. 예시 고지 부재 — xlsx 4 · `xls-*` 4 · `certificate`** | G-4 | **자체 해소 가능.** 고지 문구 삽입 | 작업조 |
| **C. `capability.html` 구값 재게시 3개소** | G-2 | **자체 해소 가능.** 해소된 지적의 시제 정정 | 작업조 |
| **D. `archive.html` 링크 64건 무응답** | G-7 | **자체 해소 가능.** localhost 절대 URL → 상대 경로 전환 | 작업조 |
| **E. `review.html` 수행보고체 4곳** | G-1 | **자체 해소 가능.** 현재 상태 서술로 재작성 | 작업조 |
| **F. `glossary_manuscript.md` 출처 없는 단정 76행** | G-2 | **자체 해소 가능하나 분량 큼.** 해설문과 단정의 경계 기준부터 세워야 함 | 작업조 |
| **G. 라이브 구판 8건 위험** | R-5 · D-9 · G-9 | **사용자 조치 필요 — P-4.** push 1회로 A~E 정정분과 함께 해소 | 사용자 |
| **H. D-3 사이드바 해석** | D-3 | **사용자 판단 필요.** 어드민 실메뉴 병기인가 role-view 대체인가 |  사용자 |
| **I. 대표 정의 미적용 5건 + 조달이자형 존치 + `예정` 승격** | D-10 · D-1 · G-4 | **대표 회신 대기.** `inquiry.html` Q-2·Q-4·Q-5·Q-6·Q-7 | 대표 |
| **J. 월별 카드 기간 정의** | 숫자 정합 | **사용자 판단 필요.** 정적 HTML(이번달) vs `app.html`(6개월) 중 기준 선택 | 사용자 |

### 권고 순서

1. **A~E를 먼저 닫는다** — 5건 전부 작업조 범위이고 근거가 확정돼 있다. 닫으면 게이트 G-1·G-2(일부)·G-4·G-7·G-9가 통과로 이동한다.
2. **H·J를 사용자에게 묻는다** — 각각 화면 구조와 숫자 기준이라 뒤에 고치면 파급이 크다.
3. **그 다음 push(P-4)** — G가 A~E 정정분과 함께 한 번에 해소된다. push 전까지 링크 공유 시 §4-4의 8건이 그대로 나간다.
4. **I는 배포를 막지 않는다** — 전건 `확인필요`/문항으로 등재돼 있고 화면 숫자는 `예시` 표기라 대표 회신 전에도 제안서 용도로는 성립한다.

### 배포되지 않은 채로 남아 있는 것 (정량)

| 구분 | 수량 | 규모 |
|---|---|---|
| 미푸시 커밋 | **0건** | `HEAD == origin/main` (`5ccfa3f`) |
| 수정(M) 추적 파일 | 14건 | +813 / −452 (1,265행) |
| 미추적(??) 파일 | **57건** | **14.2 MB** — html 30 · pdf 20 · xlsx 4 · zip 2 · css 1 |
| 루트 HTML 배포 대상 밖 | **30 / 39 (77%)** | |

한 번의 add·commit·push로 라이브 9화면 전량 교체 + 30화면 신규 공개가 동시에 일어난다.

---

## 부기 — 이 문서의 산출물

| 경로 | 이름 | 설명 |
|---|---|---|
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/verify_loop2.md` | 2차 검증 루프 | 이 문서. L절 3단계 산출물. 39항목 판정 + 게이트 G-1~G-9 전수 |

이 검증에서 수정·생성한 다른 파일은 없다.
`/Users/semi/cursor/payhug-investor-admin`는 읽기만 했고 `HEAD` `5ccfa3f` 불변, 워킹트리 변경 0건, 커밋·push 0건이다.
Figma `Tcf69tIciGxmlqCIuRb0iI`는 `get_metadata`·`get_screenshot` 조회만 했고 **쓰기 0건**이다.
헤드리스 재현·문자열 스캔의 임시 파일은 세션 스크래치패드에만 두었다.
