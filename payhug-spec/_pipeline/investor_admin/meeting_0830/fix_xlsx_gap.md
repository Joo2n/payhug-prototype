# 검산 엑셀 구멍 넷 메움 — 2026-09-01 4차 미팅용

근거: `meeting_0830/xlsx_acceptance_check.md` 합격선 대조. 대표 발언 2026-08-30 00:47:05 「워드에 있는 것들은 전부 엑셀에 나타나면 되지」 · 00:45:44 「수식이 들어 있는 채로」.

고친 파일 둘 — `build_audit_xlsx.py`(정본 생성기) · `audit_xlsx_check.py`(검사기). 산출물 네 벌은 생성기로 다시 뽑았다. 하루 선정산액 사다리(`입력` flow 상수 8칸) · ⑤ `입력!C19` · MAU 참고값은 손대지 않았다.

---

## 구멍 1 — `평균순현금` 칸

| 자리 | 넣은 것 |
|---|---|
| `기간집계!A16` | 항목명 `평균순현금` |
| `기간집계!B16` | **비움**. 일별 EC 원장이 없어 평균을 낼 수 없다 |
| `기간집계!C16` | `미확정 — 워드에 이름만 있고 정의 없음` |
| `기간집계!D16` | `ceo_definitions.md [2번] 첫 줄` |
| `기간집계!E16` | `미확정` |
| `산식!C21` | 원문 [2번] 첫 줄의 대응 셀에 `평균순현금은 기간집계 B16 (칸만 있고 값 없음 · 미확정)` 추가 |

`PSC 순현금 합`(B15) 바로 아래다. 값을 만들지 않았다 — `EC`·`PSC` 와의 관계를 정하는 것은 대표 몫이다.

**따라 밀린 자리** — ⑤ `B16→B17` · ⑥ `B17→B18` · 부족액 차감 합 `B18→B19` · S입금부족율 `B50→B51`. 부족액 차감 합을 무는 네 줄(`차감 / 채권매입수수료(%)` · `검산 — 수수료 - 차감 - PSM(정의)` · `DM 16:45 (가) 대표 근사식(%)` · `DM 16:45 차감합`)은 리터럴 `B18` 을 쓰던 것을 `LATE` 되돌아 적기로 옮겨, 행이 밀려도 따라오게 했다. 생성기 `assert` 는 `평균순현금 = 16` 을 못 박는다.

`meeting_0831/*.md` · `meeting_0830/xlsx_acceptance_check.md` 가 적어 둔 옛 행 번호(⑤ = `B16` 등)는 이 판부터 한 칸씩 밀렸다.

## 구멍 2 — `일별!J`(EC) 27칸이 전부 `=순현금`

| 자리 | 넣은 것 |
|---|---|
| `일별!J1` | 열머리를 `EC 순현금` → **`EC 순현금 (미확정)`** |
| `일별!N1` | `미확정 — EC 는 전일자 마감시점(자정) 잔액. 일별 EC 원장이 없어 J2:J28 27칸 전부 조회시점 순현금(입력 C8)을 그대로 놓았다. 그래서 PSC 는 순현금 x 조회 일수다.` |

값은 그대로 `=순현금` 27칸이다. `PSC = 순현금 × 7` 이 나오는 까닭이 이제 시트 안에 있다.

## 구멍 3 — 화면 값과 엑셀 값이 갈리는 자리

`읽는 법` 시트 맨 위 `A1:G7` 에 표를 올렸다. 통합문서를 열면 첫 시트 첫 화면이다.

| 열 | 내용 |
|---|---|
| A | 순번 |
| B | 항목 |
| C | 까닭 — `=화면대조!H{행}` |
| D | `화면대조 {행}행` |
| E | 엑셀 값 — `=화면대조!C{행}` |
| F | 화면 값 — `=화면대조!E{행}` |
| G | 차이 — `=화면대조!F{행}` |

값·까닭 전부 `화면대조` 모델 잔차 행을 **수식으로 물어 온다.** 옮겨 적은 숫자가 아니라 같은 셀이라 두 곳이 어긋날 수 없다.

| # | 항목 | 화면대조 행 | 엑셀 | 화면 |
|---|---|---|---|---|
| 1 | 조회기간 ④(%) | 155 | 4.455949 | 3.99 |
| 2 | 조회기간 ⑤(%) | 156 | 2.530991 | 2.24 |
| 3 | 조회기간 PSA | 152 | 184,076,060 | 179,970,919 |
| 4 | 조회기간 PSD | 153 | 3.089424 | 3.108481 |
| 5 | S입금부족율 raw(%) | 159 | 0.072411 | 0.074428 |
| 6 | 채권 건수 | 160 | 1,280 | 61,760 |

`읽는 법` 시트 차례의 자기 줄도 `화면 ↔ 엑셀 갈림 6줄 · …` 로 맞췄다.

## 구멍 4 — 검사기 판정 22건 추가

`audit_xlsx_check.py` 에 판정기 둘을 새로 두고 기존 `variant` 를 넓혔다. 하위 명령 `word` · `mark` 를 더했고 둘 다 `all` 의 종료 코드에 든다.

### `word_test` — 워드 변수 전수 대응 · 값/수식 (판정 11건)

기준표를 검사기 안에 두지 않는다. 이름을 `ceo_definitions.md` 원문에서 뽑는다.

- **(가) 줄 머리 이름** — `이름: …` · `이름 = …` 의 앞부분. 32종. `산식` C열 색인이 셀을 지시하는지로 본다
- **(나) 줄 안에서만 이름이 불리는 산출 대상** — `아래와 같이 … 산출해` 열거. 7종(상환액 · 투자실행금 · 투자수익 · 투자수익율 · w금융일수 · ty수익율 · **평균순현금**). `산식`·`읽는 법` 두 색인 시트를 뺀 데이터 시트에 제 칸이 있어야 한다. 여기가 `평균순현금` 을 잡는 자리다
- **(다) 괄호로 이름 붙인 기호** — 대문자 두 자 이상. 8종(EC · PSA · PSM · SAD-1 · SBD-1 · SDD-1 · SMD-1 · SMRD-1). 원문 표기와 통합문서 표기(`SBd-1`)가 대소문자만 다르므로 대소문자 무시로 대조

값/수식 판정은 `산식` C열이 지시한 자리를 실제로 열어 본다. 셀 주소·범위·`H열` 꼴을 파싱하고 괄호 안(다른 파일 인용·풀이말)은 뺀다. 값 셀이 하나라도 있으면 그 줄은 `값` 이다.

- 실측 **수식 34줄 · 값 8줄 · 자리 지시만 3줄 = 45줄**
- 값 8줄 = 할인율 · Di · 플랫폼ID · 금융일수 · 기준일 · DD-1i · 금융일수(전일자) · Dpi. 자리는 `입력`·`채권` 두 시트뿐
- `VALUE_LINES = 8` 로 **개수를 못 박았다.** 늘어도 줄어도 FAIL

### `mark_test` — EC 표식 · 갈림 표 (판정 7건)

- `일별` EC 열은 열머리에서 찾는다(열 주소를 손으로 적지 않는다). 못 찾으면 예외로 죽는다
- EC 27칸 수식이 한 가지뿐이면 `상수` 로 보고, 그때는 열머리에 `미확정` 이 있어야 하고 `미확정`·`EC`·`원장`·`순현금` 넷을 함께 담은 까닭 칸이 있어야 한다
- `읽는 법` 갈림 표를 `화면 ↔ 엑셀 갈림` 머리로 찾아, 각 행이 가리키는 `화면대조` 행의 항목명·엑셀 값·화면 값·차이·까닭과 대조한다. 그 행이 `모델 잔차` 구분인지, 차이가 실제로 0 이 아닌지도 본다

### `variant_test` 추가 (판정 4건)

세 벌(유휴 20·25·30%)에도 평균순현금 칸 · 값 없음 + `미확정` 표식 · EC 열머리 표식 · 첫 시트 맨 위 갈림 표가 있는지 본다.

### 새는 갈래 넷을 막았다

| 갈래 | 막은 방법 |
|---|---|
| FAIL 찍고 exit 0 | `word`·`mark` 는 `sys.exit(0 if 통과 else 1)`, `all` 의 `ok` 식에 `s8['통과'] and s9['통과']` |
| try-catch 가 SKIP | 두 판정기에 `try` 없음. 원문 줄을 못 읽거나 EC 열머리·갈림 표 머리를 못 찾으면 예외로 죽는다 |
| 판정 없이 값만 출력 | 두 판정기 모두 `판정` 딕셔너리 + `통과` 를 낸다 |
| 대상 0건이면 통과 | `열거 산출 대상 0건 아님` · `괄호 기호 0건 아님` · `검사한 평문 셀 0건 아님` · `EC 칸 0건 아님` · `갈림 표 행 0건 아님` · `수식으로 선 줄 0건 아님` |

---

## 판별력 시험 — 사본에서 4건 전건 FAIL

실물은 건드리지 않고 사본에 흠을 냈다.

| # | 흠 | 검사기 | 종료코드 | 깨진 판정 |
|---|---|---|---|---|
| ① | `기간집계` 평균순현금 행 16 비움 | `word` | **1** | 열거 산출 대상 전건이 칸을 갖는다 |
| ② | `일별!J1` 에서 `미확정` 뗌 · `일별!N1` 까닭 지움 | `mark` | **1** | EC 가 상수면 열머리에 미확정 표식 · EC 가 상수면 까닭 표식이 있다 |
| ③ | `읽는 법!E2` 를 3.99 로 바꿈 | `mark` | **1** | 갈림 표가 화면대조와 어긋나지 않는다 |
| ④ | `기간집계!B10`(PSMR) 수식을 값 0.5 로 | `word` | **1** | 값으로 선 줄이 못 박은 수와 같다 |
| 대조 | 손대지 않은 사본 | `word`·`mark` | **0** | — |

실물 네 벌 md5 는 시험 전후가 같다.

---

## 검증

| 검증기 | 종료코드 |
|---|---|
| `audit_xlsx_check.py all` | 0 (`전체 통과: true`) |
| `audit_xlsx_check.py notation` | 0 |
| `audit_xlsx_check.py idle` | 0 |
| `audit_xlsx_check.py variant` | 0 |
| `audit_xlsx_check.py five` | 0 |
| `audit_xlsx_check.py word` | 0 |
| `audit_xlsx_check.py mark` | 0 |
| `verify_cycle_xlsx.py` | 0 |
| `verify_weighting.js` | 0 |
| `verify_batch_symbols.py` | 0 (검사 144건 · FAIL 0건) |

## 셈 — 전후

| 항목 | 전 | 후 |
|---|---|---|
| 수식셀 | 26,872 | **26,896** (+24) |
| 값셀 | 3,206 | **3,236** (+30) |
| 시트 | 10 | 10 |
| `기간집계`·`일별`·`가맹점` 숫자 값셀 | 0 | 0 |
| `산식` 원문 줄 / 대응 셀 빈 줄 | 45 / 0 | 45 / 0 |
| `화면대조` 모델 잔차 행 | 27 | 27 |
| `기간집계` 조회기간 집계 행 | 2~28 | 2~29 |

늘어난 수식셀 24 = 갈림 표 6행 × 4칸. 늘어난 값셀 30 = 갈림 표 머리 7 + 항목 6 + 화면대조 행 6 + 순번 6 + `일별!N1` 1 + 평균순현금 4.

## 산출물

| 벌 | 경로 | 크기(바이트) |
|---|---|---|
| 기본 | `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901.xlsx` | 252,795 |
| 유휴 20% | `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴20.xlsx` | 252,796 |
| 유휴 25% | `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴25.xlsx` | 252,802 |
| 유휴 30% | `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴30.xlsx` | 252,832 |

레포 사본은 `payhug-spec/_pipeline/investor_admin/` 아래 같은 이름 네 벌이고 바이트가 같다.

## 남은 것

- **하루 선정산액 범위 160만~641만.** 대표 발언 100만~1,000만을 덮지 못한다. 축이 하루 선정산액인지 묶인 투자금액인지가 안 풀려(`meeting_20260830_analysis.md` Q13) 사다리를 건드리지 않았다. 미팅에서 묻고 고칠 일이다
- **⑤ `입력!C19`** 는 워드 원문 산식이 든 채로 2.53% 를 낸다. 대표 재작성 대기. `미확정` 표식 3곳 그대로
- **③ `입력!C18`** 빈 칸. ⑥ 은 그동안 문자열 「미확정」
- Excel 실기 재계산 미확인. 이 맥에 LibreOffice 가 없어 `audit_xlsx_check.py` 자체 평가기 결과만 봤다(평가 실패 0건)
