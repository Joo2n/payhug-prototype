# 검산 엑셀 고침 — 2026-08-31

근거 — `meeting_0831/meeting_20260831_analysis.md` · `meeting_0831/sim_readiness.md`

이 워크북은 파일로 직접 전달한다. 어드민 화면에 붙이지 않는다.

## 산출물

| 경로 | 무엇 |
|---|---|
| `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901.xlsx` | 정본 (유휴 20%) |
| `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴20.xlsx` | 유휴 20% 벌 |
| `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴25.xlsx` | 유휴 25% 벌 |
| `/Users/semi/Downloads/payhug_검산엑셀/검산_투자자어드민_20260901_유휴30.xlsx` | 유휴 30% 벌 |
| `/Users/semi/Downloads/payhug_정산주기_정리/정산주기_정리_20260830.xlsx` | 가중 기준 갈라적기 표식 3자리 |

네 벌 모두 `_pipeline/investor_admin/` 에 같은 이름의 정본 사본이 있고 바이트가 같다.

| 생성기·검증기 | 역할 |
|---|---|
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/build_audit_xlsx.py` | 검산 워크북 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/build_cycle_xlsx.py` | 정산주기_정리 워크북 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/audit_xlsx_check.py` | 검산 워크북 검증 (`all` · `notation` · `idle` · `variant` · `switch` · `sens` · `mix`) |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/verify_cycle_xlsx.py` | 정산주기_정리 검증 + 갈라적기 게이트 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/verify_weighting.js` | 가중 기준 혼용 차단 게이트 |

## 1. 총 투자자산 · 유휴 비율 두 칸

`입력` 시트 값 표가 14행에서 16행이 됐다. 규모 4칸의 행이 바뀐다.

| 셀 | 항목 | 내용 |
|---|---|---|
| `입력!C5` | 총 투자자산 | 100,000,000 — `ledger_facts.json total` |
| `입력!C6` | 유휴 비율 | 0.2 — `cash ÷ total` |
| `입력!C7` | 투자실행액 목표 | `=C5-C8` |
| `입력!C8` | 순현금 | `=ROUND(C5*C6,0)` |

`C7`·`C8` 이 값 셀에서 수식 셀로 바뀌었다. 유휴 비율 한 칸만 돌리면 나머지가 따라온다.
합은 언제나 `C5` 다 — `C8` 을 먼저 반올림하고 `C7` 이 나머지를 받는다.

행이 밀린 자리 — 스위치 `B20:B23`(전 `B18:B21`) · 플랫폼 구성비 `C26:C29`(전 `C24:C27`) ·
금액 실측 `B33:B36`(전 `B31:B34`) · MAU `B41:B43`(전 `B39:B41`) · 배달앱/전체 `B45`(전 `B43`) ·
기준일 `C10`(전 `C8`) · 로스터 머리 47행(전 45행).
생성기와 검증기는 이 주소를 라벨에서 뽑는다. 손으로 적은 자리는 없다.

`meeting_0831/sim_readiness.md:138·141·229` 의 `입력!C5 투자실행액 목표` · `C6 순현금` ·
`C8 기준일` 표기는 이 판 기준으로 `C7` · `C8` · `C10` 이다. 그 문서는 이 조 소관 밖이라 손대지 않았다.

### 반올림 어긋남 — 75,000,001 · 70,000,013

금융일수 `Di` 는 채권 시트의 값 셀이다. 유휴 비율만 손으로 바꾸면 미회수 채권의 조합이
그대로라 투자실행액이 목표에서 밀린다. 실측 — 유휴 25% 75,000,001원 · 유휴 30% 70,000,013원.

세 벌은 그 비율로 채권 배정을 다시 푼 원장이다. 세 벌 모두 목표와 차 0원.

그 자리를 셀에서도 잡는다 — `화면대조` 불변식 두 줄을 새로 뒀다.

| 항목 | 엑셀 값 | 화면 값 |
|---|---|---|
| 투자실행액 = 입력 목표 | `기간집계!B35` (미회수 Σ Ai) | `=투자실행액목표` |
| 투자자산 = 총 투자자산 | `기간집계!B37` | `=총투자자산` |

유휴 비율을 손으로 바꾸고 다시 풀지 않으면 이 두 줄이 `차이` 로 뒤집힌다(음성 시험 §7).

## 2. ⑤ 대표 재작성 대기 · 산식 ⑥ 공란

### ⑤ — `기간집계!B16`

값은 현행 산식 `=IF(B5+B15=0,0,B13*B5/(B5+B15))` 그대로 둔다.

| 칸 | 내용 |
|---|---|
| `기간집계!E16` (화면 반영) | `대기` — 노란 칠. 다른 줄은 `정본`(초록) 또는 `참고` |
| `기간집계!D16` (출처) | `ceo_definitions.md [2번] 이미지의 ⑤ — 대표 재작성 대기 (A-01)` |

`대기` 는 이 통합문서가 이미 쓰는 낱말이다 — `입력!J열` 계약 상태 `서명 대기` · `가맹점!AD` 합계 `n건 대기`.

### ⑥ — `산식!A46`

원문 45줄 중 이 한 줄만 대응 셀이 없었다. 못 채우는 이유를 그 자리에 적었다.

`산식!C46` = `대응 셀 없음 — ⑥ 안의 ③·④·⑤ 지시 대상 미확정 (TP-66 · F-23). 대표 주석 「계산식 다시 확인해볼것」`

대표 주석 원문 — `meeting_0831/ceo_definitions_20260831.txt:51`
「6번, 위에 4,5번과 다르니 헷갈리지 않도록 계산식 다시 확인해볼것」.
`meeting_20260831_analysis.md:428` 이 이 줄을 「결론이 아니라 확인 과제」로 판정했다.

`산식` 시트 대응 셀 빈 줄 0건.

## 3. S입금부족율 잔차 등재

```
엑셀 기간집계!B42   0.072411 %
화면 sRaw          0.074428 %      차 −0.002017 · 잔차율 −2.71%
```

`화면대조` `모델 잔차` 표에 `S입금부족율 raw(%)` 행을 넣었다(26행 → 27행).
사유는 다른 잔차와 같은 두 문장 — 원장 틸트(`TILT`) · 건당 반올림(`ROUNDUP`).

기존 불변식 `S입금부족율(%)` 은 `ROUND(…,2)` 뒤 `0.07 = 0.07` 을 본다. 그 줄은 그대로 남긴다.
검사는 raw 로 따로 본다 — `audit_xlsx_check.py main()` 이 `기간집계!B42` 원값과
`ledger_facts.sRaw` 를 직접 재고, `모델 잔차` 표에 그 행이 등재돼 있는지까지 게이트로 건다.

같은 표의 다른 잔차 — 조회기간 PSA +2.28% · 조회기간 PSM(차감 반영) +13.49%.

## 4. `읽는 법` 시트

워크북 첫 장. 세 덩이만 있다.

| 자리 | 무엇 |
|---|---|
| `A1:D11` | 시트 10장 차례 · 각 시트가 무엇인지 한 줄 · 주요 칸 |
| `A13:D16` | 화면 값 3개를 되짚는 경로 |
| `A18:E28` | 바꾸면 시뮬레이션이 되는 칸 10개와 그 칸이 움직이는 값 |

되짚는 경로 3줄 (전부 실물 주소를 확인해 적었다).

```
투자실행액       입력 C7 → 가맹점 B14 → 가맹점 P2:P9 → 채권 J2:J1281 (Q열=1) → 기간집계 B29 → B35
W금융일수 표기    플랫폼 C2:O5 → 플랫폼 D9:D12 → 가맹점 O2:O9 → 채권 S2:S1281 → 기간집계 B32 → B33
Ty수익율(%)     입력 C3 · 입력 C4 · 기간집계 B33 → 기간집계 B34
```

값 칸은 `=화면대조!E2` 처럼 화면 값 원본 블록을 가리키는 수식이다.

시뮬레이션 칸 — `입력!C5` 총 투자자산 · `C6` 유휴 비율 · `B20:B23` 스위치 ①②③④ ·
`C26:C29` 플랫폼 구성비 · `C14:C15` 조회기간 · `C10` 기준일 · `B45` 배달앱/전체.

## 5. 정산주기_정리 워크북 갈라적기

이 워크북의 `가중평균!B7 = 2.7504068548610725` · `B11 = 14.60%` 는 MAU(이용자 수) 비중 기준이다.
값은 그대로 둔다. 표식만 붙였다.

| 시트 | 셀 | 내용 |
|---|---|---|
| `가중평균` | `A13` · `B13:D13` · `E13` | `가중 기준` / `MAU(이용자 수) 비중 기준 시장 평균 참고값 — 화면 계산은 금액(Ai) 기준 W 3.039607일(표기 3.04) · Ty 13.21%` / 출처 |
| `읽는 법` | `F5` 머리 · `F19` | 같은 문구. `C19 = 2.7504068548610725` 바로 옆 |
| `검산` | `F1` 머리 · `F25` | 같은 문구. `C25 = 2.7504068548610725` 바로 옆 |

3.04 · 13.21 은 `ledger_facts.json` 의 `w` · `ty` 에서 읽는다. 생성기에 손으로 적지 않았다.

`verify_weighting.js` 게이트에 이 파일을 검사 대상으로 넣었다 — 판정 5줄 추가.

```
(다7) 정산주기_정리 워크북 판독
(다7) 정산주기_정리 검사 대상 0건 아님                          3,618셀
(다7) 정산주기_정리 MAU 값 자리 0건 아님 — 참고값이 지워지지 않았다   읽는 법!C19 · 검산!C25
(다7) 정산주기_정리 `가중평균` 시트 갈라적기 한 줄                표식 · 금액 · W 3.04 · Ty 13.21
(나)  정산주기_정리 워크북 — 갈라 적지 않은 자리 0건               2곳 검사
```

금지값 자리가 0건이면 FAIL 이다 — MAU 참고값이 지워지는 것도 잡는다.
같은 게이트를 `verify_cycle_xlsx.py` 에도 걸었다(`±4행 안에 참고·시장 평균·MAU 중 하나`).

## 6. 세 벌

투자자산 1억 고정. 유휴 비율만 다르다. 채권 배정을 벌마다 다시 풀었다.

| 항목 | 유휴 20% | 유휴 25% | 유휴 30% |
|---|---|---|---|
| 총 투자자산 (`입력!C5`) | 100,000,000 | 100,000,000 | 100,000,000 |
| 유휴 비율 (`입력!C6`) | 0.20 | 0.25 | 0.30 |
| 투자실행액 목표 (`입력!C7`) | 80,000,000 | 75,000,000 | 70,000,000 |
| 순현금 (`입력!C8`) | 20,000,000 | 25,000,000 | 30,000,000 |
| **투자실행액** (`기간집계!B35`) | **80,000,000** | **75,000,000** | **70,000,000** |
| 목표 대비 차 | **0원** | **0원** | **0원** |
| 투자자산 (`B37`) | 100,000,000 | 100,000,000 | 100,000,000 |
| 투자실행액 비중 (`B38`) | 80.0% | 75.0% | 70.0% |
| 순현금 비중 (`B39`) | 20.0% | 25.0% | 30.0% |
| **W금융일수 raw** (`B32`) | 3.039607 | 3.039607 | 3.039607 |
| **W금융일수 표기** (`B33`) | **3.04** | **3.04** | **3.04** |
| **① ty수익율** (`B34`) | **13.21%** | **13.21%** | **13.21%** |
| **S입금부족율 raw** (`B42`) | **0.072411%** | **0.072407%** | **0.072421%** |
| S입금부족율 표기 | 0.07% | 0.07% | 0.07% |
| **② 상환액** (`B8`) | **184,145,606** | **172,491,299** | **161,441,852** |
| PSA 투자실행금 (`B5`) | 184,076,060 | 172,425,812 | 161,381,369 |
| PSM 투자수익 (`B6`) | 69,426 | 65,369 | 60,391 |
| PSD (`B12`) | 3.089424 | 3.088695 | 3.103071 |
| **④ 투자실행금액 대비 ty** (`B13`) | **4.46%** | **4.48%** | **4.40%** |
| **⑤ 투자자산 대비 ty** (`B16`) | **2.53%** | **2.22%** | **1.91%** |
| 가맹점 수 | 8 | 8 | 8 |

**③ 은 이 표에 없다.** 대표가 「상단 현황의 기간 전체 숫자」라고만 지목했고
어느 칸인지가 정해지지 않았다(`meeting_20260831_analysis.md:335` · `U-03` · `F-23`).

**⑤ 는 대표 재전달 대기다.** 위 값은 현행 산식 `(④ x PSA) / (PSA + PSC)` 로 낸 잠정값이다.
새 수식이 오면 세 벌을 다시 낸다. 대기 표식은 `기간집계!E16` 에 `대기` 로 박혀 있다.

`화면대조` 판정 — 유휴 20% 벌은 차이 0건. 유휴 25%·30% 벌은 불변식 4줄이 `차이` 다.

| 유휴 25%·30% 벌에서 `차이` 로 뜨는 줄 | 까닭 |
|---|---|
| 투자실행액 · 순현금 · 투자실행액 비중 · 순현금 비중 | `화면 값 원본` 블록은 배포된 화면이 실제로 띄우는 20% 값이다. 모델이 25%·30% 라 갈린다 |

이 4줄 말고는 세 벌 전건 `일치` 다.

## 7. 수식·값 개수 전후 대조

| 시트 | 수식 전 | 수식 후 | 값 전 | 값 후 |
|---|---|---|---|---|
| 읽는 법 | — | 13 | — | 102 |
| 입력 | 55 | 57 | 294 | 302 |
| 플랫폼 | 55 | 55 | 121 | 121 |
| 가맹점 | 277 | 277 | 70 | 70 |
| 채권 | 25,600 | 25,600 | 1,301 | 1,301 |
| 일별 | 360 | 360 | 14 | 14 |
| 기간집계 | 44 | 44 | 122 | 122 |
| 가중치 대조 | 84 | 84 | 91 | 91 |
| 화면대조 | 361 | 373 | 799 | 811 |
| 산식 | 0 | 0 | 137 | 138 |
| **합계** | **26,836** | **26,863** | **2,949** | **3,072** |

**줄어든 시트 0곳.** 늘어난 27수식의 내역 — `읽는 법` 신설 13 · `입력` 두 칸 유도 2 ·
`화면대조` 불변식 2줄 8 · `모델 잔차` S raw 1줄 4.

아래첨자 리치텍스트 셀 80 → 82. `읽는 법` 시트의 `Di` · `Ai` 두 칸이 늘었다.
저장은 `rich_text=True` 로 다시 열지 않고 생성기가 처음부터 다시 쓴다.

## 8. 검증

| 검증기 | 종료코드 | 결과 |
|---|---|---|
| `audit_xlsx_check.py all` | **0** | 전체 통과 · 화면대조 판정 67행 차이 0 · 모델 잔차 27행 |
| `audit_xlsx_check.py notation` | **0** | 기호 셀 82 · 아래첨자 글자 254 · 규약 어긴 자리 0 · 자기시험 판별력 O |
| `audit_xlsx_check.py idle` | **0** | 두 칸 유도 판정 7건 전건 O |
| `audit_xlsx_check.py variant` | **0** | 세 벌 판정 8건 전건 O |
| `verify_cycle_xlsx.py` | **0** | PASS · MAU 값 자리 2곳 · 갈라 적지 않은 자리 0곳 |
| `verify_weighting.js` | **0** | 판정 28건 · FAIL 0 (23 → 28) |

### 새로 건 판정

| 검사 | 어디 | 무엇을 본다 |
|---|---|---|
| `idle_test` | `audit_xlsx_check.py` | `C5`·`C6` 은 값 셀 · `C7`·`C8` 은 수식 · 20/25/30% 세 벌에서 합 = 총 투자자산 · 목표 = 총자산 x (1−유휴) · 순현금 = 총자산 x 유휴 |
| `variant_test` | `audit_xlsx_check.py` | 세 벌 실물 존재 · 사본 바이트 동일 · 투자실행액이 목표와 원 단위까지 같다 · 유휴 비율 실측 = 지정값 · 불변식 두 줄 전건 일치 · ⑤ 가 `대기` 표식 · 세 벌이 서로 다르다 |
| S raw | `main()` | `기간집계!B42` 원값 ↔ `ledger_facts.sRaw` · `모델 잔차` 표 등재 여부 · 표기 비교 줄 잔존 여부 |
| 산식 | `main()` | 원문 줄 수 > 0 · 대응 셀 빈 줄 0건 |
| ⑤ 표식 | `main()` | `기간집계!E16 = 대기` · `D16` 에 `대기` 문자열 |
| 갈라적기 | `verify_cycle_xlsx.py` · `verify_weighting.js` | 금지값 자리 > 0 · 자리마다 ±4행 표식 · `가중평균` 갈라적기 줄에 표식·금액·W·Ty |

### 음성 시험 — 심어 놓고 잡히는지 확인

전부 사본에서 돌렸다. 실물은 md5 로 원본 그대로임을 확인했다.

| 심은 것 | 결과 |
|---|---|
| `가중평균` 표식 줄 삭제 | `verify_cycle_xlsx.py` FAIL(표식 낱말 X · 금액 X · W X · Ty X) · `verify_weighting.js` FAIL |
| `읽는 법!F19` 표식만 삭제 | `verify_cycle_xlsx.py` FAIL — 갈라 적지 않은 자리 1곳 `읽는 법!C19` · `verify_weighting.js` FAIL |
| `입력!C8` 순현금을 손으로 박은 값으로 | `idle_test` FAIL — 순현금이 수식 X · 목표 = 총자산 x (1−유휴) X |
| `산식!C46` 비우기 | `main()` 산식 대응 셀 빈 줄 1건 |
| `모델 잔차` S raw 행 삭제 | `main()` S raw 잔차 등재 0건 · 잔차 행 27 → 26 |
| 유휴 25% 벌의 `C6` 만 0.30 으로 손편집 | `variant_test` FAIL — 투자실행액 70,000,011 (차 +11원) · 불변식 두 줄 `차이` |

검사 대상이 0건이면 FAIL 로 잡는 자리 — 렌더 · 저장소 원천 · 원고 · 산출 문서 ·
정산주기_정리 셀 수 · 정산주기_정리 금지값 자리 · 세 벌 파일 · 산식 원문 줄 · 기호 셀 · 아래첨자 글자.

### 검사 경로 갈아 끼우기

음성 시험을 실물에 하지 않으려고 세 환경변수를 뒀다. 기본값은 전부 실물 경로다.

| 변수 | 기본값 | 쓰는 곳 |
|---|---|---|
| `AUDIT_XLSX` | `_pipeline/investor_admin/검산_투자자어드민_20260901.xlsx` | `audit_xlsx_check.py` |
| `AUDIT_VARIANT_DIR` | `_pipeline/investor_admin/` | `audit_xlsx_check.py` 세 벌 |
| `CYCLE_OUTDIR` | `~/Downloads/payhug_정산주기_정리` | `verify_cycle_xlsx.py` |
| `CYCLE_GLOB` | `~/Downloads/payhug_정산주기_정리/정산주기_정리_*.xlsx` | `verify_weighting.js` |

## 9. 다시 만드는 법

```
python3 build_audit_xlsx.py                 # 정본 한 벌
python3 build_audit_xlsx.py 유휴 20 25 30    # 세 벌
python3 build_cycle_xlsx.py                 # 정산주기_정리
```

한 벌에 약 16초. 세 벌 약 50초.

## 10. 남은 것

| 무엇 | 상태 |
|---|---|
| ⑤ 새 수식 | 대표 전달 대기(`A-01`). 오면 세 벌 재산출 |
| ⑥ 안의 ③·④·⑤ 지시 대상 | 미확정(`TP-66` · `F-23`). `산식!C46` 에 그 사실만 적혀 있음 |
| ③ 이 현황 표의 어느 칸인가 | 미확정(`U-03`) |
| `sim_readiness.md` 의 `C5`·`C6`·`C8` 표기 | 이 판에서 `C7`·`C8`·`C10`. 그 문서는 이 조 소관 밖 |
