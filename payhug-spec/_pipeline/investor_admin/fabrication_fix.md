# 임의 생성 요소 정정 — 투자자 어드민

`fabrication_audit.md` §5 적용 지시서를 대상으로 삼는다. 근거 코드는 감사와 같다 — **A**(스토리보드) · **B**(실제 어드민) · **C**(대표 정의서) · **R**(사용자 요청) · **F**(자체 정책 분석·계약서 원본).

| 항목 | 값 |
|---|---|
| 대상 레포 | `/Users/semi/cursor/payhug-investor-admin` — `HEAD` = `5ccfa3f`, **커밋·push 0건** |
| 계약서 원본 | `/Users/semi/Desktop/01_PayHug/11_계약서/` — **읽기만**, 수정·이동 0건 |
| 제외 파일 | `inquiry.html`(타 조 제작 중) · `glossary.html`(원고 개정 대기). `capability.html`은 §2 오분류 복원만 적용 |
| 불변 확인 | 사이드바 7메뉴 · 금액·비중 수치 · 비중 합 100.0% — 손대지 않음 |

---

## §1 임의생성 8건 처리

### 1-1. 워드마크 `PayHug Investor` → `PayHug Admin`

근거는 실제 어드민 `components/AdminLayout.tsx:415,447`(`Admin`이 `primary-400`)과 스토리보드 슬라이드12·13 삽입 이미지의 브라우저 탭 표제다. `<em>` 강조부가 이미 `primary-400`을 물고 있어 색 처리는 마크업 변경 없이 유지된다.

| 대상 | 파일 수 | 문자열 수 | 경로 |
|---|---|---|---|
| 화면 HTML(사이드바형) | 28 | 56 | 레포 루트 |
| 화면 HTML(엑셀 미리보기) | 4 | 8 | `xls-*.html` |
| `login.html` | 1 | 2 | 사이드바 없음 · 로그인 카드 내 워드마크 |
| `assets/template.html` | 1 | 2 | SIDEBAR 슬라이스 원본 |
| `assets/components.html` | 1 | 1 | `<title>`만 |
| **직접 치환 소계** | **35** | **69** | |
| `app.html` | 1 | 3 | `build_app.py` 재빌드로 반영 |
| **HTML 총계** | **36** | **72** | 감사 §3-1 집계와 일치 |
| `build_app.py` `<title>` 하드코딩 | 1 | 1 | `:2332`(감사 기재 `:2266`에서 드리프트) |

부수 처리 2건.

| 파일 | 처리 |
|---|---|
| `assets/base.css` `.sidebar-logo .wordmark em` 주석 | `/* "Admin"/"Investor" 강조부 */` → `/* "Admin" 강조부 */` |
| `review.html:130` | 워드마크 교체 예정을 적어 둔 서술문. 축자 치환하면 문장이 무너지므로 `워드마크는 실제 어드민 표기 PayHug Admin을 따른다.`로 재서술. 감사 §3-4가 치환 제외로 둔 지점이나, 레포 전체 잔존 0건 요건과 충돌해 현재 상태 서술로 바꾸는 쪽을 택했다 |

교체 대상이 아닌 것은 감사 §3-4 그대로 유지한다 — `index.html`·`README.md`·`DESIGN_REF.md`·`archive.html`의 산출물 제목 `PayHug 투자자 어드민 — 화면 설계(안)`, `login.html:50` 부제 `투자자 어드민`, 사이드바 7메뉴.

### 1-2. `README.md` 배포 URL·집계 주장

`git ls-tree -r origin/main` = **15파일**. 공개 주소가 가리키는 사이트에 `app.html`·상태 20종·엑셀 미리보기 4종·설명 문서·`assets/docs/`·`assets/xlsx/`가 없다. 푸시가 금지 범위이므로 주소를 지우는 대신 **공개 범위를 명시**하는 쪽으로 교정한다.

| 위치 | 현재 상태 |
|---|---|
| `README.md:5` | 공개 주소 옆에 `origin/main` 15파일만 응답한다는 사실과 공개 범위(기본 화면 8종 + 랜딩 + `assets/` 4개), 미푸시 목록을 병기 |
| `README.md` 집계 행 | `화면 14 · 상태 20 · 인터랙션 93 · 루트 HTML 35` → `기본 화면 13 · 상태 20 · 통합 프로토타입 1`. 근거 없는 `인터랙션 93`은 삭제. 루트 HTML 절대수는 타 조 산출물 유입으로 계속 움직여 집계에서 뺐다 |
| `README.md` 진입점 표 · 구조 블록 | `화면 14` → `기본 화면 13`으로 일치 |

기본 화면 13의 내역 — 사이드바 7종 + `certificate` + `login` + `xls-*` 4종. 감사 G-28이 지적한 14/13 내부 불일치가 이 값으로 해소된다.

### 1-3~1-4. 프로필 뱃지 `투자자` · `님` 접미

한 문자열이 둘을 함께 물고 있어 단일 치환으로 처리한다.

```
:   <p class="name">㈜테스트인베스트 님 <span class="type-badge">투자자</span></p>
→   <p class="name">㈜테스트인베스트</p>
```

| 대상 | 지점 |
|---|---|
| 화면 HTML(사이드바형) 32 + `assets/template.html` | 33 |
| `app.html` | 1 (SIDEBAR 슬라이스 → 재빌드 반영) |
| **소계** | **34** |
| `assets/base.css` `.sidebar-profile .type-badge` 규칙 | 다른 용처 0건 확인 후 규칙 자체 삭제 |

근거 — 실제 어드민 사이드바 프로필은 `{user.name}` 단독 출력이고 역할 뱃지가 없다(`AdminLayout.tsx:556-575,565`).

### 1-5. PDF 문서번호 `PHI-RA-2026-00NN`

채번 규칙이 A·B·C·R·F 어디에도 없다. `build_docs.py`의 f-string 생성이 유일한 출처다.

| 위치 | 처리 |
|---|---|
| `build_docs.py:332` | `<p class="docno">문서번호 PHI-RA-{mid} · 가맹점ID {mid} · 서명일 {day}</p>` → `<p class="docno">가맹점ID {mid} · {name}</p>` |
| `build_docs.py` 제9조 표 | `문서번호 \| 채번 규칙 미정의 — 견본 미기재` 행을 추가해 공백을 드러냄 |
| 재양도합의서 PDF | **16건 재생성** |

실측 파급은 **PDF 16 + 스크립트 1 = 17지점**이다. 감사 §2가 적은 21곳은 `build_agreement`만 문서번호를 인쇄한다는 점을 반영하지 않은 과다 집계다 — 계약서_서명대기 3건과 증명서 1건에는 `PHI-RA`가 애초에 없다.

### 1-6. 재양도합의서 제3조 ③

**실제 계약서 원본 대조 결과.** `4_정산금채권 재양도 합의서_가맹점직계약.docx` 제3조는 표제가 `[채권양도]`이고 **항이 2개**다.

| 원본 항 | 내용 요지 |
|---|---|
| 1 | 양도인이 양수도 계약서에 따라 양도자산을 양수인에게 양도했고, 양수인이 재양수인에게 재양도하는 데 동의 |
| 2 | 양수인이 양수와 **동시에** 양도자산을 재양수인에게 양도하는 데 동의 |

같은 폴더에 총판본 재양도합의서는 존재하지 않는다(총판본은 `2_이용계약서`·`3_양수도 계약서`만 있다). PDF가 인용한 ③ `별도의 이행행위 없이 동일 시점에 효력이 발생한다`는 직계약본에 대응 문언이 없다. PDF 스스로 당사자표에 `직계약 3자본 기준`이라 적어 두었으므로 ③을 뺀다.

| 위치 | 처리 |
|---|---|
| `build_docs.py` `CLAUSES` 제3조 | 표제 `(재양도의 시기)` → `(채권양도)`, `<li>` 3개 → **2개**, 근거를 계약서 원본 경로로 재지정 |
| `build_docs.py` `CLAUSES` 제1조 근거 | `제3조②` → `제3조 2항(동시 이행)` + 원본 경로 |
| `build_docs.py` 서명대기 제2부 제2조 | `별도의 이행행위 없이 동일 시점…` 문언 삭제, 근거 `제3조②③` → `제3조 2항` |
| PDF | **19건 재생성** (재양도합의서 16 + 계약서_서명대기 3. 증명서에는 `CLAUSES`가 없다) |

### 1-7. `하나은행 인증서`

하나은행은 락계좌·지정계좌 은행(`archive_02:97`)이지 인증서 발행기관이 아니다. 실존 기관에 없는 역할을 부여한 표기라 예시 데이터 예외에 해당하지 않는다.

| 파일:위치 | 교체값 |
|---|---|
| `acquisition--confirm.html:184` | `선택한 2건의 계약서에 전자서명함. 서명 수단은 확인 대상. 서명 후 취소 불가.` |
| `acquisition--signing.html:205` | `<h3>전자서명 진행</h3>` |
| `acquisition--signing.html:223` | `전자서명 진행 중. 서명 수단 확인 대상.` |
| `build_app.py:872,884,900` | 상동 3건 → `app.html` 재빌드로 반영 |
| `build_docs.py` 서명 수단 주석 | `화면은 전자서명을 전제하되 수단을 지정하지 않는다(정산채권 양수 · 서명 진행 상태). 수단은 확인 대상이다.` → 계약서_서명대기 PDF **3건 재생성** |

### 1-8. 계약기록 표 `서명일` 열 · `검증 완료` 뱃지

PPT 슬라이드16 표는 `MID · 가맹점 · 재양도합의서` 3열이다. `서명일` 열은 규정이 없고, `검증 완료`라는 결과 단정은 같은 레포 `certificate.html:327`의 `미포함 — 확인 대상`과 정면으로 어긋난다.

| 대상 | 처리 |
|---|---|
| `contracts.html` · `--all` · `--downloaded` | `<th>서명일</th>` 3 · 날짜 `<td>` 24 삭제 → 헤더 5열 / 행 5셀로 일치 |
| 같은 3파일 | `badge-green 검증 완료` 24건 → `badge-gray 확인 대상` |
| `build_app.py:1819,1827` | 정렬 헤더 `th('ct-tbl','signed','서명일')` 제거 · 뱃지 중립화 → `app.html` 재빌드로 반영 |
| `build_docs.py` | 화면에서 사라진 열을 긁던 `read_signed()` 전체와 호출부·인자 제거. PDF 제9조 표의 `서명일` 행은 `견본 문서 — 미기재`로 중립화(같은 표의 `서명값`·`검증 회신전문` 처리와 동일 형식) |

`contracts--empty.html`은 표 자체가 없어 대상이 아니다.

### 1-9. 준하는 값 오류 2건

| 대상 | 처리 |
|---|---|
| `전 기기에서 재로그인 필요` | `password.html:121` · `--error:139` · `--weak:155` · `--done:135` + `build_app.py:785,790` = **6지점**. `비밀번호 변경 후 기존 세션 처리는 확인 대상.` / `새 비밀번호로 변경 완료. 기존 세션 처리는 확인 대상.`로 교체. 실제 어드민에 비밀번호 변경 화면 자체가 없어 세션 정책을 단정할 근거가 없다 |
| 엑셀 파일 메타 | `xls-*.html` 4파일의 크기·생성일시 8값을 `assets/xlsx/` 실물의 `st_size`·`st_mtime`으로 교체(`2026-08-27 17:28` · `5.7/6.5/5.4/5.8 KB`). `가맹점별_투자자산`은 크기 표기도 `6.0 KB` → `6.5 KB`로 어긋나 있었다. `build_app.py`의 `XLSX` 상수는 고정값을 버리고 **빌드 시 실물에서 산출**하도록 바꿔 재발을 막는다 |

---

## §2 오분류 3건 복원

선행 조가 스토리보드를 **텍스트 프레임만** 읽어 판정한 결과다. 슬라이드 10·12 삽입 이미지에 세 요소가 모두 실재한다. 화면 쪽은 URL·조회 항목·로그인 수단 문자열이 그대로 살아 있어 **복원할 것이 없고**, 정정 대상은 이를 임의 추가로 적어 둔 판정 문서다.

### 2-1. We-bank URL `https://www.we-bank.co.kr/main_00100.act`

근거 — PPT 슬라이드10 삽입 이미지(`image6.png`) 브라우저 주소창. 경로 `main_00100.act`까지 축자로 일치.

| 파일:위치 | 정정 |
|---|---|
| `artifact_gap.md` §1 표 11행 | `외부 URL 존재하나 근거 없는 임의 추가로 분류된 상태` → 슬라이드10 삽입 이미지 주소창 근거 병기 · 처리안 `스토리보드가 지정한 목적지 그대로 유지` |
| `artifact_gap.md` §1 산문(외부 시스템 이동) | 무근거 서술 → 이미지 근거 서술. 텍스트 프레임만 읽으면 근거가 보이지 않는다는 점을 함께 적음 |
| `artifact_gap.md` §3 | `임의 URL을 넣지도 바꾸지도 않았다. 확인 대상 표기 그대로 유지.` → 이미지 주소창 인용 사실로 재서술 |
| `artifact_gap.md` §5 | `근거 없는 임의 추가로 분류된 상태다` → 근거 있음 · 실물을 만들지 않는 사유는 목적지가 외부 콘솔이라는 점으로 재서술 |
| `capability.html` 쿠콘 #16 행위 주석 | `화면에 인쇄된 이동 주소 문자열은 스토리보드에 없다` → 슬라이드10 삽입 이미지 주소창 축자 존재 |
| `capability.html` 확인 질문 4-11 | 무근거 전제 삭제, 남은 미결을 **계정 스코프**로 한정. 질문 말미 `이동 주소를 화면에 노출합니까?` 삭제 |
| `capability.html` 판정 변경표 `쿠콘 이동 주소의 근거` | `근거 없는 임의 추가` → `근거 있음` |
| `capability.html` 강등표 `쿠콘 #16 외부 이동` | `URL 근거 삭제 · 임의 추가로 분류` → 요구·URL 근거 모두 유지 |
| `capability_manuscript.md` 3행 + 4-11 문항 | 위와 같은 취지로 원고에도 반영 — 재조판 시 오분류가 되살아나지 않게 함 |

`capability.html`의 `We-bank 문자열 레포 전체 0건` 서술은 (B) 기준으로는 참이므로 문장을 지우지 않고 **운영 어드민 레포 기준**임을 한정한 뒤, 주소·로그인 수단·조회 메뉴의 근거는 코드가 아니라 스토리보드 삽입 이미지 쪽에 있다는 문장을 덧댔다.

### 2-2. `가상계좌 입금내역조회`

근거 — PPT 슬라이드12 삽입 이미지(`image8.png`) 좌측 트리 `일반조회 > 입금내역조회 · 송금내역조회 · 정산내역조회`.

판정 자체는 `capability.html`에서 이미 `확정`이었고 강등도 없었다. 출처 칸이 `SB s11`·`s12`·`s13` 캡션만 가리켜 이미지 근거가 드러나지 않던 상태라, `capability.html`·`capability_manuscript.md` 두 곳의 출처를 **슬라이드12 삽입 이미지 조회 트리 + 캡션** 병기로 바꿨다.

### 2-3. `기관코드`·`OTP`

근거 — PPT 슬라이드10 삽입 이미지 로그인 폼 라벨 `사용자ID` · `비밀번호` · `기관코드` · `OTP No.`.

판정 `확정` 유지. 출처를 `SB s11`에서 **슬라이드10 삽입 이미지 로그인 폼 라벨**로 교체(`capability.html`·`capability_manuscript.md`).

---

## §3 재검증

전 검증기를 창 없이(`--headless=new`) 그대로 돌렸다. 기준은 정정 착수 직전 같은 명령으로 뜬 값이다.

| 검증기 | 기준 | 정정 후 | 판정 |
|---|---|---|---|
| `verify_app.js` | 71 PASS / 0 FAIL | 메뉴 7 · 상태 20 · 다운로드 4 · 데이터 6 · 레이아웃 34 = **71 PASS / 0 FAIL** | 동일 |
| `verify_app.js` 죽은 컨트롤 | 0 / 215 | **0 / 213** | 0 유지 · 검사 모수 2 감소 |
| `verify_app.js` a11y 미도달 | 0 | **0** | 동일 |
| `verify_app.js` 콘솔 에러 | 0 | **0** | 동일 |
| `verify_identity.js` | 항등식 14 PASS · 비중 합 100.0% | **14 PASS / 0 FAIL** · `ratioSum 100` | 동일 |
| `verify_crossscreen.py` | 23건 일치 | **23건 · 불일치 0** | 동일 |
| `verify_links.py` | 80건 200 · 바이트 일치 | **80건 · FAIL 0 · 전건 200 · 바이트 일치** | 동일 |
| `verify_toast.js` | 10건 · 실물 동반 | **10건 · FAIL 0 · 콘솔 에러 0** | 동일 |
| 워드마크 잔존 | — | `PayHug Investor` **레포 전체 0건**(Figma 제외) | 신규 |
| `님`·`type-badge` 잔존 | — | **0건** | 신규 |
| `PHI-RA` 잔존 | — | **0건**(HTML·PDF·스크립트 전량) | 신규 |

검사 모수 215 → 213은 계약기록 `서명일` 정렬 헤더 2개가 사라진 결과다. 죽은 컨트롤은 0을 유지한다.

`verify_links.py`의 로컬 링크 참조 수는 427 → 428로 늘었다. 타 조가 작업 중 생성한 `inquiry.html`이 링크 1건을 더한 것이며 고유 링크 58개와 실측 80건은 그대로다.

`verify_toast.js`의 zip 바이트는 `609,256` / `3,260,815`로 바뀌었다. PDF 재생성분을 다시 묶은 결과이고 디스크·응답 바이트가 일치한다.

### PDF 실물 확인

재생성 22건(PDF 20 + zip 2) 전량을 열어 본문 텍스트를 대조했다.

| 검사 | 결과 |
|---|---|
| `PHI-RA` | PDF 20건 전량 **0건** |
| `하나은행` | PDF 20건 전량 **0건** |
| `별도의 이행행위 없이 동일 시점` / `제3조②③` | PDF 20건 전량 **0건** |
| `제3조 (채권양도)` 2항 구성 | 재양도합의서 16 + 계약서_서명대기 3 = **19건 수록** |
| 직계약본 근거 인용 | 같은 19건에 `재양도 합의서(가맹점직계약본) 제3조 1·2항` 수록 |
| 문서번호 행 | 재양도합의서 16건이 `채번 규칙 미정의 — 견본 미기재` |
| 서명일 | 날짜값 0건. 표 행은 `견본 문서 — 미기재` |
| 지면 확인 | `재양도합의서_M2026-0001.pdf` 1·2면, `계약서_서명대기_M2026-0001.pdf` 4면을 렌더해 표제·표·서명란 배치 이상 없음 |

### 화면 실물 확인

`app.html#contracts/default` 헤드리스 캡처로 워드마크 `PayHug **Admin**`(`Admin`이 `primary-400`), 프로필 `㈜테스트인베스트` 단독, 계약기록 5열(`서명일` 없음)·`검증` 열의 회색 `확인 대상`, 사이드바 7메뉴 유지를 확인했다.

---

## §4 Figma 재임포트 필요 노드

파일 `Tcf69tIciGxmlqCIuRb0iI` · 페이지 `3066:328`. 이번 정정은 코드·문서 범위이며 **Figma 쓰기 0건**이다. 아래 노드가 현재 HTML과 어긋난 상태로 남는다.

### 4-1. 워드마크 (8프레임 전량)

`PayHug <em>Investor</em>` → `PayHug <em>Admin</em>`. 인라인 2색(흰색 + 녹색) 텍스트 노드이므로 문자열 교체 후 **녹색 구간을 `Admin` 5자에 맞춰 재지정**해야 한다(`Investor` 8자 기준으로 잡혀 있다).

| # | 프레임 | 프레임ID | 워드마크 노드ID |
|---|---|---|---|
| 1 | 01 투자 자산 | `3070:2` | `3070:535` |
| 2 | 02 투자자산 증명서 | `3069:2` | `3069:299` |
| 3 | 03 투자 수익 | `3068:2` | `3068:471` |
| 4 | 04 쿠콘 관리 현금 | `3072:2` | `3072:70` |
| 5 | 05 가맹점 | `3071:2` | `3071:305` |
| 6 | 06 정산채권 양수 | `3073:2` | `3073:92` |
| 7 | 07 계약기록 | `3075:2` | `3075:256` |
| 8 | 08 비밀번호 변경 | `3074:2` | `3074:53` |

### 4-2. 나머지

| 대상 | 프레임 | 처리 |
|---|---|---|
| 프로필 `㈜테스트인베스트 님` + `투자자` 뱃지 | 8프레임 전량 | `님` 삭제 · 뱃지 노드 삭제. 뱃지가 빠지면서 프로필 행 폭이 남으므로 정렬 확인 필요 |
| 서명 모달 `하나은행 인증서로 서명함` / `하나은행 인증서 서명` | `3073:2` | `전자서명함. 서명 수단은 확인 대상.` / `전자서명 진행` |
| 계약기록 표 `서명일` 열 · `검증 완료` 뱃지 | `3075:2` | 열 1개 제거 후 남은 5열 폭 재배분 · 뱃지를 회색 `확인 대상`으로 |
| `전 기기에서 재로그인 필요` | `3074:2` | `기존 세션 처리는 확인 대상.` |
| 로스터 불일치 | 8프레임 | Figma는 가맹점 8건·투자실행액 `1,284,500,000`, HTML은 16건·`1,523,100,000`. 이번 정정 범위 밖이며 감사 G-31로 이미 등재 |

---

## §5 미해소

| # | 항목 | 상태 |
|---|---|---|
| 1 | Figma 8프레임 | §4 전량 미반영. 코드·Figma 표기가 갈라진 상태 |
| 2 | `capability.html:772,1428`의 `하나은행 인증서` 언급 | **의도적 유지.** 설계안 문구를 임의 추가로 판정한 감사 서술 자체이고, 감사 G-35가 이 판정을 유효로 확인했다. 화면·PDF에서 문구가 사라진 지금은 "정정 완료된 과거 지적"으로 읽히므로 원고 개정 트랙에서 시제를 손볼 여지가 있다 |
| 3 | `inquiry.html:437` `현재 전 행 '검증 완료' 예시값` | 타 조 제작 중 파일이라 손대지 않았다. 계약기록 뱃지가 `확인 대상`으로 바뀌어 이 서술이 화면과 어긋난다 — **해당 조에 인계 필요** |
| 4 | `capability_manuscript.md` `임의 추가 \| 계약기록 서명일·검증 열` 행 | 판정은 그대로 유효하다(근거 없음). 열을 실제로 걷어냈으므로 원고 개정 시 처리 완료 상태로 옮겨 적을 대상 |
| 5 | `_pipeline/investor_admin/capability.html` 스냅숏 | 레포 배포본만 정정했다. 파이프라인 보관본은 18:06 상태 그대로이며, 재조판은 `capability_manuscript.md`(정정 반영 완료)에서 나오므로 되살아날 경로는 없다 |
| 6 | `README.md` 공개 주소 | 푸시가 금지 범위라 공개 범위 명시로 처리했다. 푸시 후에는 이 문단을 걷어내는 것이 맞다 |
| 7 | 감사 §2의 `PHI-RA` 파급 21곳 | 실측 17곳(PDF 16 + 스크립트 1). 감사 집계가 과다 |
| 8 | 감사 §5 행 번호 | `build_app.py` `<title>`이 `:2266` → `:2332`로 드리프트. 다른 지점도 타 조 동시 작업으로 어긋나 전건 재조회 후 적용 |
| 9 | 감사 §5-5·§5-6·§5-7 잔여 | 요율 표기(`0.11%` 예정 병기 · `VAT 면제`→`면세` · `연 12%` 판단 대기), 대표 정의서 반영(`입금일자`→`정산예정일` · `Ty수익율` 한정어 · 상환액 산식), 비밀번호 규칙 `8~16자`, `이번달`→`이번 달` — **이번 지시 범위 밖**이라 손대지 않았다 |
| 10 | 감사 §5-8 설명 문서 정정 | `glossary.html`·`DESIGN_REF.md`·`build_archive.py` 폐기 트랙 등재 등 — 원고 개정 트랙 몫 |

---

## 산출·변경 파일

`build_archive.py`를 마지막에 돌려 `archive.html`을 갱신했다. **커밋·push 0건**, `HEAD` = `5ccfa3f` 그대로.

| 경로 | 내용 |
|---|---|
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/fabrication_fix.md` | 이 문서 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/build_app.py` | 워드마크 · 하나은행 3 · 세션 문구 2 · 계약기록 표 2 · XLSX 메타 실측 산출 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/build_docs.py` | 제3조③ · 문서번호 · 하나은행 · `read_signed()` 제거 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/artifact_gap.md` | We-bank 오분류 4곳 |
| `/Users/semi/cursor/payhug/payhug-spec/_pipeline/investor_admin/capability_manuscript.md` | 오분류 3건 원고 반영 6곳 |
| `/Users/semi/cursor/payhug-investor-admin/` 루트 HTML 35 + `assets/template.html` · `components.html` · `base.css` | §1 정정 |
| `/Users/semi/cursor/payhug-investor-admin/app.html` | `build_app.py` 재빌드 산출 |
| `/Users/semi/cursor/payhug-investor-admin/capability.html` | §2 오분류 복원 |
| `/Users/semi/cursor/payhug-investor-admin/README.md` | 배포 URL·집계 교정 |
| `/Users/semi/cursor/payhug-investor-admin/assets/docs/` | PDF 20 + zip 2 재생성 |
| `/Users/semi/cursor/payhug-investor-admin/archive.html` | `build_archive.py` 재생성 |
