# 미확정 및 미팅 협의사항

> 이 문서가 미팅 내용을 담는 가장 중요한 문서다.
> Claude Code는 이 문서의 질문을 임의로 확정하지 않는다.

## 1. 지애 매니저와 확인할 내용

### 용어

- 미지급금과 선정산 제외금액은 다른 개념인가? (이체 내역의 회수 2단계가 경유하는 '미지급계정'은 또 다른 대상 — 이름이 비슷할 뿐이므로 동일시 금지, §7-4)
- 과지급과 미회수금은 발생액과 잔액으로 구분하는가?
- 자체환급분은 플랫폼 자체 환급을 의미하는가?
- 바로이체 대상금액과 바로이체 미지급의 정확한 차이는 무엇인가?

### 매출대사 엑셀

- 각 칼럼의 정의
- 금액 출처
- `+ / -` 의미
- 지급·차감·회수 방향
- 자동·수동 처리 구분
- 완료 여부 확인 위치
- 마지막 시트 자체환급 리스트의 사용 목적
- 결산자료 및 매출내역 필터 기준

### 플랫폼 예외

- 우리가게클릭비 일반 비용과 프로모션 환급 분리 가능 여부
- 하나의 항목에 혼합된 경우 세부 처리 또는 순액 처리 여부
- 가게배달 음수 발생 원인
- 환급 미지급 8건의 플랫폼·품목·원인
- 수동이체 후 완료 기록
- 쿠팡이츠 과지급 재대사 기간
- 카드 미정산금과 플랫폼 과지급의 합산 관리 여부

### 회수

- 회수건 생성 주체
- 자동 생성 여부
- 회수 우선순위
- 차기 지급 상계 여부
- 부분·분할 회수 여부
- 회수 완료 검증 기준
- 회수 실패·보류·취소 처리

> 8/15 회의에서 과지급 회수 기능을 조기 구현하는 방향이 잡혔고(V1), 위 일곱 문항이 곧 그 기능의 정책 공백이다. 대상·절차·화면 상세는 운영 대사 확인과 함께 수립 예정이며 아직 미수립 (확인필요). 근거: 회의 8/15 U5·V1(최지원 00:43:18 · 이정규 01:28:14). 회수 기능이 없어 이미지급 버튼이 대용으로 쓰이는 문제는 §7-9.
>
> 회수 금액 기준 한 가지는 서 있다. 선정산 지급 후 원거래가 취소되면 순지급액 전액을 회수하고 채권매입수수료는 환급하지 않는다(`PAYHUG-260 본문`, 03 §8). 다만 회수 실행 수단·우선순위·실패 처리는 이 티켓에도 없어 위 일곱 문항이 그대로 남는다.

## 2. 정규 매니저와 확인할 내용

### 가맹점 UI

- 요청 UI가 가맹점 화면만인지 운영 어드민도 포함인지
- 예상 지급 차액 정의 — 8/15 회의 미언급으로 C4 미확정 유지. 같은 회의의 어드민 엑셀 '예상 수수료' 열 요구는 운영 대사용 항목 요청이지 이 가맹점 노출 라벨의 정의가 아니다 (회의 8/15 U10·V8, 최지원 01:43:22)
- 메인 화면 필수 항목
- 상세 화면 필수 항목
- 환급과 회수 별도 노출 여부
- 내부 사유 공개 범위
- 플랫폼별 상세와 합산 화면 필요 여부
- 완료된 조정 이력 조회 여부

### 알림톡

- 실제 사용 중인 템플릿
- 필수 발송 케이스
- 전체 발송과 조건부 발송 기준
- 플랫폼별 개별 발송 또는 합산
- 환급·추가 지급·회수 알림 여부
- 링크 이동 화면
- 이미지형 템플릿 필요 여부
- API 테스트 일정과 기획 전달 마감일

## 3. 팀장 또는 책임자와 확정할 내용

- 바로이체 입금예정일 당일 지급 원칙 — 8/15 회의는 운영 시각(하루 2회)만 재확인했고 계약 '매일'과의 정합은 논의되지 않음. C2 미확정 유지 (회의 8/15 U9)
- 익일까지 지급 허용 여부
- 주말·공휴일 기준
- 회수금과 추가 지급금 상계 가능 여부
- 회수 어드민 1차 개발 범위 — 8/15 회의에서 조기 구현 방향 합의(V1). 도입 시 이미지급 버튼은 제거하고 태그·상태값은 영구 유지가 확정(D12)이므로, 1차 범위에 과거 이미지급 건 정리를 넣을지 함께 정해야 한다
- 자동 회수 포함 여부
- 가맹점 UI와 알림톡 개발 범위
- 최종 정책 승인자

## 4. 문서 자체 이슈 (2026-07-15 이식 시 발견)

- ~~01_SERVICE_OVERVIEW.md 원문 유실~~ → 2026-07-15 원문 재공급으로 복구 완료
- 00_README.md 화면 검토 원칙 8번 항목 원문 유실 (여전히 미복구)

## 5. 문서 정합성 검토에서 발견된 확정 필요 사항 (2026-07-15)

- "추가 지급"이 3가지 의미로 혼용: ① 당일 산식 합산 항목(03) ② 별도 미래 지급분(바로이체 등) ③ 과지급의 가맹점 노출 문구(05 §5) — 의미 확정 필요
- 미지급금의 가맹점 노출 용어가 05 §5 매핑 표에 없음
- 취소·환불·추가 매입·프로모션 환급은 02 용어집에 정의 없음
- "이전 지급 조정금액"(05 메인)이 미회수 잔액인지 당회 상계액인지 미정
- 가게배달 음수(04 §2)·쿠팡이츠 정산 출금(04 §5)의 화면·알림톡 노출 방식 미정의
- "지급 실패" 상태의 화면 문구 부재 (알림톡 케이스에는 존재)
- 플랫폼 환급 vs 자체환급분이 가맹점 용어에서 "플랫폼 환급금" 하나로 합쳐짐 — 동일 개념인지 확정 필요

## 6. 최신 코드(develop 2026-08-10/08-08) 대조에서 추가된 확정 필요 사항 (2026-08-12)

- **가맹점 배달 상세의 '환급액'(refundAmount, 08-05 신설)과 6대 개념의 '환급' 매핑**: 코드 주석은 "플랫폼이 지급한 환급 가산(CS환급/조정, 프로모션 순환급 등)" — 정책 '환급'(페이허그→가맹점)과 동일 개념인지 미확정. 같은 낱말이 두 갈래로 쓰이므로 02 용어집에 "플랫폼 환급" 구분 표기 여부 결정 필요. (근거: merchant services/salesService.ts:317-318)
- **이체 내역 탭의 '회수(COLLECTION)'**: 쿠콘 이체 기록상 분류(출금+입금 페어) — 어드민 '매출 회수'(락계좌→모계좌 스윕)인지 정책 '과지급 회수'인지 데이터 정의 대조 필요. (admin TransferRecordsTab.tsx:20)
- **총판(참여자) 데이터 스코프**: 가맹점 수·선정산 통계 API에 총판 구분 파라미터가 없음 — 서버가 토큰 기준으로 소속 가맹점만 내려주는지 확인 필수(타 총판 정산 규모 노출 리스크). (admin hooks/useDashboardData.ts:49-70)
  - 회원 목록 API(`GET /admin/partners`)도 같은 패턴이다. 요청 파라미터는 유형 하나뿐이고 스코프 인자가 없으며, 프론트 주석은 서버가 토큰 기준으로 "자기+하위"만 내려주는 것을 전제한다. 같은 전제가 두 API에 걸쳐 있어 한 번에 확인해야 한다. (admin `services/partnerService.ts:52-55`, `app/partners/page.tsx:694-695`·`:866-868`)
- **계약 프로세스 초기화 모달 권한**: 버튼에 isAdmin 가드가 없어 총판에게도 노출되는 것으로 읽힘 — 서버 측 권한 검증 여부 확인 필요. (admin merchants/[id]/page.tsx:780-796)
  - 회원 관리 화면도 같은 구조다. 프로필·삭제만 isAdmin으로 가려 두고 수정 버튼·비활성화 버튼·상태(활성/비활성) 선택은 참여자 전원에게 노출되며, 차단은 서버 거부에만 기댄다. 노출 범위를 화면에서 좁힐지 서버 검증만으로 둘지 함께 정해야 한다. (admin `app/partners/page.tsx:1398-1427`, `:1012-1024`)
- **미정산 누락 '이틀 이상' 규칙**: 판정 기준 시각이 지급 캘린더(C2)와 연동되는지 확인 필요.
- **`/settlements/[id]/fee-adjustments` 고아 라우트**: 진입 링크 0건 + '원래 선정산' 링크가 존재하지 않는 라우트로 이동(404) — 화면 존치/폐기 결정 필요.

## 7. 2026-08 확정 필요 사항 (라운드3 이후 누적)

7-1~7-11의 출처는 두 갈래다. 하나는 8/15 정산 회의(`_pipeline/enhance_202608/round3/meeting_changes.md`)의 미결 10건, 다른 하나는 정책서 신판 재조판 검수(`round3/qa_r3_final.md`)에서 정책 판단이 서야 원고를 고칠 수 있어 의도적으로 손대지 않은 5건이다. 전부 `확인필요`이며 임의 확정 금지.

8/15 미결 중 U5(과지급 회수 상세 정책)는 §1 '회수'·§3에 같은 질문이 이미 있어 그쪽에 근거만 보강했고, U8·U9·U10은 C1·C2·C4로 이미 등재돼 있어 아래에 다시 세우지 않는다.

### 7-1. 차액 산식의 적용 범위 — 취소 환수·미회수 이월에도 같은 식을 쓰는가

- **질문** — `건별 차액 = 예상수수료 − 실제 수수료` 한 줄을 유형 8종 전부에 적용하는가, 취소 환수·미회수 이월은 예외로 두고 ※ 주석을 붙이는가.
- **배경** — 차액 원장은 발생 원인 4종(매입 확정 차이·예상매출 대사·취소 환수·미회수 이월)을 한 곳에 담는다. 그런데 취소 환수는 매출 자체가 사라진 건이고 미회수 이월은 다른 선정산에서 넘어온 잔액이라 수수료 차이로 설명되지 않는다. 산식을 장 머리에 단정으로 두면 이 두 유형에서 값이 맞지 않는다. (가설)
- **위치** — 정책서 신판 [차액 정산] §2① 정의·유형 표, [차액 정산 내역] case5 / 어드민 정산 현황 '차액 정산' 탭 목록 '차액' 열
- **영향** — 예외를 인정하면 산식 한 줄로 8종을 설명하던 구조가 깨져 유형별 산출 근거를 따로 써야 한다. 예외 없이 간다면 취소 환수·미회수 이월 금액의 서버 산출 방식을 확인해 근거를 채워야 한다.
- **출처** — QA `qa_r3_final.md` §6 정책 판단 1 · 원고 `dsl_delta.md` [차액 정산] §2① · 회의 8/15 D14(취소 환수 = 주문 취소 + 승인 취소)
- **연계** — 「수수료 차액」이라는 낱말 자체가 건별 상세·일자별 요약·배치 상세 세 자리에서 다른 대상을 가리킨다(02 §9, `IFX4GRC60ibOVCWNrd6VQt` `3754:1408`). 산식 적용 범위를 정할 때 어느 자리의 값인지 먼저 갈라야 한다.

### 7-2. 순수익 정의 상충 — 전달금 부가세를 두 번 빼는가

- **질문** — `순수익 = 수익 + 부가세 − 전달금`인가, `순수익 = 수익 + 부가세 − 전달금 − 전달금 부가세`인가.
- **배경** — 원장 열 스키마는 전달금을 "부가세 포함 표기"로 정의하고 순수익에서 전달금만 뺀다. 반면 전달금 재귀속 절의 항등식은 전달금과 전달금 부가세를 따로 뺀다. 두 식이 같은 지면에서 쓰이면 부가세가 두 번 빠진다. 서버 정의 미확인이라 어느 쪽이 맞는지 판정할 근거가 없다. (확인필요)
- **위치** — [정산 상세] §4① 원장 한 행 / [수수료 원장] §1① 열 스키마 · §6 계산식 / 어드민 수수료 원장·배분 시뮬레이션
- **영향** — 검산 예시(EX)와 원장 표 수치가 함께 움직인다. 두 식이 재귀속 전후로 각각 맞는 것이라면 각 식에 적용 시점을 명시해야 하고, 한쪽이 틀린 것이라면 정책서 산식과 화면 표 중 어디를 고칠지 정해야 한다. `수익 = 매출 − 원가` 여부도 같은 자리에서 미확인 상태다.
- **출처** — QA §6 정책 판단 2 · 원고 `dsl_delta.md` [정산 상세] §4① · [수수료 원장] §6

### 7-3. 차액 원장 적재 범위 — 선정산 시작일 이전 거래를 싣는가

- **질문** — 거래일이 선정산 시작일 이전인 거래의 플랫폼 차감·환급을 차액 원장에 건으로 적재하는가, 원장 자체를 만들지 않는가.
- **배경** — 8/15 회의는 이 거래분을 지급액에 가감하지 않는다는 것까지만 확정했다(개발 반영 완료). 회의 발언은 "표시는 하되 지급에 가감되지 않게 조치"였는데 조판 원고 첨삭은 "플랫폼 차감·환급 원장 미발생"으로 적혀 있어, 표시 대상인지 미발생인지가 갈린다. (확인필요)
- **위치** — [선정산 결과] §2④ · §4 case11 / [차액 정산] §2① / Flow [선정산 지급 흐름] 시작일 분기 / 화면설계서 2705 심화 4·10
- **영향** — 적재한다면 차액 정산 탭 집계·엑셀에 지급액과 연결되지 않는 건이 섞이므로 별도 유형이나 제외 표식이 필요하다. 적재하지 않는다면 운영이 대사할 때 대조할 화면 근거가 사라진다.
- **출처** — QA §6 정책 판단 3 · 회의 8/15 D1(이정규 00:02:19 · 슬랙 결론 00:45:08) · 원고 `dsl_delta.md` 첨삭 CAP

### 7-4. 미지급계정의 실체 — 은행 계정인가 내부 계정인가

- **질문** — 매출 회수 2단계가 경유하는 '미지급계정'이 은행에 실재하는 계좌(가상계좌 등)인가, 장부상 내부 계정인가.
- **배경** — 이체 내역의 회수 기록은 락계좌 → 미지급계정 출금과 미지급계정 → 모계좌 입금 두 행으로 남는데, 이 계정의 실체가 화면·코드 어디에도 정의돼 있지 않다. 이름이 비슷해 6대 개념의 '미지급금'과 동일시될 위험이 크다. 정책서에서 빨강 유지 중. (확인필요)
- **위치** — [이체 내역·VOC] §2③ 자금 경유 계정 구조 / 어드민 정산 현황 '이체 내역' 탭 회수 2단계 펼침 / 02 용어집 '미지급금' 행
- **영향** — 은행 계정이면 잔액 대사 대상이 되어 회수 완료 판정 기준이 계좌 잔액으로 서고, 내부 계정이면 기록 대사만 남는다. 어드민 '매출 회수'(락계좌→모계좌 스윕)와 정책 '과지급 회수'의 경계 서술도 여기에 따라 달라진다.
- **출처** — QA §6 정책 판단 4 · 원고 `dsl_delta.md` [이체 내역·VOC] §2③ · 코드 admin `TransferRecordsTab.tsx:24-100`(회수 짝 매칭) — §6의 '회수(COLLECTION)' 분류 항목과 같은 뿌리

### 7-5. 단계 번호 원문자 표기 통일 — 회수 2단계·VOC 2단계

- **질문** — 절차의 단계 번호를 ①② 원문자로 통일하는가, 절 번호에만 원문자를 쓰고 단계는 아라비아 숫자로 두는가. 함께 걸린 자구 = '배치당'(Flow) ↔ '정산 배치당'(본문) 통일 여부, "정산일 없는 묶음"이 정산 배치를 가리키는지 여부.
- **배경** — 정책서 신판은 절 번호에 ①②③을 쓰는데, 회수 2단계(1 출금 / 2 입금)와 VOC 2단계(1 조회 / 2 다운로드)만 아라비아 숫자다. 같은 지면에 두 체계가 섞여 절과 단계가 구분되지 않는다.
- **위치** — [이체 내역·VOC] §2③ · §4① / 화면설계서 2707 / 어드민 이체 내역 탭 회수 펼침 라벨
- **영향** — 표기 규약이라 금액·로직에는 닿지 않지만, 재조판 규격서(`round2/format_recipe4.md` §3.10)에 기준을 못 박지 않으면 다음 재조판에서 다시 갈린다. 화면 라벨 원문을 그대로 쓰는 규칙과 부딪힐 수 있어 화면 문구 확인이 앞서야 한다.
- **출처** — QA §6 정책 판단 5 · 같은 문서 '원고 결정 필요 자구 3건'

### 7-6. 운영 대사 절차와 어드민이 지원해야 할 정보 (회의 U1)

- **질문** — 운영 담당자(GM)가 정산 대사를 어떤 절차로 하며, 그 절차를 어드민 어느 화면에서 무슨 정보로 받쳐야 하는가.
- **배경** — 시작일 이전 거래 차감 문제도 운영 대사에서 금액 차이로 먼저 드러났다. 그런데 기획·개발 모두 대사 절차를 모르는 상태이고(수작업 3시간 언급), 차이를 짚어내는 지점이 곧 회수 정책의 감시 지점이라는 합의만 있다. 담당자 청취 전. (확인필요)
- **위치** — [선정산 결과] §6 미확정 / 신설 예정 [과지급 회수] 장 운영 절차 절 / 이 문서 §1 '매출대사 엑셀' 문항과 한 뿌리
- **영향** — 확정되면 어드민 엑셀 열 구성(예상 수수료 열 추가 요구 포함)과 회수 감시 지표가 함께 정해진다.
- **출처** — 회의 8/15 U1(이정규 00:14:49) · V7 · 액션 A3·A14(GM 대사 재수행 · 의견 청취)

### 7-7. 매입 취소 차감이 실릴 항목과 로직 (회의 U2)

- **질문** — 지급 완료 뒤 매입 단계에서 취소된 건의 차감을 차액 8종 중 어디에 싣는가, 새 유형을 만드는가.
- **배경** — '매입 취소'는 현 시스템에 개념 자체가 없다는 것이 8/15 확정이다. 실제 사례가 확인돼 로직 개발이 예정됐을 뿐이고, 사례 자료 없이 항목을 미리 만들지 말자는 합의가 있다. (확인필요)
- **위치** — [차액 정산] §2① 유형 표 · §6 미확정 / [선정산 결과] §3 산식 주석
- **영향** — 새 유형을 세우면 대분류 카드·필터·엑셀 열이 함께 늘고, 기존 '취소 환수'에 합치면 주문 취소·승인 취소와 발생 시점이 다른 건이 한 유형에 섞인다.
- **출처** — 회의 8/15 U2(이정규 01:39:15) · V2 · A5(사례 기반 티켓 반영) · `PAYHUG-189 본문`(승인취소·매입취소 어드민 반영 범위 4건을 정하면서 처리 분기는 설계서 노드 `IFX4GRC60ibOVCWNrd6VQt` `4465-680` 참조로만 두고 회수 금액 산정 기준을 적지 않음, 진행 중) · `PAYHUG-194 본문`(승인취소·주문취소·부분취소·매입취소 4종의 표기 어휘가 "고민 필요" 상태)
- **연계** — 취소 4종의 표기 어휘는 §7-19의 미대상 사유 어휘와 같은 자리에서 만난다. `PAYHUG-186`의 사유 `취소`가 이 4종을 한 배지에 담기 때문이다.

### 7-8. 기록용 수수료의 계산식과 면제 수수료와의 관계 (회의 U3)

- **질문** — 기록용 수수료를 어떻게 계산하며, `선정산 수수료 − 기록용 수수료 = 면제 수수료`가 성립하는가.
- **배경** — 수수료 면제 가맹점은 실제 수수료가 0원이어도 원래 값을 기록용으로 남긴다. 앞으로 기간 한정 면제·항목별 면제가 생길 수 있다는 전제로 값을 따로 두기로 했으나, 산식은 개발이 확인 후 전달하기로 하고 회의에서 확정되지 않았다. (확인필요)
- **위치** — [선정산 결과] §4 case10(수수료 면제) · §6 / [정산 공통 용어] §2 '수수료 면제' 행
- **영향** — 산식이 서면 면제 사례의 검산 예시를 쓸 수 있고 어드민 예상 수수료 열 요건과도 맞물린다. 다만 여기서 나오는 값은 요율 근거가 아니다 — C1과 무관하게도 값 채택 금지.
- **출처** — 회의 8/15 U3(최지원·이정규 01:43:22) · V3 · A13(계산식 확인 후 전달)

### 7-9. 회수 기능 도입 전까지 이미지급 버튼의 회수 대용 사용을 허용하는가 (회의 U4)

- **질문** — 정식 과지급 회수 기능이 나오기 전까지 이미지급 버튼을 상계·회수 대용으로 계속 쓰게 둘 것인가, 지금부터 막을 것인가.
- **배경** — 이미지급은 과거 수동 이체(바로이체)를 원장에만 지급 완료로 남기는 기록 처리이고 실행해도 돈이 움직이지 않는다(8/15 확정). 회수 기능이 없어 이 버튼이 편법으로 쓰이고 있으며, 기능을 남겨 두자는 의견과 지금 막자는 의견이 갈려 팀장 협의가 필요하다. (확인필요)
- **위치** — [미정산 누락] §2② 처리 두 경로 · §6 / 화면설계서 2705 키노트 5(3가지 동작) / 어드민 미정산 누락 배너
- **영향** — 막으면 회수 기능 도입까지 상계 수단이 없어 운영 대응이 수작업으로 남고, 두면 원장과 실제 자금 흐름이 계속 어긋나 CS 소지가 쌓인다. 어느 쪽이든 이미지급 태그·상태값 영구 유지는 확정이다.
- **출처** — 회의 8/15 U4(최지원 01:27:25) · D8·D12·D13 · V1

### 7-10. 회원 관리와 가맹점 관리를 합치는가 (회의 U6)

- **질문** — 어드민 회원 관리와 가맹점 관리를 하나로 합치는가, 파트너·담당 필드만 옮기는가.
- **배경** — 가맹점 관리에 파트너·담당을 넣는 필드가 있어 두 화면의 경계가 흐려졌다. 계약 체인 구조를 개발이 어떻게 바꾸는지 먼저 파악한 뒤 그 위에 정책을 얹기로 해, 구조가 서기 전에는 결정할 수 없다. (확인필요)
- **위치** — 신설 예정 체인 구조 요구사항 문서(회원 관리 모달 도식) / 어드민 IA 회원 관리·가맹점 관리
- **영향** — 합치면 어드민 메뉴 구조와 권한 범위가 함께 바뀌어 IA 개편(트랙 D) 범위에 들어온다.
- **출처** — 회의 8/15 U6(이서준 01:52:11) · V6 · A9(도식화 담당 배정)
- **연계** — 유형 간 생성·변경 규칙의 정책 정의는 §7-13에 따로 세워 뒀다. 이 항목의 "체인 구조 파악 선행"이 그쪽의 선행 조건이다.

### 7-11. 체인 내 가맹점 수수료 분리 배분 요구 (회의 U7)

- **질문** — 상위 총판이 하위 가맹점에 대해 수수료를 나눠 가져가는 구조를 지원할 것인가.
- **배경** — 사업 쪽에 그런 요구가 있었다는 언급만 있고 요구사항이 문서로 정리되지 않았다. (확인필요)
- **위치** — 신설 예정 체인 구조 요구사항 문서 / [수수료 원장] 폭포수 배분
- **영향** — 지원하면 원장 폭포수에 계약 단위 분리 축이 하나 더 생겨 배분 계산과 세금계산서 발행 단위까지 닿는다.
- **주의** — 회의에서 나온 0.6·0.2는 배분 구조를 설명하려고 든 예시 숫자이며 요율이 아니다. C1과 무관하게도 어떤 값도 채택 금지.
- **출처** — 회의 8/15 U7(최지원 01:55:02) · `PAYHUG-182 본문`(수수료율을 회원 자체 값이 아니라 직속 상위 회원과 현재 회원 사이의 계약 관계 값으로 관리, 전체 수수료율은 회원이 아니라 회원 간 연결 관계에 표시) · `PAYHUG-200 본문`(회원관리에서 수수료율 입력·뷰, 수취/대납 설정을 PAYHUG-182와 연계)
- **주의** — 수수료율이 「회원 속성」이 아니라 「회원 간 계약 관계 속성」이라는 구조는 병존 요율이 어느 단계 관계에 붙는 값인지를 가르는 근거가 되지만, `PAYHUG-182`는 구체 요율 값을 확정하지 않는다. C1 해소로 취급하지 않는다.
- **연계** — 배분 축이 하나 더 생기는 문제는 §7-14의 조직 다단 적층과 같은 자리에서 만난다. 정산 상품 폼의 조직 슬롯이 1단뿐이라 다단 체인은 지금 구조로 배분에 담기지 않는다.

아래 7-12~7-14는 출처가 다르다. 2026-08-20 슬랙 회원 관리·권한 계층 검토(`analysis/slack_20260820_회원관리_권한계층_리포트.html`)에서 올라온 것으로, 회원 유형 체계와 가입 링크에 걸린다. 앞의 7-1~7-11과 마찬가지로 전부 `확인필요`다.

세 건의 코드 근거는 로컬 클론 `~/cursor/payhug-admin-web`·`~/cursor/payhug-merchant-web`에서 직접 확인한 것이다. 두 가지를 감안해야 한다. 하나, 이 클론은 원격 main보다 53개 커밋 뒤에 있어 최신 develop과 다를 수 있다. 둘, 이 저장소 안의 스냅샷 `01_payhug-admin-web-main`은 가입 링크 생성기가 좁혀지기 전 상태(상위 조직 노출 시절)라 같은 파일이라도 내용이 다르다. 행 번호를 다시 짚을 때는 어느 사본인지 밝힌다.

### 7-12. 가입 링크 발급 주체 — 현행 안내의 근거와 개편 후 자동 발급 채택 여부

질문이 두 단이다. 현행·병행 기간과 개편 후가 서로 다른 판단을 요구하고, 둘 중 하나만 정해도 나머지가 남는다.

- **질문 (a) 현행·병행 기간** — 운영이 안내하는 "파트너 계정으로 URL을 생성한다"가 유효한 발급 주체 정책인가, 아니면 서버 요건이나 과거 동작에서 온 관행인가. 그리고 이미 배포된 파트너 코드·영업 코드 URL을 병행 기간에 유지하는가 차단하는가.
- **질문 (b) 개편 후** — 회원마다 난수 링크를 자동 발급하고 체인은 관계에서 파생되는 방식(개편안 R4·R5)을 발급 정책으로 공식 채택하는가.
- **배경** — 어드민 링크 생성기는 파트너·제휴사·영업 코드를 각각 선택 항목으로 두고 하나만 골라도 링크를 만들어 복사까지 내준다(안내 문구도 "참여자를 1명 이상 선택하면"). 가맹점 가입 화면도 코드 넷을 각각 있을 때만 실어 보내므로 영업 코드 단독 가입이 프론트에서 막히지 않는다. 반면 운영은 파트너 코드가 필수라 파트너 계정으로 URL을 만들라고 안내하는데, 이 '필수'가 정책인지 서버 요건인지 근거가 없다. 영업 코드 단독으로 가입했을 때 체인과 배분이 어떻게 구성되는지도 서버 미확인이다. (확인필요)
- **혼동 주의** — 개편 문서가 이 생성기를 "폐기"로 적어 둔 것은 **개편안(TO-BE)의 계획**이지 현행 서비스의 상태가 아니다. 현행 어드민의 3드롭다운 생성기는 지금도 살아 있고, 같은 자리에 "병행 기간 중 구 생성기 및 기배포 URL의 유효성은 확인 필요"가 함께 적혀 있다. 그래서 폐기 방향이 있다는 사실이 (a)를 지우지 않는다.
- **함께 확인** — 현행 생성기는 한 번 좁혀진 적이 있다. 예전 코드는 상위(조상) 파트너를 서버에서 받아 선택지에 합치고 최상위 파트너를 기본값으로 자동 선택했는데, `fix: 가입 링크 생성기에서 상위 조직 노출 제거`(admin 커밋 `3fbc230`)로 상위를 선택지에서 뺐다. 코드 주석이 든 이유가 곧 체인 시작점 규칙이다 — "상위 조직 코드로 링크를 만들면 파트너 체인이 상위부터 시작되어 로그인한 파트너 본인이 계약(및 수수료 배분)에서 빠진다". (a)를 판단할 때 운영 안내가 이 변경 이전 동작을 가리키는 것인지 이후에도 유효한 별도 요건인지부터 갈라야 한다.
- **위치** — 어드민 회원 관리 가입 링크 생성기 / 회원 관리 개편 상태 I5·I6 · 레지스터 B2 / 가맹점 가입 플로우
- **영향** — (a)는 병행 기간에 이미 배포된 URL을 유효로 볼지와 그 기간 중 영업·영업조직 계정의 발급 허용 범위를 정한다. (b)는 수수료 체인의 시작점을 코드 선택이 아니라 관계 데이터로 옮기는 결정이어서 체인 조회·배분 계산의 기준점이 함께 바뀐다. 둘 사이 전환기에 두 방식이 겹치는 구간이 생기므로, 같은 가맹점이 두 경로로 들어왔을 때의 우선순위도 함께 정해야 한다. 이 우선순위 문항은 개발 확인 질문 목록에도 올라가 있다(리포트 §7-13번, 7-12 연계 표기).
- **출처** — admin `app/partners/page.tsx:699`(영업 선택지에 SALES·SALES_ORG 혼합)·`:715-722`(코드별 조건부 파라미터)·`:799`(1명 이상 안내), merchant `app/signup/page.tsx:262-274`(코드 조건부 전달, salesCode는 271) · `_pipeline/member_redesign/decisions_0819.md:126-127`(R4·R5 난수 링크 자동 발급) · `_pipeline/member_redesign/code_analysis.md:31`(개편안의 생성기 폐기·병행 기간 유효성 확인 필요) · 슬랙 #voc대응-정산 김진환 8/20 16:11("파트너코드는 필수라서 엔케이페이먼츠 계정으로 URL을 생성해야 한다")

### 7-13. 회원 유형 간 생성·변경 규칙 — 정책으로 선정의하는가 서버 구현을 추인하는가

- **질문** — 유형×유형 생성·변경 허용표를 정책으로 먼저 정의하는가, 서버 구현(등록가능유형 목록 + 검증)을 사후 추인하는가.
- **배경** — 프론트에 계층 규칙이 하드코딩된 곳이 한 군데도 없다. 등록 가능한 유형은 서버가 내려준 목록으로 거르고, 상위 지정은 활성 여부와 자기 자신 제외만 볼 뿐 유형 제약이 없으며, "SALES 유형은 SALES_ORG 유형의 회원을 생성/변경할 수 없습니다" 같은 문구도 프론트에 없고 서버 메시지를 그대로 띄우는 구조다. 즉 규칙의 실체가 서버에만 있고 문서에는 없다. 실제로 메타플레이 계정이 화면 표기 '영업'(코드 상수 SALES)으로 등록돼 하위 조직을 수정하지 못하는 사례가 발생했는데, 유형을 정정하는 경로는 개편안에도 없다. §7-10이 전제한 "계약 체인 구조를 개발이 어떻게 바꾸는지 먼저 파악"이 이 항목의 선행 조건이다. (확인필요)
- **위치** — 어드민 회원 관리 등록·수정 모달 / 이 문서 §7-10 / 회원 관리 개편 Flow E
- **영향** — 오등록 계정을 정정하는 절차, 등록가능유형 정책표, 참여자 수정 권한을 필드 단위로 어디까지 열지가 함께 정해진다. 서버 추인으로 가면 정책 문서는 서버 응답을 받아 적는 위치가 되고, 선정의로 가면 서버 검증과 정책표의 불일치를 대사할 근거가 생긴다.
- **출처** — admin `app/partners/page.tsx:860-866`(상위 후보 무제약)·`:872`(등록가능유형 필터)·`lib/apiClient.ts:62`(서버 message 그대로 표출, 해당 문구 프론트 검색 0건) · `01_payhug-admin-web-main/lib/devMockData.ts:253-265`(관리자 기준 등록가능유형 7종) · VOC 시트 캡처 8/20 13:07(슬랙 파일 F0BRH81RUKB, 에러 문구 원문) · `PAYHUG-182 본문`(직속 상위 기준표 = 영업 → 영업조직 / 영업조직 → 하위파트너 / 하위파트너 → 상위파트너 / 상위파트너 → 없음)
- **주의** — `PAYHUG-182`의 직속 상위 기준표는 진행 중인 개편안의 목표 구조이며 현행 서버 구현이 아니다. 정책표로 선정의할 후보로 다루되 현행 상태로 옮겨 적지 않는다.

### 7-14. 영업조직 다단 적층 허용 여부와 '영업' 용어 삼중 충돌

- **질문** — 영업조직이 같은 유형으로 여러 단 쌓이는 구조(영업조직 → 영업조직 → 영업)를 허용 구조로 정의하는가. 허용한다면 화면 표기 '영업', 회원 유형 상수 SALES, 정산 도메인 role 상수 SALES(뜻은 영업조직 몫) 셋을 어떻게 갈라 부르는가.
- **배경** — 상위 지정에 유형·깊이·순환 제약이 없어 프론트만 보면 같은 유형을 몇 단이든 쌓을 수 있고, 서버가 막는지는 미확인이다. 개편안 Q4는 "중간 단계 생략 허용"이라 단독 체인을 허용한 것이지 동일 유형 적층을 결정한 것이 아니며, 표준 구조는 조직 1단이다. 그런데 실데이터에 이미 이 경우가 있다. 용어 충돌은 세 지점이다. ① 링크 생성기의 '영업' 선택지에 SALES와 SALES_ORG가 섞여 들어간다. ② 정산 정책 화면 한 파일 안에 role 상수 `SALES: "영업조직"`과 회원 유형 상수 `SALES: "영업"`이 같이 있어 같은 낱말이 두 뜻으로 쓰인다. ③ 정산 상품 폼의 조직 슬롯이 영업조직 하나뿐이라 3단 조직 체인은 배분에 담기지 않는다 — 이 지점이 §7-11의 배분 축과 직결된다. (확인필요)
- **위치** — 02 용어·상태값(영업·영업조직 정의) / 어드민 정산 상품관리 / 회원 관리 개편 D6·D7·상태 카탈로그 / 이 문서 §7-11
- **영향** — 다단을 허용하면 배분 슬롯 구조와 링크 체인 재구성 규칙이 함께 바뀌고, 용어장에는 화면 표기와 코드 상수를 갈라 적는 열이 필요해진다. 허용하지 않으면 이미 존재하는 실데이터를 어떤 절차로 1단으로 되돌릴지가 §7-13의 정정 절차와 묶인다.
- **출처** — admin `app/partners/page.tsx:699`(혼합 선택지)·`:860-866`(무제약 상위 지정), `app/settlement/policies/page.tsx:46`(role SALES = 영업조직) vs `:78`(회원 유형 SALES = 영업), `app/settlement/policies/PolicyFormModal.tsx:241-247`(조직 슬롯 1단) · `_pipeline/member_redesign/decisions_0819.md:114`(Q4) · `01_payhug-admin-web-main/lib/devMockData.ts:74-121`(목 체인 최대 4단, 조직 2중 없음) · `PAYHUG-182 본문`(직속 상위 기준표에 영업조직의 직속 상위가 하위파트너 하나뿐이라 영업조직 적층 자리가 없음)
- **주의** — 이 항목을 다루며 나오는 영업·영업조직 요율 숫자는 C1(수수료율) 미확정 범위다. 코드나 화면에 그 값이 있다는 사실까지만 근거로 쓰고 정책값으로 채택하지 않는다.

아래 7-15~7-17은 바이패스(선정산 시작일 이전 입금분의 수동이체 지급)에 걸린 것으로, 출처는 티켓 PAYHUG-208·PAYHUG-253과 2026-08-20 이중 지급 사고다. 용어는 02 §2, 판정 기준은 03 §7, 예외 처리와 사고 사실은 04 §7·§8에 있고 아래 세 건은 그 위에 남은 정책 공백이다. 앞의 항목들과 마찬가지로 전부 `확인필요`다.

### 7-15. 선정산 시작 전 조정금액(광고비 등)을 바이패스 지급액에서 차감하는가

- **질문** — 선정산 시작일 이전에 발생한 플랫폼 조정금액(광고비 등)을 바이패스 지급액 산출에서 차감하는가, 락계좌에 입금된 금액 그대로 보내는가.
- **배경** — 바이패스는 선정산 미대상 정산금을 운영자가 수동이체로 돌려주는 작업이고, 가맹점 화면 안내문도 "입금된 금액 그대로 이체 돼요"로 원칙을 적는다(merchant `app/settlement/account/excluded/page.tsx:16`). 그런데 시작일 이전 거래에도 광고비 같은 플랫폼 차감 항목이 붙는 경우가 있어, 차감 후 금액을 보내야 하는 케이스가 있는지가 티켓 정책 표에 확인 필요로 남아 있다. PAYHUG-208 확인 요청사항 3번 미답. (확인필요)
- **위치** — 04 §7 바이패스 예외 처리 · 확인 필요 / 03 §7 판정 기준 / 어드민 가맹점 상세 바이패스 블록 반환액
- **영향** — 차감한다면 건별로 조정 항목을 끌어와 검산 근거에 한 줄이 늘고, 반환액과 락계좌 실입금액이 어긋난 건이 정상 범위로 들어온다. 차감하지 않는다면 시작 전 조정금액을 어느 시점 어느 원장에서 정리할지 별도 경로를 세워야 한다.
- **주의** — 여기서 다루는 차감은 플랫폼 조정 항목이며 페이허그 수수료가 아니다. 바이패스 지급액에 요율을 적용하는 문제로 옮겨 읽지 말 것(C1 미확정).
- **출처** — `PAYHUG-208 본문` 「정책 및 예외사항」 표(선정산 시작 전 조정금액 행) · 같은 티켓 확인 요청사항 3번 · merchant `app/settlement/account/excluded/page.tsx:16` · `PAYHUG-253 본문`(208 확인 요청사항 1·2번은 각각 「수동 바이패스 처리 확정」·「상품 배정이 되어야 선정산이 시작됨」으로 해소되어 취소선 처리, 3번만 미해결로 남음)
- **연계** — 선정산 시작 전 조정금액을 지급액에서 자동으로 빼는 처리 자체는 선정산 쪽에 있다(04 §10, `PAYHUG-171 본문`, 운영 배포 2026-08-22). 바이패스 지급액에도 같은 차감을 적용하는지가 이 항목의 질문이다.

### 7-16. 바이패스 지급 버튼의 운영 배포 시점과 그때까지의 수동 운영 기준

- **질문** — 어드민 바이패스 지급 버튼과 지급 사실의 원장 기록을 언제 운영에 올리는가. 그때까지 이중 지급을 무엇으로 막는가.
- **배경** — PAYHUG-208(기획)과 하위작업 PAYHUG-209(개발)는 완료 상태이지만 운영에 올라간 것은 조회뿐이다. 2026-09-02 코멘트에서 운영 담당이 "지급실행버튼도 고도화해야하는거고, 자동화사용이아니라 현재는 지급조회성입니다"로 정정했다. 지급 버튼과 원장 기록은 `feature/bypass` 가지에만 있다. 운영 기준을 세우는 PAYHUG-253(「바이패스 운영 기준 정립 및 어드민 지급 버튼 활성화」)이 진행 중이며 PAYHUG-208을 blocks한다. 티켓 목표 셋 중 조회 하나만 나가 있어 04 §8 이중 지급의 원인이 남아 있다. (확인필요)
- **위치** — 04 §7 예외 처리 기준(선정산 배치 제외 행) · §8 / 어드민 가맹점 상세 바이패스 블록 / 정산 메뉴 바이패스 대상 조회
- **영향** — 배포 전 구간에서는 지급 사실이 원장에 남지 않아 같은 거래가 선정산 배치에 다시 실리는 것을 시스템이 막지 못한다. 그 구간의 차단을 운영 절차로 둘지 배치 쪽 임시 조치로 둘지에 따라 운영 매뉴얼과 개발 범위가 함께 정해진다.
- **출처** — `PAYHUG-208 코멘트 2026-09-02`(윤지애, 지급조회성 정정) · `PAYHUG-208 본문`·`PAYHUG-209 본문`(2026-09-07 티켓 상태 완료) · `PAYHUG-253 본문`(진행 중, PAYHUG-208 blocks) · 원고 `_pipeline/enhance_202609/round1/bypass_content.md` AD_MERCHANT_DT_BYPASS_STATE deep 13(처리 결과의 원장 반영 미확인)
- **근거 보강** — `PAYHUG-253 본문`이 1차 개발 범위를 「바이패스 전 과정 자동화가 아니라, 조회된 대상에 대해 운영자가 어드민에서 확인 후 직접 지급하도록 지급 버튼을 활성화하는 것」으로 못 박았다. 자동 판정·자동 감지는 후속 고도화 범위이며 배포 시점은 티켓에 없다. 그때까지의 수동 운영 기준 12항목은 같은 티켓에 확정 문구로 있고 02 §2·03 §7·04 §7에 반영되어 있다.

### 7-17. 이중 지급 230,694원을 과지급으로 다루는가 미회수금으로 다루는가

- **질문** — 2026-08-20 이중 지급 230,694원을 6대 개념 중 과지급으로 등재하는가 미회수금으로 등재하는가. 회수 실행 수단은 무엇인가.
- **배경** — 청목황금갈비·김성관화덕구이에서 8/19 거래일 하나카드 입금분을 오전·오후 두 차례 바이패스로 지급한 뒤 같은 날 오후 선정산이 시작돼 동일 거래가 선정산 대상에 다시 실렸다(71,252원 + 159,442원). 회수 로직이 없어 운영이 수동으로 추적하고 있다. 02 §4는 과지급을 발생 원인·최초 금액, 미회수금을 남은 회수 잔액으로 가르는데 이 건은 어느 쪽으로도 등재돼 있지 않다. (확인필요)
- **위치** — 04 §8 발생 사례 / 02 §4 과지급·미회수금 / 이 문서 §1 '회수' 일곱 문항 · §7-9
- **영향** — 과지급으로 세우면 회수 어드민 1차 범위(§3·§7-9)에 이 건이 들어오고, 미회수금으로만 두면 잔액 관리 대상이 되어 발생 원인이 원장에 남지 않는다. 회수 수단을 다음 선정산액 차감으로 할지 가맹점 직접 입금으로 할지도 같은 자리에서 정해야 한다.
- **주의** — 여기서 쓰는 '회수'는 과지급 회수다. 어드민 '매출 회수'(락계좌→모계좌 스윕), 플랫폼 조정 회수와 바꿔 적지 않는다(§7-4·§6 참조).
- **출처** — 사고 사실 2026-08-20(청목황금갈비·김성관화덕구이, 230,694원 = 71,252원 + 159,442원) · `PAYHUG-208 본문` 「정책 및 예외사항」 표(바이패스 지급 완료 후 동일 거래가 선정산 대상에 포함되는 경우 → 선정산 배치에서 제외) · 02 §4 · `PAYHUG-253 본문`(같은 사례를 예외 처리 표에 「중복 지급 사유 확인 후 회수 처리」로 두고, 이 사례 이후 신규 선정산 시작은 오전 진행을 원칙으로 운영. 회수 완료 여부는 티켓에 없음)

아래 7-18~7-26의 출처는 2026-09-07 기준 Jira 기획 티켓 25건 조사(`_pipeline/jira_20260907/tickets.md`)다. 앞의 항목들과 마찬가지로 전부 `확인필요`이며 임의 확정 금지다. 티켓 본문을 코멘트가 뒤집은 4건(PAYHUG-208·214·169·194)은 코멘트 쪽을 따랐다.

### 7-18. 취소 회수 예시의 수수료 비율이 실제 요율인가

- **질문** — `PAYHUG-260` 예시에서 역산되는 채권매입수수료 1.0%와 시스템이용료 0.11%가 계약·정책의 실제 요율인가, 설명을 위해 고른 숫자인가.
- **배경** — 티켓은 순지급액 10,000원에 채권매입수수료 100원, 시스템이용료 11원을 든다. 문단이 `예를들어`로 열리고 10,000원이라는 반올림된 값이라, 비율이 요율에서 나온 것인지 서술 편의로 고른 것인지 가려지지 않는다. C1은 요율 후보가 최소 5벌 병존하는 최우선 충돌이다. (확인필요)
- **후보 대조표**

| C1 후보 | 채권매입수수료 1.0%와의 관계 | 시스템이용료 0.11%와의 관계 |
|---|---|---|
| 단일 1% (세무노무 제안서) | 일치 | 후보에 해당 항목 없음 |
| 채권매입 1% (Q&A_260707) | 일치 | 후보에 해당 항목 없음 |
| 표기 1.0% · 예시 실계산 0.604% (스토리보드 260527) | 표기와 일치, 실계산과 불일치 | 후보에 해당 항목 없음 |
| 0.6% + 0.11% + 0.1% (VAT 계산 xlsx 내부 배분) | 세 항의 합 0.81%와 불일치 | 가운데 항 0.11%와 수치 일치 |
| 채권매입 0.25% (네오소프트·플랙스허브 계약) | 불일치 | 후보에 해당 항목 없음 |
| 1%/0.25%/0.11% 등 (Figma 선정산시스템) | 1% 갈래와 일치, 0.25% 갈래와 불일치 | 0.11% 갈래와 수치 일치 |

- **수치 일치가 개념 일치는 아니다** — 0.11%가 세 자리에 나오는데 뜻이 다르다. ① 티켓 예시의 시스템이용료는 가맹점이 부담하는 항목이다. ② VAT 계산 xlsx의 0.11%는 내부 배분에서 투자자 몫이다. ③ `PAYHUG-229 본문`의 투자자 할인율 0.11%는 투자자의 채권매입 수수료율이며 가맹점 수수료율과 다른 층이다. 같은 숫자를 근거로 세 항목을 잇지 않는다.
- **합계도 맞지 않는다** — 티켓 예시의 가맹점 부담 수수료 합은 111원, 곧 1.11%다. 0.6+0.11+0.1 = 0.81%와도, 단일 1%와도 어긋난다.
- **원장 실측과도 어긋난다** — 26.08.09 어드민 원장의 건별 채권매입 배분액 3건은 `0.6% + 소수점 이하 올림`과 전건 일치하고 `1%`와는 3건 전부 어긋난다(Figma `EUK2P3tDOkprcnRmMqNns4` `22:263`, 검산은 `analysis/00_종합.md` C1). 티켓 예시에서 역산되는 1.0%는 원장에 부과된 값과 다른 층위일 수 있다. 두 값의 관계는 §7-27이다.
- **위치** — 02 §1 선정산 지급액 구성 항목 / 03 §8 회수액 산식 / `analysis/00_종합.md` C1
- **영향** — 요율로 채택하면 C1의 다섯 후보 중 하나가 사실상 확정되어 계약·배분·세금계산서 산식이 함께 고정된다. 설명용 숫자로 남기면 03 §8 예시는 검산 예시로만 쓰고 요율 자리는 비워 둔다.
- **출처** — `PAYHUG-260 본문` 예시 표 · `analysis/00_종합.md` C1 · `PAYHUG-229 본문` 1차 미팅 2026-08-28(투자자 할인율 정의)

### 7-19. 선정산 미대상 사유 어휘 통일

- **질문** — 거래 단위 미대상 사유의 이름을 어느 벌로 확정하는가.
- **배경** — 세 벌이 병존한다. (확인필요)

| 출처 | 어휘 |
|---|---|
| `PAYHUG-186 본문` 「미대상 사유 표기」 절 | `선정산 미대상` / `선정산 개시 전` |
| `PAYHUG-186 본문` 「용어 정리」 절 | `취소` / `선정산 미적용` / `선정산 시작일 이전` |
| `PAYHUG-214 본문` 매핑표 | `대상` / `대상 아님` + 사유 `연결 진행 중` · `미연결` |

- **한 티켓 안에서도 갈린다** — 186은 같은 본문의 두 절이 서로 다른 이름을 쓴다. 214는 그 위에 세 번째 조합을 얹는데, 코멘트 2026-08-22 이서준이 「운영팀 요청에 대한 아이데이션이며 확정된 사항 없음」으로 정정해 214 본문 전체를 확정으로 취급하지 않는다.
- **판단 시점이 다르다** — 186은 거래가 발생한 날 기준으로 거래 한 건을 가르고, 214는 조회 시점 기준으로 플랫폼 한 개의 현재 상태를 가른다. 어휘를 합칠 때 두 축을 한 낱말에 담지 않아야 한다.
- **데이터 구분 가능 여부가 앞선다** — 사유를 데이터로 갈라낼 수 없으면 사유 없이 `미대상`만 표기하는 선택지가 티켓에 열려 있다.
- **연계** — 사유 `취소`가 담는 취소 4종(승인취소·주문취소·부분취소·매입취소)의 표기는 `PAYHUG-194 본문`에서 "고민 필요" 상태이고, 매입취소 자체의 로직 자리는 §7-7이다.
- **위치** — 02 §5 거래 단위 선정산 대상 판정 / 04 §11 확인 필요 / 어드민 매출 상세·정산현황 건별 상세·매출내역 엑셀·선정산결과 엑셀
- **영향** — 어휘가 서면 화면 배지·엑셀 사유 컬럼·상태 카탈로그가 한 벌로 정리된다. 서지 않으면 같은 거래가 화면마다 다른 이름으로 불린다.
- **출처** — `PAYHUG-186 본문`(두 절의 어휘 불일치) · `PAYHUG-214 본문` 매핑표 · `PAYHUG-214 코멘트 2026-08-22`(이서준, 아이데이션 단계) · `PAYHUG-194 본문`(취소 4종 표기)

### 7-20. 계약서 용어(미지급금·과지급금)와 내부 6개념의 대응표

- **질문** — 계약서의 `미지급금`·`과지급금`과 내부 6대 개념(미지급금·선정산 제외금액·바로이체·과지급·미회수금·환급)을 어느 대응으로 잇는가.
- **배경** — 계약서는 플랫폼이 락계좌에 지급한 돈 중 회수하지 못한 돈을 `미지급금`, 더 받은 돈을 `과지급금`으로 쓴다. 내부는 선정산 실행 시 가맹점에 지급하지 않은 돈과 더 지급한 돈으로 쓴다. 주체와 자금 방향이 다른데 낱말이 같다. 2차 미팅에서 투자자 어드민을 계약서 정의에 맞추고 내부 용어를 그쪽으로 옮기는 방향에 합의했으나 대응표는 작성되지 않았다. (확인필요)
- **절대원칙에 걸린다** — 프로젝트 절대원칙 7이 계약 용어와 운영 6개념의 매핑을 미확정으로 두고 임의 매핑을 금지한다. 절대원칙 2는 6개 개념의 병합을 금지한다. 통일 합의만으로 내부 정의를 계약 정의로 덮으면 두 원칙을 함께 어긴다.
- **C5와 한 뿌리** — `analysis/00_종합.md` C5는 계약 2개념(과정산금·미정산금)과 운영 6개념, 가맹점 노출 용어의 3층 매핑을 미제정 상태로 둔다. 이 항목은 그 표에 계약서의 `미지급금`·`과지급금` 두 낱말을 함께 올려야 한다는 요구다.
- **위치** — 02 §2 미지급금 · §4 과지급·미회수금 / `analysis/00_종합.md` C5 / 투자자 어드민 용어 정의
- **영향** — 대응표가 서면 투자자 어드민·관리자 어드민·가맹점 화면이 같은 낱말을 같은 뜻으로 쓸 수 있다. 서기 전에 용어만 바꾸면 기존 원장·화면·정책서의 같은 낱말이 두 뜻으로 섞인다.
- **출처** — `PAYHUG-229 본문` 2차 미팅 2026-08-30 · 02 §2·§4 · `analysis/00_종합.md` C5 · 프로젝트 절대원칙 2·7

### 7-21. 이체수수료의 부담 주체와 선정산 지급액 산식 내 위치

- **질문** — 이체수수료를 선정산 지급액에서 빼는가 빼지 않는가. 부담주체는 누구인가.
- **배경** — 다섯 출처가 자리를 다르게 둔다. (확인필요)

| 출처 | 이체수수료의 자리 |
|---|---|
| `PAYHUG-260 본문` 예시 | 지급액 계산에 들어가지 않음. 10,000 − 100 − 11 = 9,889이며 330을 빼면 9,559로 어긋남 |
| 03 §2 카드 선정산 산식 | 지급액에서 300원 차감 |
| 어드민 실구현 산식 (`analysis/00_종합.md` §2) | 선정산수수료 묶음(채권매입 + 시스템 + 이체 300)에 포함되어 차감 |
| Figma 정책 원문 (`EUK2P3tDOkprcnRmMqNns4` `77:311`, `26.08.15 [기본 정책]`) | "선정산 수수료 : 채권매입 수수료, 시스템이용료, 이체 수수료의 합계"이고 지급액은 대상액에서 선정산 수수료를 빼므로 차감 쪽 |
| 어드민 화면 요약 카드 (admin `app/settlement/overview/PreSettlementTab.tsx:551`) | `선정산 수수료` 값의 내역을 `매입+시스템+이체`로 적어 차감 쪽 |

넷은 차감, 하나(`PAYHUG-260` 예시)는 비차감이다.

`확정` **부담 주체는 정산 상품 설정으로 갈린다.** 어드민의 정산 상품 등록 화면에 `수수료 납부 옵션` 항목이 있고, 이체수수료 납부자를 상위파트너·하위파트너(영업 상품이면 상위영업·하위영업) 중에서 켠다. 필드 주석이 규칙을 그대로 적는다: `수수료 납부자: "" = 가맹점 부담(기본), UPPER/LOWER = 해당 파트너 납부` (admin `app/settlement/policies/PolicyFormModal.tsx:37`). 화면에도 같은 문구가 붉게 붙어 있다: `*모두 OFF 일경우 가맹점이 납부합니다.` (admin `app/settlement/policies/PolicyFormModal.tsx:274`)

시스템이용료도 같은 구조로 납부자를 고른다. Figma 정책 원문이 "가맹점에게 시스템 이용료를 받지 않기 때문에 총판 어드민은 다른 구성이 필요하다"고 적은 것과 맞물린다.

`확인 필요` **그러면 위 표는 충돌이 아니라 조건 분기일 수 있다.** 가맹점 부담이면 지급액에서 빠지고(03 §2), 파트너 부담이면 빠지지 않는다(`PAYHUG-260` 예시). 다만 티켓 예시가 어느 설정을 전제하고 적힌 것인지 본문에 없어 단정할 수 없다. 티켓이 주체를 "부담주체"라고만 쓴 것도 가맹점마다 달라서일 수 있으나 이 역시 티켓에 근거가 없다.

`확인 필요` 조건 분기가 맞다면 03 §2의 산식은 **가맹점 부담일 때의 산식**으로 한정해 적어야 하고, 파트너 부담일 때의 산식이 따로 있어야 한다. 현재 03 §2에는 그 구분이 없다.
- **C3와 한 뿌리** — C3는 이체수수료 300원의 단위(가맹점당 하루 1회 vs 정산금 건당)·차감 위치·부담 주체를 미확정으로 둔다. 부담 주체는 정산 상품 설정 값으로 정해지는 것이 코드에서 확인되므로 C3 세 갈래 중 하나가 풀린다. 단위와 차감 위치는 남는다.
- **금액은 맞물린다** — 티켓 예시의 330원은 C3에 적힌 VAT 계산 xlsx의 330원(300원 + 부가세)과 일치한다. 금액 근거는 서로 받쳐 주되 차감 위치는 여전히 갈린다.
- **위치** — 02 §1 선정산 지급액 구성 항목 / 03 §2 카드 선정산 산식 / `analysis/00_종합.md` C3
- **영향** — 자리가 정해져야 지급액 검산 예시와 03 §3의 세금계산서 과세 대상 구분이 함께 선다. 취소 회수액에 이체수수료를 더하지 않는다는 것은 티켓에 확정으로 있으므로 회수 산식은 이 판단과 무관하게 유지된다.
- **출처** — `PAYHUG-260 본문` 예시 표와 각주 · 03 §2 · `analysis/00_종합.md` C3·§2 어드민 실구현 산식 · Figma `EUK2P3tDOkprcnRmMqNns4` `77:311` · admin `app/settlement/policies/PolicyFormModal.tsx:37`·`:274` · admin `app/settlement/overview/PreSettlementTab.tsx:551`

### 7-22. 원거래 취소 회수의 실행 경로와 회수 실패 시 처리

- **질문** — 취소로 발생한 회수 대상 금액을 6대 개념 중 과지급으로 등재하는가 미회수금으로 등재하는가. 회수 수단은 무엇이며 회수가 실패하면 어디로 보내는가.
- **배경** — `PAYHUG-260`은 회수 금액 기준(순지급액 전액)과 수수료 비환급 원칙만 정한다. 락계좌 잔액 부족·가압류로 회수가 되지 않을 때의 경로가 티켓에 없고, 하위 개발 티켓 PAYHUG-261도 Backlog다. (확인필요)
- **회수 3갈래를 섞지 않는다** — 여기서 쓰는 회수는 가맹점에게 나간 지급액을 되찾는 축이다. 어드민 「매출 회수」(락계좌→모계좌 스윕)와 플랫폼 조정 회수는 다른 축이며, 실패 알림은 스윕 축인 §7-23에 따로 있다.
- **위치** — 02 §4 회수 / 03 §8 회수액 산식 / 04 §9 확인 필요 / 이 문서 §1 '회수' 일곱 문항
- **영향** — 과지급으로 세우면 회수 어드민 1차 범위(§3·§7-9)에 이 유형이 들어오고, 미회수금으로만 두면 발생 원인이 원장에 남지 않는다. 실패 경로를 정하지 않으면 회수 대상이 어디에도 적재되지 않은 채 운영 수작업으로 남는다.
- **출처** — `PAYHUG-260 본문`(회수 기준만 확정, 실패 경로 없음) · 연결 하위작업 PAYHUG-261 상태 Backlog · 02 §4 · 이 문서 §7-17(같은 판정을 요구하는 선행 사례)

### 7-23. 락계좌 전액 회수 실패의 판정·알림·잔액 처리

- **질문** — 회수 예정금액보다 실제 회수금액이 적을 때 무엇을 기준으로 부분 회수로 판정하고, 누구에게 어느 채널로 알리며, 남은 미회수 잔액을 어디로 보내는가.
- **배경** — 락계좌 입금액은 회수 시 전액 출금을 기준으로 운영하는데, 가압류·압류로 전액이 나오지 않는 경우가 있다. 김성관화덕구이 사례에서 운영자가 일부만 회수된 것을 눈으로 확인하고 추가로 살펴본 뒤에야 가압류를 알았다. 기획 티켓 PAYHUG-251은 본문이 비어 있고 요청 내용은 운영 티켓 PAYHUG-250에만 있다. (확인필요)
- **미정 항목** — 알림 수신 대상, 채널(어드민 화면·슬랙·알림톡), 발송 시점, 중복 발송 억제 규칙, 부분 회수 판정 임계값(1원 차이도 알리는지), 미회수 잔액의 후속 처리(다음 회수 이월인지 별도 채권 관리인지).
- **요청 예시 금액** — 회수 대상 1,000,000원 / 실제 회수 696,375원 / 미회수 303,625원. 요청 문구 초안도 PAYHUG-250에 있으나 확정 문구가 아니다.
- **회수 축 주의** — 이 회수는 어드민 「매출 회수」(락계좌→모계좌 스윕)다. 정책의 과지급 회수(§7-22)와 같은 낱말을 쓰되 다른 개념이다(절대원칙 6).
- **위치** — 04 §12 / 06 어드민·알림톡 부분 회수 알림 / 02 §4 미회수금
- **영향** — 임계값과 채널이 정해져야 알림이 운영에 올라간다. 잔액의 후속 경로를 정하지 않으면 미회수 잔액이 6대 개념 중 어디에도 적재되지 않는다.
- **출처** — `PAYHUG-250 본문`(요청 내용·사례·예시 금액·문구 초안) · `PAYHUG-251 본문`(기획 티켓 본문 없음, Backlog)

### 7-24. 선정산 개시일의 정의와 플랫폼별 개시일 기록

- **질문** — 「선정산 개시일」이 가맹점 단위 값인가 플랫폼 단위 값인가. 플랫폼별 개시일 기록이 데이터로 남아 있는가. 선정산 중단·재개 이력이 있는 가맹점의 개시일은 무엇으로 표기하는가.
- **배경** — 세 티켓이 같은 값에 기대는데 정의가 어디에도 문장으로 없다. (확인필요)

| 티켓 | 이 값을 쓰는 자리 |
|---|---|
| `PAYHUG-186` | 거래일에 그 플랫폼이 선정산 중이었는지 판정. 플랫폼별 개시일 기록 유무를 미결로 둠 |
| `PAYHUG-242` | 가맹점의 실제 선정산 개시일을 어드민에 노출. 설계서 노드 참조뿐이고 정의·노출 위치가 본문에 없음 |
| `PAYHUG-253` | 바이패스 구간 ①②의 경계. 최초 시작(ON)과 중단·재개 시점을 함께 봄 |

- **기록이 없으면 판정이 안 선다** — 186의 거래일 기준 판정은 과거 거래를 다시 볼 수 있어야 성립한다. 플랫폼별 개시일 기록이 없으면 기록 적재가 먼저 필요하고, 그 전까지 과거 거래의 대상·미대상은 판정할 수 없다.
- **중단·재개가 있으면 개시일이 하나가 아니다** — 253이 중단·재개 구간을 바이패스 경계로 쓰므로, 242가 화면에 한 값만 보이면 그 값이 최초 개시일인지 최근 재개일인지 갈린다.
- **위치** — 02 §5 거래 단위 선정산 대상 판정 / 03 §7 바이패스 판정 / 04 §11 확인 필요 / 06 어드민 가맹점 상세 선정산 개시일 필드
- **영향** — 정의가 서면 186의 거래 판정, 242의 화면 값, 253의 바이패스 구간이 같은 값을 참조하게 된다. 서지 않으면 세 곳이 각자 다른 날짜를 쓴다.
- **출처** — `PAYHUG-186 본문` 미결(플랫폼별 개시일 기록 유무) · `PAYHUG-242 본문` 미결(개시일 정의·노출 위치·중단 재개 표기, 설계서 `IFX4GRC60ibOVCWNrd6VQt` `4435-1091`) · `PAYHUG-253 본문`(구간 ①② 정의)

### 7-25. 당일 미차감액의 이월 규칙

- **질문** — 당일 차감되지 못한 금액을 며칠까지 이월하는가. 이월 상한이 있는가. 이월 잔액을 화면에 어떻게 표기하는가.
- **배경** — 차감 대상 금액이 당일 선정산 대상액보다 크면 이월되어 다음 정산일에 차감되는 동작이 사례로 확인된다(8/24 광고비가 8/25에 차감되지 못하고 8/26에 차감). 그러나 이월 기간과 상한이 티켓에 없고, 이월 잔액은 현재 별도로 노출되지 않아 실제 차감이 일어난 정산일의 내역으로만 확인된다. 미차감 이월액 노출은 「현재 정책에서 혼선을 줄 수 있어」 후속 고도화로 이연되었고 원본 운영 요청 PAYHUG-226은 진행 중이다. (확인필요)
- **절대원칙 4와 접점** — 「지급 전 음수 = 당일 차감」에 당일 차감액이 지급 대상액을 넘는 분기가 붙는다. 이 분기의 상한이 없으면 이월 잔액이 무한정 쌓이는 구간이 생긴다.
- **위치** — 03 §10 / 05 UI요구사항 광고비 항목 / 어드민 선정산 상세
- **영향** — 규칙이 서면 이월 잔액을 원장 항목으로 세울지 표시 항목으로만 둘지가 정해진다. 서기 전에 노출부터 붙이면 가맹점이 보는 잔액과 실제 차감 예정액이 어긋날 수 있다.
- **출처** — `PAYHUG-240 본문`(사례·이연 결정·설계서 `IFX4GRC60ibOVCWNrd6VQt` `4435-1729`) · 연결 운영 요청 PAYHUG-226 진행 중

### 7-26. 스크래핑 재시도 정책

- **질문** — 매출 스크래핑이 1건 오류났을 때 무엇을 다시 돌리는가. 카드와 배달 각각의 재시도 단위와 횟수를 어떻게 두는가.
- **배경** — 현재 1건이 오류나도 처음부터 다시 돌리는 처리가 없다. 처음부터 다시 돌리는 방식 자체가 불가하다는 개발 의견이 있고, 재시도 정책 설정을 별도 과업으로 설계할 필요가 제기되었으나 티켓에 후속 답변이 없다. (확인필요)
- **수집 축이 셋이다** — 매일 3일 범위, 월 1회 1개월 범위, 계약 시 3개월 범위. 뒤의 둘은 개발 착수 전이며, 3개월 수집을 촉발할 계약 프로세스 지점도 "예시" 수준으로만 적혀 있다.
- **심사와의 연결이 비어 있다** — 3개월 수집 결과를 가맹점 매출 심사에서 어떻게 판정에 쓰는지가 수집 티켓과 가입 프로세스 티켓 양쪽에 없다.
- **절대원칙 5와 접점** — 스크래핑 시각·최종 업데이트 일시를 화면에 어떻게 구분해 보이는지가 재시도 정책과 함께 정해져야 한다.
- **위치** — 03 §5 매출 데이터 수집 범위 / 05 UI요구사항 최종 업데이트 일시 / 04 지연 반영 거래 처리
- **영향** — 재시도 단위가 정해져야 수집 실패 상태(02 §5 데이터 상태)와 정산 보류 판정이 이어진다. 정하지 않으면 누락 건이 늦게 붙어 이미 지급 완료된 정산에 사후로 얹히는 경로가 남는다.
- **출처** — `PAYHUG-233 본문` · `PAYHUG-233 코멘트 2026-08-30`(최지원 제기 · 이정규 재시도 처리 방법 필요) · `2026-08-31`(김진환, 별도 과업 설계 필요 · 후속 답변 없음) · `PAYHUG-256 본문`(매출 심사 판정 기준 미기재)

아래 7-27~7-32의 출처는 Figma 두 페이지 전수 판독이다. 정책 원문 페이지(`EUK2P3tDOkprcnRmMqNns4` `1:10`, `_pipeline/jira_20260907/figma_policy_EUK.md`)와 [운영] 개선건 페이지(`IFX4GRC60ibOVCWNrd6VQt` `3174:746`, `_pipeline/jira_20260907/figma_operation_improvements.md`)다. 앞의 항목들과 마찬가지로 전부 `확인필요`이며 임의 확정 금지다.

### 7-27. 가맹점 부담 채권매입 요율 — 문서 표기 1%와 원장 실측 0.6%의 관계

- **질문** — 가맹점이 부담하는 채권매입 수수료율은 얼마인가. 26.08.09 원장에 실제로 부과된 비율은 그 요율과 어떤 관계인가.
- **배경** — 정책 문서·계통도·슬랙·요율설정 시트가 모두 `1%`로 적는데, 같은 날짜 어드민 원장의 건별 채권매입 배분액은 `0.6% + 소수점 이하 올림`과 전건 일치한다. 두 값을 잇는 문장이 판독한 파일 어디에도 없다. (확인필요)
- **검산** — 산식이 정한 「소수점 이하 올림」을 적용한 값이다.

| 건별 정산금액 | 실제 배분액 | 0.6% + 올림 | 1% + 올림 | 0.604% + 올림 | 0.25% + 올림 |
|---|---|---|---|---|---|
| 9,918 | 60 | 60 일치 | 100 | 60 일치 | 25 |
| 9,037 | 55 | 55 일치 | 91 | 55 일치 | 23 |
| 18,559 | 112 | 112 일치 | 186 | 113 어긋남 | 47 |

- **읽어낼 수 있는 것** — `0.604%`는 세 번째 건에서 어긋나 배제된다. `1%`와 `0.25%`는 3건 전부 어긋난다. `0.6% + 올림`은 3건 전부 맞는다.
- **주의** — 표본이 3건이라 요율 확정에는 모자란다. **`0.6%`를 확정 요율로 선언하지 않는다.** 「원장 실측 3건이 0.6% + 올림과 일치한다」는 사실까지만 쓴다. 함께 대조한 투자자 몫 `0.11%`는 3건 중 2건만 맞는다(9,037원 건은 올림값 10, 실제 배분 11).
- **함께 확인** — `1%`가 가맹점 계약률이고 `0.6%`가 이해관계자에게 배분되는 몫이라는 해석이 가능하다. 그러나 원장 화면은 참여자 배분 합계를 그대로 「배분」으로 표기할 뿐이고, 가맹점에게 1%를 떼고 그중 일부만 배분한다는 문장이 없다.
- **섞지 않는다** — 회의에서 배분 구조를 설명하려고 든 예시 숫자 0.6·0.2(§7-11)와 이 원장 실측값은 출처가 다르다. 수치가 비슷하다는 이유로 두 근거를 잇지 않는다.
- **후보 목록의 출처** — 전수 판독에서 `0.604%`와 `0.6+0.11+0.1%`라는 표기는 한 곳도 나오지 않는다. C1 후보 표의 스토리보드 260527·VAT 계산 xlsx 두 줄은 원본에서 출처를 다시 확인해야 한다.
- **위치** — `analysis/00_종합.md` C1 / 02 §7 수수료 용어와 배분 체인 / 03 §3 채권매입수수료 산식 / 이 문서 §7-18
- **영향** — 관계가 서면 세금계산서 발행 기준액, 참여자 배분 원장, 가맹점 화면의 선정산 수수료 표기가 같은 요율을 참조하게 된다. 서기 전에는 어느 값도 산식에 넣을 수 없다.
- **출처** — Figma `EUK2P3tDOkprcnRmMqNns4` `22:263`(원장 캡처, 판독자 검산) · `22:309`(슬랙 「현재 페이허그 수수료율인 1%」) · `22:278` `22:285` `22:287` `22:307`(계통도·메모의 1%) · `22:257`(요율설정 시트) · `5:71` 행 47·48(산식과 계약 제17조 근거) · 양수도계약서 제17조

### 7-28. 수수료 체인 단계별 병존 요율 — 상위파트너와 페이허그

- **질문** — 하위파트너 → 상위파트너 구간이 0.45%(카드)·0.47%(배달)·0.5%(계통도) 중 무엇인가. 상위파트너 → 페이허그 구간이 0.23%(배달)·0.25%(카드·계통도) 중 무엇인가. 카드와 배달을 요율로 가르는 구조가 확정된 것인가.
- **배경** — 같은 파일 안에서 계통도는 한 값을, 정산 계산식 표는 상품별로 다른 값을 쓴다. 어느 것이 유효한지 적은 문장이 없다. (확인필요)

| 구간 | 계통도 | 계산식 표 |
|---|---|---|
| 하위파트너 → 상위파트너 | 0.5% (`22:279` `22:299`) | 0.45% 카드 (`22:215` `22:218`) · 0.47% 배달 (`22:217`) |
| 상위파트너 → 페이허그 | 0.25% (`22:280` `22:298` `22:266`) | 0.25% 카드 (`22:215`) · 0.23% 배달 (`22:217`) |

- **함께 걸린 것** — 요율설정 화면(`22:296`)은 하위파트너 채권매입 `1`을 뺀 8개 행이 전부 `0`이고, 손으로 적은 값(0.11 / 0.25 / 0.5 / 0.4 / 0.1 / 0.2 / 0.3 / 300)이 화면에 들어가 있지 않다. 화면에 없는 「영업사원 0.3」 행을 만들라는 뜻인지도 갈리지 않는다(`22:301` `22:302`). 하위영업업체 요율은 시트에 `미정`으로 적혀 있고(`22:257`), 이체수수료 부담 주체 두 줄은 `확인필요`로 적혀 있다.
- **주의** — 여기서 나오는 값은 전부 C1 미확정 범위다. 어느 값도 채택하지 않는다.
- **위치** — 02 §7 배분 체인 / `analysis/00_종합.md` C1 / 어드민 정산 상품관리 요율 설정
- **영향** — 구간 값이 정해져야 참여자 배분 원장과 세금계산서 정산 단위가 선다. 카드·배달을 요율로 가르는 구조라면 정산 상품 설정에 상품 축이 하나 더 필요하다.
- **출처** — Figma `EUK2P3tDOkprcnRmMqNns4` `22:215` `22:217` `22:218` `22:266` `22:279` `22:280` `22:296`~`22:308` · `22:257`(요율설정 시트) · `22:258`(각 사별 역할정의)

### 7-29. 정산 반영 수수료 차액의 부호 — 정책은 빼고 코드는 더한다

- **질문** — 선정산 지급액 산식에서 정산 반영 수수료 차액을 빼는가 더하는가. 두 표기가 같은 결과를 내는가.
- **배경** — 정책 원문은 `선정산 지급액 = 선정산 대상액 − 선정산 수수료 − 정산 반영 수수료 차액(주문취소·승인취소·매입취소 포함) − 플랫폼 차감액, 플랫폼환급액`이고, 현행 코드는 `선정산 대상액 − 선정산 수수료 + 정산 반영 수수료 차액(취소 환수 포함) − 오프라인 차감 − 플랫폼 차감 + 플랫폼 환급`이다. 부호가 반대다. (확인필요)
- **결과가 같을 수 있다** — 코드 주석이 이 값과 건별 거래 수수료 차액은 모집단도 부호 규약도 다르다고 적는다. 두 값이 서로 다른 부호 규약을 쓰면 표기가 반대여도 결과가 같아진다. 단정하지 않는다.
- **화면은 코드와 맞는다** — 8/15 화면은 `28,371,179 − 180,700 + 31,455 = 28,221,934`, 8/17 화면은 `8,758,508 − 77,176 − 345,481 + 317,689 − 2,394 = 8,651,146`으로 각각 표기값과 일치한다. 화면에 찍히는 부호는 그날 값의 부호이며 산식은 항상 부호를 포함해 더한다.
- **쉼표 묶음도 갈린다** — 정책 원문의 마지막 항 `− 플랫폼 차감액, 플랫폼환급액`은 환급까지 빼는 것으로 읽히는데 코드는 플랫폼 환급을 더한다.
- **대상액 산식도 두 갈래다** — 26.08.15 원문은 선정산 대상액에서 거래수수료 차액을 빼고, 세금계산서 기준 메모(`22:287`)는 그 항이 없다.
- **위치** — 03 §11 선정산 지급액 산식 / 02 §9 차액 용어와 부호 규약 / 어드민 정산 현황 선정산 결과 요약 카드
- **영향** — 부호가 정해져야 지급액 검산 예시를 쓸 수 있고, 차액 정산 탭 집계와 지급액의 대사가 선다. 정하지 않으면 정책서 산식과 화면 값이 서로 다른 근거로 남는다.
- **출처** — Figma `EUK2P3tDOkprcnRmMqNns4` `77:329`(정책 산식) · `77:328` `85:342`(화면 캡처와 검산) · `22:287`(세금계산서 기준 대상액) · admin `app/settlement/overview/PreSettlementTab.tsx:98`·`:466`·`:467` · `types/settlement.ts:434`

### 7-30. 「예상 지급 차액」과 「거래 수수료 차액」이 같은 값을 가리키는가

- **질문** — 가맹점 앱의 `예상 지급 차액`과 어드민의 `거래 수수료 차액`을 한 항목으로 볼 것인가. 부호 규약이 반대인 채로 둘을 나란히 둘 것인가.
- **배경** — Figma 정책 페이지에서 `예상 지급 차액`은 다섯 곳(`5:18` `5:44` `5:45` `5:50` `5:53`)에 나오고 정의문이 한 곳도 없다. 26.08.15 프레임에는 이 라벨이 없고 `거래 수수료 차액`·`정산 반영 수수료 차액` 둘로 갈라져 있다. 현행 코드에서 이 라벨은 가맹점 앱에만 있고 어드민 레포에는 0건이다. (확인필요)
- **부호 규약이 반대다**

| 라벨 | 어디에 | 뺄셈 방향 | 양수의 뜻 | 근거 |
|---|---|---|---|---|
| 거래 수수료 차액 | 어드민 | 실제 − 예상 | 추가 차감 | Figma `77:329` `77:328` `85:342` / admin `PreSettlementTab.tsx:546` · `types/settlement.ts:430-431` |
| 예상 지급 차액 | 가맹점 앱 | 예상 − 실제 방향 | 환급 | merchant `services/salesService.ts:116`·`:188` |
| `fee_adjustment_amt` | DB 원장 (카드) | 예상 − 실제 | 환급 | admin `types/settlement.ts:432` |

- **정정** — 어드민 화면 캡션이 정책 원문과 정반대라는 판정은 성립하지 않는다. 정책 원문·화면 캡처 2건·코드 두 곳이 모두 `실제 − 예상 / 양수 = 추가 차감`으로 같다. 반대 규약을 쓰는 것은 어드민 화면의 같은 항목이 아니라 가맹점 앱의 다른 라벨과 DB 원장 컬럼이다.
- **정의 실마리는 하나뿐** — 가맹점 앱 코드 주석의 `환급 +, 차감 −`이 현재 유일한 정의 근거다. 가맹점 툴팁은 무엇을 왜 반영하는지만 말하고 산식·부호를 말하지 않는다.
- **위치** — `analysis/00_종합.md` C4 / 02 §1 예상 지급 차액 · §9 차액 용어와 부호 규약 / 05 가맹점 화면 라벨
- **영향** — 한 항목으로 보면 두 화면 중 한쪽의 부호 표기를 뒤집어야 하고, 다른 항목으로 보면 두 라벨의 모집단을 갈라 적어야 한다. 정의가 서기 전까지 값·대응을 단정하지 않는다(C4).
- **출처** — Figma `EUK2P3tDOkprcnRmMqNns4` `5:18` `5:35` `5:44` `5:45` `5:50` `5:53` `77:311` `77:329` · merchant `services/salesService.ts:116`·`:188` · `app/settlement/page.tsx:38-40`·`:362` · `app/settlement/adjustments/page.tsx:122` · admin `types/settlement.ts:430-434` · `app/settlement/overview/PreSettlementTab.tsx:546`

### 7-31. 플랫폼 상태값 두 벌의 대응 — 어드민 3값과 가맹점 뱃지 2값

- **질문** — 어드민 요약의 상태 3값(`선정산 중` · `연결 진행 중` · `미연결`)과 가맹점 화면 뱃지 2값(`미연동` · `계좌 연결 필요`)을 어떻게 대응시키는가. 뱃지 명칭을 어느 벌로 확정하는가.
- **배경** — 두 벌이 서로 다른 자리에서 각각 제안·반영됐고 대응이 정의되지 않았다. 운영 요청서는 상태값·배치·노출 방식을 「기획 검토를 통해 정의」로 넘긴다. (확인필요)

| 자리 | 값 | 근거 |
|---|---|---|
| 어드민 요약(가맹점 관리 상세·매출내역 상단) | 선정산 중 / 연결 진행 중 / 미연결 | `IFX4GRC60ibOVCWNrd6VQt` `4277:843` `4277:846` `4277:848` |
| 가맹점 화면 뱃지 | 미연동(회색) / 계좌 연결 필요(연한 빨강) | `IFX4GRC60ibOVCWNrd6VQt` `3802:8` |
| 슬랙 제안 명칭 | 선정산 제외 / 변경필요 | `IFX4GRC60ibOVCWNrd6VQt` `4083:431` |

- **함께 걸린 것** — 쿠팡이츠는 여러 가맹점 중 1곳이라도 계좌를 변경하지 않으면 플랫폼 선정산 토글을 켜지 않고, 불일치가 뜨면 토글을 자동으로 끄는 로직 요구가 슬랙에 있으나 대응 장표가 없다.
- **위치** — 02 §5 상태값 후보 / 05 가맹점 미리 받는 돈 화면 뱃지 / 06 어드민 플랫폼 상태 요약
- **영향** — 대응이 서면 어드민과 가맹점이 같은 상태를 같은 이름으로 부르고, 상태 카탈로그에 한 벌로 올릴 수 있다. 서지 않으면 같은 플랫폼이 화면마다 다른 이름으로 표시된다.
- **출처** — `IFX4GRC60ibOVCWNrd6VQt` `4277:848`(PAYHUG-206 운영 요청서) · `4277:843` `4277:846`(참고안 상태 정의) · `3802:8`(뱃지 2종과 툴팁 문구) · `4083:427` `4083:431`(슬랙 제안·토글 로직 요구)
- **연계** — 거래 단위 미대상 사유 어휘는 §7-19다. 그쪽은 거래 한 건을 거래일 기준으로 가르고, 이 항목은 플랫폼 한 개의 현재 상태를 조회 시점 기준으로 가른다. 두 축을 한 낱말에 담지 않는다.

### 7-32. 「미리 받는 돈」 0원의 사유 구분

- **질문** — 가맹점 화면에서 0원으로 표시되는 카드·배달앱 항목이 연동이 안 된 것인지 연동은 됐으나 선정산이 꺼진 것인지를 화면에서 갈라 보이는가. 「연동됐으나 선정산 off」 상태에 별도 표시를 두는가.
- **배경** — 가맹점은 개별 연동 상태도, 선정산 on/off 여부도 확인할 수 없고 두 경우 모두 0원으로 표시된다. 뱃지 2종(미연동 · 계좌 연결 필요)은 장표에 반영됐으나 「연동됐으나 선정산 off」에 해당하는 값은 없고, 그 상태를 별도로 표시할지 묻는 질문이 미회신이다. (확인필요)
- **위치** — 05 가맹점 미리 받는 돈 화면 / 02 §5 상태값 후보 / 이 문서 §7-31
- **영향** — 갈라 보이면 0원의 사유가 화면에서 읽히고 문의가 줄어든다. 두면 같은 0원이 서로 다른 원인을 감춘 채 남는다. 고도화 방향으로 「내 정보 > 연동 플랫폼 관리」에서 개별 관리 화면을 두자는 의견이 함께 적혀 있다.
- **출처** — `IFX4GRC60ibOVCWNrd6VQt` `4083:427` `4083:431`(슬랙 원문) · `3802:8`(뱃지 노출 기준) · `3658:2837`(PAYHUG-70, 메인 카드 0원과 상세 403원이 어긋난 캡처 2장)

### 미확정 3대 — 이번 라운드에서도 변동 없음

C1(수수료율)·C2(지급 캘린더)·C4(예상 지급 차액) 전부 미확정 유지다(회의 U8·U9·U10). 회의에서 요율을 확정한 발언은 없고, 지급 캘린더는 운영 시각 재확인에 그쳤을 뿐 계약 '매일'과의 정합은 논의되지 않았으며, 예상 지급 차액은 언급 자체가 없다. 어드민 '예상 수수료' 열 요구(V8)와 §7-11의 0.6·0.2 예시를 C4·C1 확정 근거로 오독하지 말 것. 정책서 신판에서 C2는 검정(운영 확정)과 빨강(정합 확인필요) 2색으로 표기만 나눴을 뿐 어느 값도 정합으로 판단하지 않았다.

2026-09-07 기준 Jira 티켓 25건 조사에서도 세 건 모두 미확정으로 남는다.

| 레지스터 | 티켓에서 확인된 것 | 미확정으로 남는 것 |
|---|---|---|
| C1 | `PAYHUG-260` 예시에서 채권매입수수료 1.0%·시스템이용료 0.11%가 역산된다. `PAYHUG-229`가 투자자 할인율을 0.11%로 정의한다 | 예시 비율이 실제 요율인지(§7-18). 투자자 할인율은 가맹점 수수료율과 다른 층이므로 C1에 흡수시키지 않는다 |
| C2 | 하루 두 회차(오전 11시 30분·오후 6시)가 어드민 티켓과 가맹점 화면 양쪽에서 확인된다(03 §5). 쿠팡이츠 정산금 지급일은 영업일 기준 D+4다 | 거래일로부터 며칠 뒤 지급인지. 계약 '매일'·운영 D+1·DB 설계 익익영업일의 정합. 쿠팡이츠 D+4는 플랫폼이 락계좌에 넣는 날이라 이 축과 다르다 |
| C4 | 25건 어디에도 예상 지급 차액 정의가 없다 | 정의 전부 |

Figma 정책 페이지(`EUK2P3tDOkprcnRmMqNns4` `1:10`)와 [운영] 개선건 페이지(`IFX4GRC60ibOVCWNrd6VQt` `3174:746`) 전수 판독에서도 세 건 모두 미확정으로 남는다. 근거가 늘어난 만큼 질문의 자리가 좁아졌을 뿐이다.

| 레지스터 | 판독으로 확인된 것 | 미확정으로 남는 것 |
|---|---|---|
| C1 | 산식 원문(`채권매입수수료 = 결제주체지급금액 × 선정산수수료율, 소수점이하 올림`)과 근거 조항(양수도계약서 제17조). 26.08.09 원장 실측 3건이 `0.6% + 올림`과 전건 일치하고 `1%`·`0.25%`는 3건 전부 어긋나며 `0.604%`는 셋째 건에서 어긋나 배제된다. 판독 파일에 `0.604%`·`0.6+0.11+0.1%` 표기가 0건이다 | 요율 값 자체. 표본 3건은 확정에 모자라므로 `0.6%`를 채택하지 않는다. 문서 `1%`와 실측 `0.6%`의 관계(§7-27). 체인 단계별 병존 값(§7-28). C1 후보 표 두 줄의 출처 재확인 |
| C2 | 정책 원문에 `주말·공휴일 포함 D+1 정산`·`오전 11:30~12:00`·`매일 11시30분·18시`가 있다. 기준시간은 전일 00시 00분~23시 59분, 회수 대사일은 매일 19시, 카드 미매입 회수는 승인일 D+5 영업일이다. `D+2` 표기는 판독 파일에 없다 | 기산일 축의 정합(계약 매일 · 운영 D+1 · DB 설계 D+2). 같은 파일 안에서 갈리는 지급 회차(워드 1회 · 프레임 2회). C2 세 후보 중 DB 설계 D+2만 판독 파일 밖 근거다 |
| C4 | 라벨이 다섯 곳(`5:18` `5:44` `5:45` `5:50` `5:53`)에 나오고 **정의문이 한 곳도 없다**는 사실. 26.08.15 프레임에서 `거래 수수료 차액`·`정산 반영 수수료 차액` 둘로 갈린다. 현행 코드에서 이 라벨은 가맹점 앱에만 있고 부호 규약은 `환급 +, 차감 −`이다 | 정의 전부. 가맹점 앱 라벨과 어드민 `거래 수수료 차액`이 같은 값인지(§7-30). 값과 대응을 단정하지 않는다 |
