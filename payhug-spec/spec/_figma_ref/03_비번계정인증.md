# 비밀번호·계정 인증 정책 화면설계서 분석 — Figma 그린버전

> 출처: Figma `IFX4GRC60ibOVCWNrd6VQt`(PH_화면설계서) / 페이지 `1216:2630` 이름 **`[가맹점_홈페이지] 화면설계 (그린버전)`**
> 조사일: 2026-07-20. 방법: `get_metadata`(section/frame 전수) + `get_screenshot`(정책 가이드 타일 고해상) + `get_design_context`/메타 text-node 추출(화면설계서 벡터 텍스트).
> 이 페이지에는 비번·계정 인증 관련 **완성 산출물 3덩어리**가 좌측 음수좌표(x≈-17,000) 영역에 모여 있다.
>  - `3165:527` **section "비밀번호 정책"** (x=-17478,y=-3066, 11436×11841) — 래스터 이미지 10장으로 된 **정책·UI 가이드 문서** (작성 2026.07.02, 주식회사 페이허그)
>  - `3180:773` **section "계정 인증 (로그인, 회원가입, 비밀번호 찾기, 비밀번호 재설정)"** (x=-17469,y=11167, 10874×8122) — **4개 화면설계서** 벡터 프레임 (작성자 이서준, Date 2026.07.02)
>  - `3181:1168` **section "[가맹점 랜딩페이지] 로그인 버튼 생성"** — (본 태스크 범위 밖, 랜딩 CTA 관련)
> 별도로 최상위에 `3188:1182` **frame "로그인 (개선)"**(PH_LO)이 있으며, `3180:773` 안의 로그인 화면설계서와 **텍스트 완전 동일**(개선안 사본).
> ⚠️ 이 산출물은 실제 프론트 레포(파일 `Tcf...` 페이지 `511:174`)가 아니라 **디자인 화면설계 파일**에 있다. 아래 어드민 11·가맹점 02가 "미문서화/코드에만 존재"라 적은 항목의 **문서 근거**가 여기서 처음 확보된다.

---

## 1. 프레임 · 디스크립션 스타일 (02_예시스타일과 비교)

이 페이지의 두 산출물은 **스타일이 서로 다르고**, 02(최지원 B안, 3분할+번호마커)와도 다르다.

### (A) "비밀번호 정책 · UI 가이드" — 정책 문서형 (`3165:527`)

- **형식**: 벡터가 아니라 **PNG 10장**(`3165:529`~`538`, 각 2479×3509, 4열×3행 배치)으로 임포트된 **문서형 가이드**. Payhug 로고 + H1 `비밀번호 정책 · UI 가이드` + 부제 `로그인·회원가입·비밀번호 찾기·재설정 화면의 검증 규칙과 상태별 UI 예시` + `2026.07.02 · 주식회사 페이허그`.
- **구성**: ① 번호형 정책 섹션(`1. 공통 비밀번호 정책` … `4. 색상 상태`) — 각 섹션은 **회색 헤더행 표**(구분/정책, 입력값/처리방식 등) ② `화면별 UI 예시` — **폰 목업 + 우측 상태카드** 반복. 상태카드는 좌상단 **알약 배지**(`기본`=회색 / `오류`=빨강 / `성공`=초록) + 제목 + 1줄 설명 + `입력 예시` 코드칩 + 불릿 3개 + **`결과` 강조박스**(초록/빨강) + 노랑 **핵심 규칙 콜아웃**(느낌표 아이콘). ③ 말미 `최종 사용자 노출 문구 모음` 표.
- **색 토큰(문서 §4 원문)**: Success 텍스트 `#28A745`/테두리 `#9FE870` · Error `#FF383C` · Default 텍스트 `#7E8299`/점 `#C7CAD6`. (필드 포커스 연그린은 화면설계서 쪽 `#C9E9B4`.)
- **02와 차이**: 02는 화면 1개=1프레임에 번호마커·우측 key note. 이건 **여러 화면·상태를 한 문서에 카드로 나열**한 "정책+UI 스펙북". 개조식·명사종결이 아니라 **완결형 서술("~합니다")과 표**를 쓴다.

### (B) 4개 "…화면설계서" — Table 메타헤더형 (`3180:773`)

- **형식**: 프레임 폭 1920, 상단 **2행 메타 표**(라벨셀 회색 `#EDEDED`, 값셀 흰색, 우측 Payhug 로고 병합) — 02의 메타바와 동일 骨格이나 **좌측 목업+우측 패널 3분할이 아니라**, 아래로 **케이스별 블록을 세로 스택**한다.
- **케이스 블록 1개** = 파란 볼드 소제목(`text-[#4c4fe1] 15px`, 예 `기본 케이스`·`에러 케이스 — 휴대전화번호 형식 오류`) + 목업 이미지(1240px) + 하단 **회색 캡션바**(`상태`/`입력`/`결과` 라벨 + 값). `입력`은 **코드칩**(Menlo), `결과`는 초록(`#1e8a3b`) 또는 빨강(`#d42b30`) 세미볼드.
- **메타 표 원문 값**(4개 화면설계서):

| Page | Page ID | Flow | System·Date·Writer |
|---|---|---|---|
| 페이허그 로그인 | `PH_LO` | `페이허그 소개 > 로그인` | pc · 2026.07.02 · 이서준 |
| 페이허그 회원가입 · 비밀번호 설정 | `PH_JO_PW` | `페이허그 소개 > 로그인 > 회원가입 약관 동의 > 본인인증 > 비밀번호 등록` | pc · 2026.07.02 · 이서준 |
| 페이허그 비밀번호 찾기 | `PH_PW_FI` | `로그인 > 비밀번호 찾기` | pc · 2026.07.02 · 이서준 |
| 페이허그 비밀번호 재설정 | `PH_PW_RE` | `로그인 > 비밀번호 찾기 > 비밀번호 재설정` | pc · 2026.07.02 · 이서준 |

- **Page ID 규칙**: 02의 `ST_*`(선정산)와 달리 인증군은 `PH_*` 접두(PH_LO / PH_JO_PW / PH_PW_FI / PH_PW_RE). 화면설계서 실제 작성(태스크 #9) 시 어드민은 `ST_*`, 가맹점 인증은 `PH_*` 체계를 이어 쓰면 된다.

---

## 2. 비밀번호 · 계정 인증 정책 원문 (그대로 인용) — `확정`(원문 확인)

> 아래는 `3165:527` 가이드와 `3180:773` 화면설계서에서 **문자 그대로** 추출. 근거일 2026.07.02, 작성 주식회사 페이허그/이서준. node ID 병기.

### 2-1. 공통 비밀번호 정책 (가이드 §1, 타일 `3165:529`)

| 구분 | 정책 |
|---|---|
| 길이 | **8자 이상 16자 이하** |
| 영문 | 포함 필수 |
| 숫자 | 포함 필수 |
| 특수문자 | 포함 필수 |
| 공백 | 입력 불가 |
| 한글 | 입력 불가 |
| 사용 가능한 특수문자 | `! @ # $ % ^ & * ( )` |

- 콜아웃 원문: "회원가입·비밀번호 재설정 화면에서는 **8~16자·영문·숫자·특수문자 4개 조건만 기본 체크 항목으로 노출**합니다. 공백·한글·허용되지 않은 특수문자는 상시 노출하지 않고, 해당 값이 입력된 경우에만 오류 문구로 안내합니다."

### 2-2. 비밀번호 입력 처리 (가이드 §2, 타일 `3165:529`)

| 입력값 | 처리 방식 |
|---|---|
| 공백 | 입력값에 저장하지 않음 |
| 한글 | 입력값에 저장하지 않음 |
| 16자 초과 입력 | 16자까지만 입력 가능 |
| 허용되지 않은 특수문자 | 입력값은 유지하되 오류 상태로 처리 |

### 2-3. 오류 메시지 우선순위 (가이드 §3, 타일 `3165:530`) — "여러 개 발생해도 가장 먼저 해결할 오류 1개만 노출"

| 순위 | 조건 | 오류 문구 |
|---|---|---|
| 1 | 공백 입력 시도 | `비밀번호에 공백을 사용할 수 없습니다.` |
| 2 | 허용되지 않은 특수문자 포함 | `사용 가능한 특수문자: ! @ # $ % ^ & * ( )` |
| 3 | 길이·영문·숫자·특수문자 미충족 | `영문, 숫자, 특수문자를 포함한 8~16자로 입력해주세요.` |

- 표시 방식 원문: "오류는 필드 바로 아래 **인라인으로만 표시하고 토스트는 사용하지 않습니다.** … 메시지 영역 높이를 미리 확보하고, 짧은 fade-in으로 표시합니다." (공백 오류는 "경고 후 **약 2.2초 뒤 사라짐**" — 화면설계서 캡션 `3180:900`)

### 2-4. 실시간 조건 체크·특수문자 AND 판정 (가이드 UI 예시 타일 `3165:533`)

- 회원가입/재설정은 입력 중 **4개 조건 실시간 체크리스트**(충족=그린 체크, 미충족=회색). 로그인은 체크리스트를 **쓰지 않고** 오류 시 인라인 문구만.
- 특수문자 판정 = "**허용 문자 있음 AND 비허용 문자 없음**". 예: `abcd123!.` → 허용문자(!) 있어도 비허용(`.`) 존재 → 특수문자 체크 회색 유지 + 오류.
- 핵심 규칙 원문: "'조건은 다 그린인데 버튼은 비활성'인 모순 상태를 막기 위해 특수문자 체크를 그린으로 만들지 않고 **회색 + 오류 문구로 처리**." / 성공 시 "4개 체크리스트는 축약 성공 문구로 대체됨."

### 2-5. 아이디(로그인) 형식 정책 — 화면설계서 `PH_LO`(`3180:774`) + 가이드 타일 `3165:531`

- 아이디 = **사업자등록번호 또는 휴대전화번호, 숫자만 입력**(숫자 아닌 값 자동 제거).
- **`01`로 시작 → 휴대전화번호로 판별**, 숫자 **10~11자리**여야 통과. (예 `010123`=6자리 → `올바른 휴대전화번호 형식이 아닙니다.`)
- **`01`로 시작하지 않으면 → 사업자등록번호로 판별.** 숫자 **10자리 + 체크섬 검증**. (예 `1234567890`=10자리이나 "**체크섬 검증 불통과**" → `올바른 사업자등록번호 형식이 아닙니다.`)
- 로그인 화면설계서 케이스: 기본="입력 전·placeholder만 노출·로그인 버튼 비활성" / 포커스="아이디 필드 포커스 — 연그린 테두리(`#C9E9B4`)" / 활성="아이디 `01012345678`·비밀번호 `Payhug12!` → 두 필드 그린 테두리·로그인 버튼 활성".

### 2-6. 휴대전화번호 본인인증(비밀번호 찾기) 형식 — `PH_PW_FI`(`3180:1014`) + 타일 `3165:536`,`537`

- placeholder `숫자만 입력`, **숫자만 저장, 최대 11자**(한글·영문 자동 제거). `01`로 시작 + 숫자 **10~11자리** 필요, **입력 중 실시간 검증**.
- 성공(`01012345678`) → `본인 인증 버튼 활성(그린) → 비밀번호 재설정 화면 이동`. 이 화면은 **비밀번호 정책 미적용**(번호만 검증).

### 2-7. 최종 사용자 노출 문구 모음 (가이드, 타일 `3165:537`) — 그대로 사용할 문안

| 상황 | 문구 |
|---|---|
| 비밀번호 공백 입력 | `비밀번호에 공백을 사용할 수 없습니다.` |
| 비밀번호 허용 안 된 특수문자 | `사용 가능한 특수문자: ! @ # $ % ^ & * ( )` |
| 비밀번호 정책 미충족 | `영문, 숫자, 특수문자를 포함한 8~16자로 입력해주세요.` |
| 비밀번호 사용 가능 | `사용 가능한 비밀번호입니다.` |
| 비밀번호 확인값 불일치 | `비밀번호가 일치하지 않습니다.` |
| 비밀번호 확인값 일치 | `비밀번호가 일치합니다.` |
| 비밀번호 확인 필드 기본 안내 | `비밀번호를 한번 더 입력해주세요.` |
| 로그인 아이디 미입력 | `사업자등록번호 또는 휴대전화번호를 입력해주세요.` |
| 휴대전화번호 형식 오류 | `올바른 휴대전화번호 형식이 아닙니다.` |
| 사업자등록번호 형식 오류 | `올바른 사업자등록번호 형식이 아닙니다.` |
| 비밀번호 찾기 미입력 | `휴대전화번호를 입력해주세요.` |

- ⚠️ **회원가입 비밀번호 필드 placeholder는 가이드 표와 미세 불일치**: 목업 회색 안내가 `영문, 특수문자, 숫자 조합 **8자 이상**`(상한 16자 누락)으로 그려짐(`3180`). 정책 표는 `8자 이상 16자 이하`. → 화면설계 확정 시 placeholder 문구 통일 필요(`확인 필요`).

### 2-8. 정책에 **없는** 항목 (여전히 `확인 필요`)

- **로그인 시도 횟수 제한 / 계정 잠금(lockout) / 2FA**: 이 가이드·화면설계서 어디에도 없음. 오직 입력 형식·비밀번호 구성 검증만 다룸.
- **세션(토큰) 유효시간·갱신 정책**: 없음(가맹점 02 §16의 150분은 코드 하드코딩값, 어드민 11 B-9 동일 — 근거 미해소).
- **KCB 본인인증을 게이트로 두는 정책 근거**: 없음(Flow에 "본인인증" 단계만 표기).

---

## 3. 우리 208 화면 매핑 (재사용 후보)

이 그린버전 화면설계서 4종은 **가맹점 웹 인증군(M-02)** 과 1:1 대응한다(가맹점 `02_인증.md` 기준). 어드민(A-11)은 자가 가입·비번찾기 화면이 없어 로그인만 대응.

| 그린버전 (IFX file) | Page ID | → 우리 화면 (M-02, `merchant/02_인증.md`) | 프론트 node (`Tcf...` 511:174) | 비고 |
|---|---|---|---|---|
| 페이허그 로그인 (`3180:774`, 사본 `3188:1182`) | `PH_LO` | 로그인 `/login` | `845:2`(기본)·`851:2`·`856:2`·`862:2` | 아이디 체크섬·비번정책 원문이 M-02 행3·36 근거를 채움 |
| 회원가입·비밀번호 설정 (`3180:907`) | `PH_JO_PW` | 회원가입 3단계 비번설정 `/signup` | `890:2`(기본)·`894:2`(체크리스트)·`898:2`(CapsLock) | 실시간 4조건 체크리스트·확인 일치 = M-02 행32·33 |
| 비밀번호 찾기 (`3180:1014`) | `PH_PW_FI` | 비번찾기 1단계 본인인증 `/find-password` | `907:2`·`823:2` | 번호 형식만 검증(비번 미적용) = M-02 행42~45 |
| 비밀번호 재설정 (`3180:1082`) | `PH_PW_RE` | 비번찾기 2단계 재설정 | `818:2`·`829:2` | 회원가입과 동일 규칙 = M-02 행48 |

- **재사용 판단**: 이 그린버전 4종은 **가맹점 인증 화면설계서의 최신 확정 시안**(2026.07.02)이다. 태스크 #9(어드민 `303:173` 화면설계서 작성)에서 로그인 설계 시 **`PH_LO` 케이스 스택·캡션바 포맷을 그대로 이식** 가능. 단 어드민 로그인은 형식검증 없이 공란 여부만 보므로(A-11 B-2~4) 케이스 구성이 더 단순.

---

## 4. 어드민 11 · 가맹점 02 "확인 필요" 채움/잔존

이 Figma가 처음으로 **문서 근거**를 제공하는 항목(→ `가설`/`미문서화` 강등분을 화면정책 `확정` 근거로 승격 가능):

- ✅ **비밀번호 구성 정책**(가맹점 02 §C `[ ] 비밀번호 구성 정책의 문서 근거`, B행36): 8~16자·영문+숫자+특수문자·허용 `!@#$%^&*()`·공백/한글 불가 — **§2-1~2-4에 원문 확정.** `lib/passwordPolicy.ts` 코드와 일치.
- ✅ **로그인 아이디 형식**(가맹점 02 §C `[ ] 로그인 아이디로 사업자등록번호 허용`, B행3): `01`시작=휴대폰(10~11), 그외=사업자(10+체크섬) — **§2-5에 원문 확정.**
- ✅ **사용자 노출 문안**: 오류·성공 12종 확정(§2-7) — 어드민/가맹점 인라인 문구 표준으로 채택 가능.

여전히 **미해소(확인 필요 유지)**:

- ❌ 세션 TTL 150분 / 리프레시(가맹점 02 §16, 어드민 11 B-9) — 이 문서에 없음.
- ❌ 로그인 시도 제한·계정 잠금·오픈리다이렉트 검증(어드민 11 §C) — 이 문서에 없음.
- ❌ KCB 본인인증 게이트 정책 근거(가맹점 02 §C) — Flow 단계 표기만 있고 정책 조항 아님.
- ⚠️ 회원가입 placeholder "8자 이상"(상한 누락) vs 정책 "8~16자" 표기 불일치(§2-7) — 화면 확정 시 통일.

---

## 부록. 근거 node id

- 정책 가이드 타일: 표지·§1·§2 `3165:529` / §3·§4·로그인예시 `3165:530` / 로그인 기본·아이디오류 `3165:531` / 비번정책미충족·회원가입설정 `3165:532` / 실시간체크·비허용특수문자 `3165:533` / 공백·전체충족 `3165:534` / 확인불일치·재설정성공 `3165:535` / 비번찾기기본·번호오류 `3165:536` / 유효번호·최종문구모음 `3165:537`.
- 화면설계서: 로그인 `3180:774`(사본 `3188:1182`) / 회원가입·비번설정 `3180:907` / 비번찾기 `3180:1014` / 비번재설정 `3180:1082`.
